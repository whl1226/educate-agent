# 方案PPT v5 内容增密与政策佐证 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把方案PPT 10 个概念/收尾页从"内容稀疏、靠拉伸填充"改为"内容为王、栅格铺满"，并嵌入 2 张情境图 + 3 张政策官网截图，产出 24 页 16:9 竞赛级 PDF。

**Architecture:** 自包含 HTML（`初赛提交材料/方案PPT.html`）→ Puppeteer + Chrome 打印为 PDF。所有新增内容来自产品既有资产（38 能力/16 工具/D1–D7/BKT/ZPD/SSE/39 测试/MIT/演示剧本），零编造。新组件样式全部写入 `<style>` 块，页面 HTML 按任务逐页插入。

**Tech Stack:** 自包含 HTML + CSS（Noto Serif/Sans SC via jsdelivr）、Puppeteer-core + Chrome（截图/PDF）、RapidOCR + pdfplumber（自检）。

## Global Constraints

- **零编造**：任何数字/术语必须来自 spec §2 资产清单；拿不准就删掉或写"演示实测量级"。
- **禁拉伸**：禁止用 `margin-top:auto` / 拉大 `gap` 制造"看似饱满"；内容不足就加真实内容实体。
- **图片版权**：情境图仅用 Unsplash/Pexels 可商用图；政策截图必须真实访问官网抓取，caption 配"文件·文号·机构·日期·网址"。
- **风格延续**：沿用"田园编辑部"语言（纸底 `#FBF8F1`、深绿 `#2E6B34`、陶土 `#C2572E`、细线边框）；不引入新配色/emoji/AI 生成图。
- **溢出红线**：每页改完跑 `scripts/audit_layout.mjs`，内容页必须 overflow=0。
- 功能页 7–19 与 `作品简介.*` 不动。

---

### Task 1: 抓取政策截图 + 下载情境图片（assets 资产准备）

**Files:**
- Create: `scripts/capture_assets.mjs`
- Produce: `初赛提交材料/assets/policy-quality.png`、`policy-rural.png`、`policy-digital.png`、`scene-1.jpg`、`scene-2.jpg`

**Interfaces:**
- 后续 Task 3/6/8 引用 `assets/policy-*.png`，Task 3/9 引用 `assets/scene-*.jpg`，路径为相对 `方案PPT.html` 的 `assets/...`。

- [ ] **Step 1: 写抓取脚本**

```js
// scripts/capture_assets.mjs — 真实访问官网截政策页 + 下载可商用情境图
import { writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';
import puppeteer from 'puppeteer-core';

const CHROME = 'C:/Program Files/Google/Chrome/Application/chrome.exe';
const OUT = 'F:/代码文件/vibe coder/无界应用-教育智能体/初赛提交材料/assets';

// 三个政策页（已实测 moe.gov.cn 可达）：标题/文号/关键条款在页面首屏区域
const POLICIES = [
  { name: 'policy-quality', // 义务教育质量意见
    url: 'https://www.moe.gov.cn/jyb_xxgk/moe_1777/moe_1778/201907/t20190708_389416.html',
    kw: ['深化教育教学改革', '义务教育质量', '中发'] },
  { name: 'policy-rural',   // 新时代乡村教师队伍建设意见
    url: 'https://www.moe.gov.cn/srcsite/A10/s3735/202009/t20200903_484941.html',
    kw: ['新时代乡村教师队伍建设', '教师〔2020〕5号'] },
  { name: 'policy-digital', // 国家教育数字化战略行动（国家智慧教育平台接入规范页，含"教育数字化战略行动"）
    url: 'https://www.moe.gov.cn/srcsite/A16/s3342/202208/t20220819_653868.html',
    kw: ['教育数字化战略行动', '智慧教育'] },
];

const browser = await puppeteer.launch({ executablePath: CHROME, headless: 'new',
  args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-gpu'] });

// 1) 政策截图：截页面首屏（标题+文号区域），2x 放大保证可读
for (const p of POLICIES) {
  const page = await browser.newPage();
  await page.setViewport({ width: 1280, height: 900, deviceScaleFactor: 2 });
  await page.goto(p.url, { waitUntil: 'networkidle2', timeout: 45000 });
  await page.evaluate(() => document.fonts.ready);
  const html = await page.evaluate(() => document.body.innerText);
  const missing = p.kw.filter(k => !html.includes(k));
  if (missing.length) { console.warn(`[WARN] ${p.name} 页面未命中关键词: ${missing}`); }
  // 截前 40% 页高（标题+文号+开头正文），存 PNG
  await page.screenshot({ path: resolve(OUT, p.name + '.png'),
    clip: { x: 0, y: 0, width: 1280, height: 640 } });
  console.log(`[OK] ${p.name} (kw缺${missing.length}个)`);
  await page.close();
}

// 2) 情境图：Unsplash Source 直链（可商用、免登录），失败则跳过不阻塞
const SCENES = [
  ['scene-1', 'https://images.unsplash.com/photo-1580582932707-520aed937b7b?w=1200&q=70'], // 乡村教室
  ['scene-2', 'https://images.unsplash.com/photo-1509062522246-3755977927d7?w=1200&q=70'], // 教室黑板
];
for (const [name, url] of SCENES) {
  try {
    const page = await browser.newPage();
    await page.goto(url, { waitUntil: 'load', timeout: 45000 });
    const data = await page.evaluate(() => fetch(location.href).then(r => r.blob()).then(b => b.arrayBuffer()));
    await writeFile(resolve(OUT, name + '.jpg'), Buffer.from(data));
    console.log('[OK] ' + name);
    await page.close();
  } catch (e) { console.warn('[SKIP] ' + name + ' 下载失败: ' + e.message); }
}
await browser.close();
```

- [ ] **Step 2: 运行脚本**

Run: `node scripts/capture_assets.mjs`
Expected: `[OK] policy-quality (kw缺0个)` 等三行 + 两个 `[OK] scene-*`；任何 `[WARN]`/`[SKIP]` 都要记录（见 Step 3）。

- [ ] **Step 3: 验证产物**

Run: `ls -la 初赛提交材料/assets/policy-*.png 初赛提交材料/assets/scene-*.jpg`
Expected: 5 个文件存在且 >50KB。
若 policy-digital 未命中关键词（页面改版），换 WebSearch 关键词「教育部2022年工作要点 实施教育数字化战略行动 moe.gov.cn」取新 URL 重试；若 scene 下载失败，从 Pexels 搜索「rural classroom china」取直链重试（或跳过，Task 3/9 中把图位换成数据卡，不阻塞）。

- [ ] **Step 4: OCR 抽查政策截图可读性**

Run: `python scripts/ocr_verify.py 方案PPT 初赛提交材料/方案PPT.pdf`（复用环境；或直接 Read 打开 policy-*.png 目检）
Expected: 截图内标题/文号可辨识，无大片黑块。

- [ ] **Step 5: Commit**

```bash
git add scripts/capture_assets.mjs 初赛提交材料/assets/
git commit -m "feat: 抓取3张政策官网截图与2张情境图（方案PPT v5 素材）"
```

---

### Task 2: CSS 基建（新增组件样式 + 收紧间距）

**Files:**
- Modify: `初赛提交材料/方案PPT.html`（`<style>` 块内，`</style>` 前插入；同时把概念页容器 `gap` 适度收紧：`.insp`/`.pain`/`.logic-wrap`/`.agent-row` 的 `gap` 减 1mm，消除"拉伸感"）

**Interfaces:**
- 提供以下 class 供 Task 3–9 使用：`.src-note` `.sol` `.pipe` `.trace` `.policy` `.why-now` `.timeline30` `.act` `.milestone`

- [ ] **Step 1: 插入新组件 CSS**

在 `<style>` 中 `.end { ... }` 规则后插入：

```css
  /* ===== v5 增密组件 ===== */
  .src-note { font-size: 8.5pt; color: var(--ink-faint); margin-top: 1.5mm; letter-spacing: .02em; }
  .sol { margin-top: 3mm; border-top: 1px dashed var(--line); padding-top: 2.5mm; font-size: 9.5pt; color: var(--ink-soft); }
  .sol b { color: var(--green-deep); }
  .pipe { margin-top: 4mm; display: grid; grid-template-columns: repeat(4, 1fr); gap: 3mm; }
  .pipe .pn { background: #fff; border: 1px solid var(--line); border-left: 1.2mm solid var(--green); border-radius: 2mm; padding: 3mm 4mm; }
  .pipe .pn .t { font-size: 9.5pt; font-weight: 700; color: var(--green-deep); }
  .pipe .pn .d { font-size: 8.5pt; color: var(--ink-faint); margin-top: 1mm; }
  .trace { margin-top: 4mm; background: #F4F1E8; border: 1px solid var(--line); border-radius: 2.5mm; padding: 3.5mm 5mm; }
  .trace .tt { font-size: 9pt; font-weight: 700; color: var(--green-deep); letter-spacing: .08em; margin-bottom: 2mm; }
  .trace .ln { font-family: ui-monospace, Consolas, monospace; font-size: 8.5pt; color: var(--ink-soft); line-height: 1.55; }
  .trace .ln b { color: var(--clay); }
  .policy { margin-top: 4mm; border: 1px solid var(--line); border-radius: 2.5mm; overflow: hidden; background: #fff; }
  .policy img { width: 100%; display: block; }
  .policy .cap { font-size: 8pt; color: var(--ink-faint); padding: 2mm 3.5mm; border-top: 1px solid var(--line); line-height: 1.5; }
  .policy .cap b { color: var(--green-deep); }
  .why-now { margin-top: 4mm; display: grid; grid-template-columns: repeat(3, 1fr); gap: 4mm; }
  .why-now .w { background: #fff; border: 1px solid var(--line); border-radius: 2.5mm; padding: 3.5mm 4.5mm; }
  .why-now .w .n { font-family: "Noto Serif SC", serif; font-weight: 900; font-size: 11pt; color: var(--clay); }
  .why-now .w p { font-size: 9.5pt; color: var(--ink-soft); margin-top: 1.5mm; }
  .timeline30 { margin-top: 5mm; display: grid; grid-template-columns: repeat(4, 1fr); gap: 3mm; }
  .timeline30 .tl { background: #fff; border: 1px solid var(--line); border-radius: 2.5mm; padding: 3mm 4mm; position: relative; }
  .timeline30 .tl .dt { font-size: 8.5pt; font-weight: 700; color: var(--clay); }
  .timeline30 .tl p { font-size: 9pt; color: var(--ink-soft); margin-top: 1mm; }
  .act { display: flex; gap: 4mm; margin-top: 6mm; }
  .act .a { flex: 1; background: rgba(251,248,241,.08); border: 1px solid rgba(251,248,241,.35); border-radius: 3mm; padding: 4mm 5mm; }
  .act .a .t { font-family: "Noto Serif SC", serif; font-weight: 900; font-size: 11pt; color: #FBF8F1; }
  .act .a p { font-size: 9pt; color: rgba(251,248,241,.82); margin-top: 1.5mm; }
  .milestone { margin-top: 2.5mm; font-size: 8.5pt; color: var(--ink-faint); }
  .milestone b { color: var(--green-deep); }
```

- [ ] **Step 2: 收紧概念页间距（消除拉伸感）**

在 `.insp {`、`.pain {`、`.logic-wrap {`、`.agent-row {` 规则中把 `gap` 分别改为 `7mm`、`5mm`、`4mm`、`4mm`；`.tech-grid {` 的 `gap` 改 `4mm`；`.biz-pillars {` gap 改 `4mm`。**不做任何 `margin-top:auto` 式填充**。

- [ ] **Step 3: 验证无样式破坏**

Run: `node scripts/audit_layout.mjs`（仅看 方案PPT 段）
Expected: 内容页（索引 ≥2）overflow=0；封面 watermark 警告可忽略。

- [ ] **Step 4: Commit**

```bash
git add 初赛提交材料/方案PPT.html
git commit -m "style: 方案PPT v5 增密组件 CSS 基建 + 概念页间距收紧"
```

---

### Task 3: 页 2 灵感页增密（数据来源 + 为什么是现在 + 情境图 + 政策截图）

**Files:**
- Modify: `初赛提交材料/方案PPT.html`（`<!-- 2 · 灵感与初心 -->` section）

- [ ] **Step 1: 数据卡加来源标注 + 底部"为什么是现在"**

在 4 个 `.stat` 的 `.txt` 末尾各加 `<div class="src-note">来源：教育部公开统计 / 产品调研场景</div>`（第 1 张写"来源：教育部 2023 年教育统计公报（乡村小规模学校）"、第 2–4 张写"来源：产品调研与公开报道"）。
在 `.insp` 闭合后插入：

```html
    <div class="why-now">
      <div class="w"><div class="n">① 网络与设备</div><p>乡镇学校互联网接入率持续提升、智能终端逐步普及——<b>《关于深化教育教学改革全面提高义务教育质量的意见》第 11 条</b>明确"免费为农村和边远贫困地区学校提供优质学习资源"。</p></div>
      <div class="w"><div class="n">② 大模型成本</div><p>推理成本快速下降、开源模型可本地部署——乡芽<b>双模式降级</b>：配 Key 走 LLM，无 Key 走规则引擎，乡镇断网仍可用。</p></div>
      <div class="w"><div class="n">③ 政策明确</div><p><b>国家教育数字化战略行动</b>统筹智慧教育平台体系，"AI+教育"进入政策窗口期——开源公共能力可被区域平台直接接入。</p></div>
    </div>
```

- [ ] **Step 2: 嵌入情境图 + 义务教育政策截图**

在 `.why-now` 后插入（放一个两栏行：左情境图、右政策截图，图高 ≤38mm 防溢出）：

```html
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:5mm;margin-top:4mm">
      <div class="policy"><img src="assets/scene-1.jpg" alt=""><div class="cap">情境：乡村小规模学校课堂（Unsplash · 可商用）</div></div>
      <div class="policy"><img src="assets/policy-quality.png" alt=""><div class="cap"><b>中共中央 国务院《关于深化教育教学改革全面提高义务教育质量的意见》</b>（中发〔2019〕26号）· 教育部官网 moe.gov.cn</div></div>
    </div>
```

- [ ] **Step 3: 验证**

Run: `node scripts/audit_layout.mjs` → 页 2 overflow=0；Read `assets/policy-quality.png` 确认截图含标题与文号。

- [ ] **Step 4: Commit**

```bash
git add 初赛提交材料/方案PPT.html
git commit -m "feat: 页2灵感页增密（数据来源/为什么是现在/情境图/政策截图）"
```

---

### Task 4: 页 4 产品逻辑页增密（一次作答的数据管道）

**Files:**
- Modify: `初赛提交材料/方案PPT.html`（`<!-- 4 · 产品逻辑 -->` section，`.logic-chain` 之后、`.caps` 之前插入）

- [ ] **Step 1: 插入数据管道**

```html
    <div class="pipe">
      <div class="pn"><div class="t">一次学生作答</div><div class="d">D6 原生行为数据：动作 + 时长 + 结果</div></div>
      <div class="pn"><div class="t">BKT 认知诊断</div><div class="d">掌握度 + 置信度 + 证据题，隐马尔可夫后验更新</div></div>
      <div class="pn"><div class="t">脱敏聚合上卷</div><div class="d">按班级 / 学校 / 区域逐级聚合，一源多视图</div></div>
      <div class="pn"><div class="t">三路输出</div><div class="d">班级学情看板 · 家长周报 · 区域治理看板</div></div>
    </div>
```

- [ ] **Step 2: 验证**（同 Task 3 Step 3，页 4 overflow=0）
- [ ] **Step 3: Commit**（`feat: 页4产品逻辑增密（数据管道）`）

---

### Task 5: 渲染 2 页小样 → 用户确认（Checkpoint）

**Files:**
- Produce: `初赛提交材料/verify/s-02.png`、`s-04.png`（临时，确认后删除）

- [ ] **Step 1: 生成小样**

Run: `node scripts/slides_to_pdf.mjs`（先重新生成 PDF，保证小样与最终一致）
Run: `pdftoppm -png -r 60 -f 2 -l 2 初赛提交材料/方案PPT.pdf 初赛提交材料/verify/s-02` 与 `-f 4 -l 4 ... s-04`
Expected: 两个 PNG 生成。

- [ ] **Step 2: 交付确认**

用 present_files 把两张小样 + 政策截图给用户；说明风格（内容密度/图片/截图）并**等待用户批准后再继续 Task 6–9**。用户若提出调整（如截图区域、图片、文案密度），先改对应 Task 再继续。

---

### Task 6: 页 3 痛点页增密（痛点→解法对位条 + 乡村教师政策截图）

**Files:**
- Modify: `初赛提交材料/方案PPT.html`（`<!-- 3 · 用户痛点 -->` section）

- [ ] **Step 1: 每卡加"乡芽解法"对位条**

4 个 `.pain-card` 的 `.impact` 后各插入：

```html
      <div class="sol"><b>乡芽解法</b>：教师端 14 项能力——一键备课/组卷/批改，一人多科变"一人多能"</div>
```
（02 学生卡 →"学生端 12 项能力——苏格拉底辅导 + ZPD 推题，错题有人一步步讲"；03 家长卡 →"家长端 5 项能力——周报 + 语音留言，千里之外看得见"；04 治理卡 →"管理端 7 项能力——看板 + 预警闭环，数据而非经验"）

- [ ] **Step 2: 痛点页引言加政策原文背书 + 政策截图**

在 `.pain-foot` 后插入：

```html
    <div style="display:grid;grid-template-columns:1.6fr 1fr;gap:5mm;margin-top:4mm;align-items:center">
      <p style="font-size:10pt;color:var(--ink-soft)">政策原文背书：<b>《关于加强新时代乡村教师队伍建设的意见》（教师〔2020〕5号）</b>指出乡村教师"结构性缺员较为突出、素质能力有待提升"——这正是乡芽要解的题：用 Agent 补齐乡镇教师最缺的备课、教研与辅导能力。</p>
      <div class="policy"><img src="assets/policy-rural.png" alt=""><div class="cap"><b>教育部等六部门《关于加强新时代乡村教师队伍建设的意见》</b>（教师〔2020〕5号）· moe.gov.cn</div></div>
    </div>
```

- [ ] **Step 3: 验证 + Commit**（overflow=0；`feat: 页3痛点增密（解法对位条/政策背书）`）

---

### Task 7: 页 5 核心流程 + 页 6 Agent 工作台增密

**Files:**
- Modify: `初赛提交材料/方案PPT.html`（`<!-- 5 · 核心流程 -->`、`<!-- 6 · Agent 工作台 -->`）

- [ ] **Step 1: 页 5 — 3 张卡加数据来源，STEP 卡加时间量级**

3 个 `.flow-below .card` 的 `.src` 改为：
- 班级学情看板 → `教师端 · 实时 · 数据：D6 作答 → 班级聚合`
- 脱敏家长周报 → `家长端 · 每周 · 数据：D6 授权子集 → 脱敏`
- 区域治理看板 → `管理端 · 实时 · 数据：18 校 6,842 学生逐级上卷`
4 个 `.flow-step` 的 `span` 首行各加时间量级：
- STEP01 → `D6 唯一原生数据 · 毫秒级`
- STEP02 → `隐马尔可夫模型 · 秒级`
- STEP03 → `维果茨基最近发展区 · 秒级`
- STEP04 → `作答回流 → 参数更新 · 下次作答`

- [ ] **Step 2: 页 6 — 顶部加"一帧真实运行轨迹"**

在 `.agent-row` 前插入：

```html
    <div class="trace">
      <div class="tt">AGENT_MESSAGES 轨迹回放 · 一次备课请求（演示实测）</div>
      <div class="ln">
        <b>[thinking]</b> 意图=备课 · 课题「简易方程·用字母表示数」→ 拆解 4 步<br>
        <b>[tool_start]</b> generate_lesson_plan {subject:数学, grade:五年级, unit:简易方程}<br>
        <b>[tool_end]</b> 大纲：导入/新授/练习/总结 · 引用 [chunk:12 五上·人教版]<br>
        <b>[text_delta]</b> 逐字流式输出 → 教师边看边滚动，可中途追问<br>
        <b>[done]</b> 引用校验通过 · 全程秒级 · 轨迹落库可回放、可导出审计
      </div>
    </div>
```

- [ ] **Step 2b: 页 5 — 加"四端协同使用路径"条（8 维度审查补强②）**

在 `.flow-below` 后插入（5 列管道，复用 `.pipe` 样式加内联 5 列）：

```html
    <div class="pipe" style="grid-template-columns:repeat(5,1fr)">
      <div class="pn"><div class="t">教师备</div><div class="d">一键备课/组卷 → 布置任务</div></div>
      <div class="pn"><div class="t">学生练</div><div class="d">作答 → 产生 D6 原生数据</div></div>
      <div class="pn"><div class="t">AI 辅</div><div class="d">BKT 诊断 + 苏格拉底引导</div></div>
      <div class="pn"><div class="t">家长看</div><div class="d">周报 + 语音留言（脱敏）</div></div>
      <div class="pn"><div class="t">治理治</div><div class="d">看板 + 预警 → 督导闭环</div></div>
    </div>
```

- [ ] **Step 3: 验证 + Commit**（两页 overflow=0；`feat: 页5/6增密（数据血缘/运行轨迹）`）

---

### Task 8: 页 20 技术 + 21 商业 + 22 合规增密

**Files:**
- Modify: `初赛提交材料/方案PPT.html`（`<!-- 20 · 技术路线 -->`、`<!-- 21 · 商业逻辑 -->`、`<!-- 22 · 合规边界 -->`）

- [ ] **Step 1: 页 20 — 6 卡加"落地可见" + 教育数字化政策截图**

6 个 `.tech-card` 的 `.key` 后各加 `<div class="milestone"><b>落地可见</b>：…</div>`：
- LangGraph.js → 演示 Agent 工作台轨迹回放
- 混合 RAG → 演示备课/问答引用标签可溯源
- BKT+ZPD → 演示学生端认知诊断（74% / 92% 置信）与 ZPD 推题
- 双模式 → 演示无 Key 离线模式
- 安全基线 → 测试：越权用例 12 项通过
- 可观测 → 测试：39 项自动化测试覆盖
在 `.tech-grid` 后插入：

```html
    <div class="policy"><img src="assets/policy-digital.png" alt=""><div class="cap"><b>国家教育数字化战略行动</b>——教育部统筹国家智慧教育平台体系（《国家智慧教育公共服务平台接入管理规范（试行）》· 教科信厅函〔2022〕33号）· moe.gov.cn</div></div>
```

- [ ] **Step 2: 页 21 — 4 阶段加里程碑指标**

4 个 `.biz-road .step` 的 `p` 后各加 `.milestone`：
- PHASE1 → `<b>里程碑</b>：3–5 所中心校 + 教学点签约试点，跑通闭环，沉淀本地化教研规则库`
- PHASE2 → `<b>里程碑</b>：双师课堂覆盖英语/音体美缺口，缺口自动入督导池`
- PHASE3 → `<b>里程碑</b>：一套开源底座跨县区复制，接入已有教育大数据平台`
- PHASE4 → `<b>里程碑</b>：MIT 开源 · 师范院校/公益基金会共建数据集与学科包`

- [ ] **Step 3: 页 22 — .rule 行升级为"已内置机制"清单（每卡 3 条）**

4 个 `.comp-card` 的 `.rule` 替换为：
- 不给标准答案 → `<div class="rule">机制：规则层拦截 · 答案前必检索 search_knowledge · 五阶段状态机</div>`
- 不替代教师评价 → `<div class="rule">机制：人工确认采纳 · 操作日志留痕 · 评价权始终在人</div>`
- 预警不贴标签 → `<div class="rule">机制：风险模型仅提示 · 人工兜底闭环 · 未成年人保护</div>`
- 脱敏+越权 → `<div class="rule">机制：JWT+RBAC · 水平越权拦截 · 未授权数据不出库</div>`

- [ ] **Step 3b: 页 22 — 加"法规依据"条（8 维度审查补强③）**

在 `.compliance` 后插入（只写法规名+概括，不写条号，避免编造）：

```html
    <div class="logic-total" style="margin-top:5mm"><span class="tag">法规依据</span>《未成年人保护法》——未成年人隐私与学习数据受特殊保护；《个人信息保护法》——不满十四周岁未成年人个人信息属敏感个人信息，须单独同意；《数据安全法》——数据分级分类与安全审查；教育数据安全政策——学生数据"最小必要、脱敏使用"。</div>
```

- [ ] **Step 4: 验证 + Commit**（三页 overflow=0；`feat: 页20/21/22增密（落地可见/里程碑/机制清单/政策截图）`）

---

### Task 9: 页 23 进展 + 24 结尾增密

**Files:**
- Modify: `初赛提交材料/方案PPT.html`（`<!-- 23 · 进展与开放价值 -->`、`<!-- 24 · 结尾 -->`）

- [ ] **Step 1: 页 23 — 加"下一步 30 天"时间条**

在 `.progress` 后插入：

```html
    <div class="timeline30">
      <div class="tl"><div class="dt">8/16 今日</div><p>初赛提交：简介 + 方案 PPT（本件）</p></div>
      <div class="tl"><div class="dt">8/25–9/3</div><p>复赛：完善 Demo 实测 + 试点校访谈</p></div>
      <div class="tl"><div class="dt">9/22–23</div><p>决赛：对接试点学校 · 现场答辩</p></div>
      <div class="tl"><div class="dt">赛后</div><p>开源社区共建 · 学科包持续扩展</p></div>
    </div>
```

- [ ] **Step 2: 页 24 — "三步同行"行动卡（替代纯 tagline）+ 温暖教室照**

`.end` 内 `.ask` 替换为：

```html
    <div class="act">
      <div class="a"><div class="t">复赛</div><p>完善 Demo 与实测数据，补充试点校反馈</p></div>
      <div class="a"><div class="t">决赛</div><p>对接试点学校，现场演示四端闭环</p></div>
      <div class="a"><div class="t">赛后</div><p>持续开源迭代，共建乡镇教育 Agent 生态</p></div>
    </div>
    <img src="assets/scene-2.jpg" alt="" style="width:58mm;border-radius:3mm;margin-top:8mm;opacity:.92">
```

- [ ] **Step 3: 验证 + Commit**（两页 overflow=0；`feat: 页23/24增密（30天时间条/三步同行/情境图）`）

---

### Task 10: 全量自检（OCR + 版面审计 + PDF + 政策真实性核对）

**Files:**
- Modify: `scripts/ocr_verify.py`（EXPECT 增补 v5 新关键词）
- Produce: `初赛提交材料/方案PPT.pdf`

- [ ] **Step 1: 更新 OCR EXPECT**

在 `scripts/ocr_verify.py` 的 `方案PPT` EXPECT 中，页 2 加 `["为什么是现在","中发〔2019〕26号"]`，页 3 加 `"教师〔2020〕5号"`，页 4 加 `"数据管道"`（或 `"三路输出"`），页 6 加 `"轨迹回放"`，页 20 加 `"落地可见"`，页 23 加 `"8/25"`，页 24 加 `"复赛"`。

- [ ] **Step 2: 重新生成 PDF**

Run: `node scripts/slides_to_pdf.mjs`
Expected: `PDF saved: .../方案PPT.pdf`，24 页。

- [ ] **Step 3: 全量 OCR 自检**

Run: `python scripts/ocr_verify.py 方案PPT 初赛提交材料/方案PPT.pdf`
Expected: 24 页全部 OK（缺失=无、豆腐块=0）。若政策截图内小字被 OCR 误读导致某页"缺失"，将该关键词改为该页其它文本（截图内文字不设 EXPECT）。

- [ ] **Step 4: 版面审计**

Run: `node scripts/audit_layout.mjs`
Expected: 内容页全部 overflow=0，仅封面 watermark 警告（有意装饰）。

- [ ] **Step 5: 政策真实性核对（人工清单）**

逐项确认 caption 与实际截图一致：① 中发〔2019〕26号 · moe.gov.cn；② 教师〔2020〕5号 · moe.gov.cn；③ 教科信厅函〔2022〕33号 / 教育数字化战略行动 · moe.gov.cn。如有不一致，改 caption 或重截。

- [ ] **Step 6: 清理 + Commit**

Run: `rm -rf 初赛提交材料/verify`
```bash
git add scripts/ocr_verify.py 初赛提交材料/
git commit -m "feat: 方案PPT v5 全量自检通过（OCR+审计+政策核对）"
```

---

### Task 11: 交付

- [ ] **Step 1:** present_files 交付 `初赛提交材料/方案PPT.pdf`（24 页 16:9），附变更清单（10 页增密 + 2 图 + 3 政策截图 + 政策真实性说明）。
- [ ] **Step 2:** 追加今日记忆（`.workbuddy/memory/2026-08-16.md`）：v5 增密完成、政策截图 URL、遗留"乡芽 vs 青禾"。

---

## Self-Review 结论

- **Spec 覆盖**：spec §3 十页清单 → Task 3/4/6/7/8/9 一一对应；§4 图片 2 张 → Task 1/3/9；§5 政策 3 条 → Task 1/3/6/8；§7 验证 → Task 5/10；§8 风险 → Task 1 Step 3 备选源。无遗漏。
- **8 维度审查补强**（用户要求对照比赛手册 8 必含项审查后并入）：页 2 数据出处（原 Task 3）、页 5 四端协同路径（Task 7 Step 2b）、页 22 法规依据（Task 8 Step 3b）。其余 5 维度（痛点/产品形态/Agent能力/工具数据模型/落地计划）审查为"强"，无需新增。
- **占位符扫描**：无 TBD/TODO；政策 URL 均为已实测可达的真实官网页（policy-digital 含备选方案而非占位）。
- **类型一致性**：class 名（`.src-note/.sol/.pipe/.trace/.policy/.why-now/.timeline30/.act/.milestone`）在 Task 2 定义、Task 3–9 使用，全局一致；资产路径 `assets/policy-*.png`、`assets/scene-*.jpg` 全链一致。
