# 乡芽 Agent 工作台改造计划 V2（LangGraph.js + 步骤流体验）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将「乡芽」从"四端表单功能页"改造为**以 Agent 工作台为核心的智能体应用**——像 opencode / Claude Code 一样：流式展示思考过程（thinking）、每次工具调用的参数与结果（tool cards）、逐步执行的全链路证据（trace）、可追溯的结果交付（refs/报告），形成覆盖任务理解→流程编排→工具调用→知识增强→多轮交互→结果交付的完整 Agent 闭环。

**Architecture:** 单一领域 Agent 核心（LangGraph.js `createReactAgent` ReAct 循环 + 自定义 StateGraph 包装：意图识别 → 工具循环 → 审查交付），所有四端业务能力注册为带 schema 的工具（ToolRegistry），由 LLM 自主编排调用；SSE 流式网关把 thinking/tool_call/tool_result/text_delta 事件推给前端；前端新增 `agent.html` 工作台（步骤流 UI：思考块折叠、工具卡片展开、流式文本、引用 refs）。知识增强升级为优秀级：embedding 向量检索 + BM25 混合（RRF 融合）+ 引用校验。多轮记忆用 LangGraph `checkpoint-sqlite` 落现有 SQLite。

**参考开源项目（V2 制定前已调研）：**
| 项目 | 借鉴点 |
|---|---|
| [openclaw/openclaw](https://github.com/openclaw/openclaw) | `src/agents`(agent loop)、`src/tools`(工具注册+上下文注入)、`src/llm`(provider 抽象)、`src/context-engine`(上下文组装)、`src/audit`(审计)——消息=工具调用链的展示模型 |
| [langchain-ai/langgraphjs](https://github.com/langchain-ai/langgraphjs) | `langgraph-core`(StateGraph/stream)、`langgraph-supervisor`(多 Agent 编排)、`checkpoint-sqlite`(记忆持久化，与项目 SQLite 同源)、`langgraph-ui`(步骤流 UI 设计) |
| opencode（本环境） | 步骤流渲染模型：每条 assistant 消息 = 思考块（可折叠）+ 工具卡片（名称/参数/状态/耗时/可展开结果）+ 流式文本 |

**Tech Stack:** NestJS 10、TypeScript 5.6、TypeORM + better-sqlite3、`@langchain/langgraph`、`@langchain/langgraph-checkpoint-sqlite`、`@langchain/openai`（DeepSeek OpenAI 兼容）、vitest。

**评审指标 → 任务映射：**
| 手册 8.2 要求 | 任务 |
|---|---|
| 任务输入 | Task 6 `POST /agent/chat`（SSE 流式统一入口） |
| 意图理解 | Task 5 IntentNode（LLM 分类 + 规则兜底） |
| 任务规划 | Task 5 ReAct 循环（LLM 自主决定步骤与工具顺序） |
| 能力调用 | Task 4 ToolRegistry（16 个工具，全端覆盖）+ Task 5 执行 |
| 结果交付 | Task 11 交付卡片（报告+引用+证据链）+ Task 6 trace 落库 |
| 验证与反馈 | Task 1 vitest 矩阵 + Task 12 演示剧本 |
| 知识增强（优秀级） | Task 2 embedding+BM25 混合 RRF+引用校验 |
| 多轮交互 | Task 5 checkpoint-sqlite 记忆 + Task 10 苏格拉底状态机 |

---

## 阶段总览

| 阶段 | 时间 | 内容 | 任务 |
|---|---|---|---|
| P0 | 8.15–8.16 | 初赛材料（PPT 技术路线含 V2 架构图）+ 清理调试垃圾 | Task 0 |
| P1 | 8.17–8.24 | 知识底座 RAG + 工具注册表 + Agent 循环 + SSE 网关（可流式演示） | Task 1–6 |
| P2 | 8.25–8.31 | Agent 工作台前端（步骤流 UI）+ BKT/ZPD/苏格拉底接入 + 交付追溯 | Task 7–11 |
| P3 | 9.1–9.3 | 演示剧本 + 全链路自检 + 复赛提交物 | Task 12 |

---

## Task 0: 初赛材料与调试垃圾清理

**Files:**
- Delete: `server/d1.txt`、`server/d2.txt`、`server/d3.json`、`server/d4.json`、`server/body.json`、`server/body2.json`、`server/-b`
- Create: `docs/初赛方案-技术路线.md`

- [ ] **Step 1: 清理调试残留**

```powershell
Remove-Item -LiteralPath "F:\代码文件\vibe coder\无界应用-教育智能体\server\d1.txt","F:\代码文件\vibe coder\无界应用-教育智能体\server\d2.txt","F:\代码文件\vibe coder\无界应用-教育智能体\server\d3.json","F:\代码文件\vibe coder\无界应用-教育智能体\server\d4.json","F:\代码文件\vibe coder\无界应用-教育智能体\server\body.json","F:\代码文件\vibe coder\无界应用-教育智能体\server\body2.json","F:\代码文件\vibe coder\无界应用-教育智能体\server\-b"
```

- [ ] **Step 2: 撰写技术路线文档**

`docs/初赛方案-技术路线.md`：项目定位、Agent 工作台核心体验（附步骤流 UI 示意图）、LangGraph.js 编排架构、优秀级 RAG（embedding+BM25+引用校验）、BKT/ZPD 算法、数据闭环、合规边界。供 PPT 直接引用。

- [ ] **Step 3: 提交初赛材料（8.16 截止）**

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: 清理调试残留，补充初赛技术路线文档"
```

---

## Task 1: 测试基础设施（vitest）

**Files:**
- Modify: `server/package.json`
- Create: `server/vitest.config.ts`
- Create: `server/src/modules/agent/__tests__/sanity.test.ts`

- [ ] **Step 1: 安装依赖**

```bash
cd "F:\代码文件\vibe coder\无界应用-教育智能体\server"
npm i -D vitest
npm i @langchain/langgraph @langchain/openai @langchain/core @langchain/langgraph-checkpoint-sqlite
```

- [ ] **Step 2: vitest 配置**

`server/vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
    testTimeout: 30000,
  },
});
```

- [ ] **Step 3: package.json scripts**

```json
{ "scripts": { "test": "vitest run", "test:watch": "vitest" } }
```

- [ ] **Step 4: 冒烟测试**

`server/src/modules/agent/__tests__/sanity.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
describe('sanity', () => {
  it('vitest 可运行', () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 5: 运行测试**

Run: `npm test`（`server/` 目录）→ Expected: `1 passed`

- [ ] **Step 6: Commit**

```bash
git add server/package.json server/package-lock.json server/vitest.config.ts server/src/modules/agent/__tests__/sanity.test.ts
git commit -m "test: 引入 vitest 测试基础设施"
```

---

## Task 2: 优秀级知识增强（embedding 向量 + BM25 混合 RRF + 引用校验）

**Files:**
- Create: `server/src/db/entities/knowledge.entities.ts`（追加 `knowledge_chunks` 表）
- Create: `server/src/modules/knowledge/embedding.ts`（EmbeddingProvider 抽象：OpenAI 兼容 / 确定性哈希降级）
- Create: `server/src/modules/knowledge/hybrid-retriever.ts`（BM25+向量 RRF 融合 + 引用校验）
- Create: `server/src/modules/knowledge/__tests__/hybrid-retriever.test.ts`
- Create: `server/src/modules/knowledge/knowledge.module.ts`

**设计（优秀级标准）：** 教材分块入库 → 每块生成 embedding 向量（真模型或确定性哈希降级）→ 查询时 BM25（FTS5）与余弦相似度双路召回 → RRF 融合排序 → 图谱命中加权 → 输出带 ref 的片段。引用校验函数 `verifyRefs` 供交付层使用：LLM 声称的引用必须命中真实检索集合，否则视为幻觉。

- [ ] **Step 1: 追加 knowledge_chunks 实体**

在 `server/src/db/entities/knowledge.entities.ts` 末尾追加：

```ts
/** 教材知识块（RAG 检索单元：分块 + 向量） */
@Entity('knowledge_chunks')
@Index('idx_kc_textbook', ['textbookId'])
@Index('idx_kc_kp', ['knowledgePointId'])
export class KnowledgeChunk extends BaseEntity {
  @Column({ name: 'textbook_id', type: 'int' })
  textbookId: number;

  @Column({ name: 'knowledge_point_id', type: 'int', nullable: true })
  knowledgePointId: number | null;

  @Column({ type: 'varchar', length: 128 })
  title: string;

  @Column({ type: 'text' })
  content: string;

  /** 章节定位（溯源用） */
  @Column({ type: 'varchar', length: 64, nullable: true })
  chapter: string | null;

  /** 向量：JSON 数字数组（降级模式为 0/1 特征向量） */
  @Column({ type: 'text', nullable: true })
  embedding: string | null;

  /** 来源标注（与 textbook 一致） */
  @Column({ type: 'varchar', length: 64, nullable: true })
  source: string | null;
}
```

- [ ] **Step 2: EmbeddingProvider 抽象**

`server/src/modules/knowledge/embedding.ts`:

```ts
export interface EmbeddingProvider {
  readonly name: string;
  /** 文本 → 向量（长度由模型决定，降级模式固定 64 维） */
  embed(text: string): Promise<number[]>;
}

/** OpenAI 兼容 embedding（通义 text-embedding-v3 / OpenAI 等），需 EMBEDDING_API_KEY */
export class OpenAiEmbedding implements EmbeddingProvider {
  readonly name = 'openai-compatible';
  constructor(
    private readonly baseUrl: string,
    private readonly apiKey: string,
    private readonly model: string,
  ) {}

  async embed(text: string): Promise<number[]> {
    const res = await fetch(`${this.baseUrl.replace(/\/$/, '')}/embeddings`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${this.apiKey}` },
      body: JSON.stringify({ model: this.model, input: text.slice(0, 8000) }),
    });
    if (!res.ok) throw new Error(`embedding API ${res.status}`);
    const json = (await res.json()) as { data?: Array<{ embedding: number[] }> };
    const vec = json.data?.[0]?.embedding;
    if (!vec) throw new Error('embedding 结果为空');
    return vec;
  }
}

/** 确定性降级：字符哈希特征向量（64 维 0/1），无 Key 可跑、可复现 */
export class HashEmbedding implements EmbeddingProvider {
  readonly name = 'hash-fallback';
  readonly dim = 64;

  embed(text: string): Promise<number[]> {
    const vec = new Array(this.dim).fill(0);
    let seed = 0;
    for (const ch of text) {
      seed = (seed * 31 + ch.codePointAt(0)!) >>> 0;
      vec[seed % this.dim] = 1;
      // 二元组特征增强
      if (ch.codePointAt(0)! > 0x4e00) vec[(seed >>> 3) % this.dim] = 1;
    }
    return Promise.resolve(vec);
  }
}

export function cosine(a: number[], b: number[]): number {
  let dot = 0, na = 0, nb = 0;
  for (let i = 0; i < a.length; i++) {
    dot += a[i] * b[i];
    na += a[i] * a[i];
    nb += b[i] * b[i];
  }
  return na && nb ? dot / (Math.sqrt(na) * Math.sqrt(nb)) : 0;
}

export function l2Normalize(v: number[]): number[] {
  const n = Math.sqrt(v.reduce((s, x) => s + x * x, 0)) || 1;
  return v.map((x) => x / n);
}
```

- [ ] **Step 3: 混合检索器（BM25 + 向量 RRF 融合）**

`server/src/modules/knowledge/hybrid-retriever.ts`:

```ts
import { Injectable, Logger } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { KnowledgeChunk, KnowledgePoint } from '../../db/entities/knowledge.entities';
import { cosine, l2Normalize, type EmbeddingProvider } from './embedding';

export interface RetrievedChunk {
  id: number;
  title: string;
  chapter: string | null;
  content: string;
  source: string | null;
  score: number;
  ref: string;
}

@Injectable()
export class HybridRetriever {
  private readonly logger = new Logger(HybridRetriever.name);

  constructor(
    @InjectRepository(KnowledgeChunk) private readonly chunks: Repository<KnowledgeChunk>,
    @InjectRepository(KnowledgePoint) private readonly kps: Repository<KnowledgePoint>,
    private readonly embedding: EmbeddingProvider,
  ) {}

  /** 倒排：查询词 → chunk 命中数（BM25 近似：词频 + IDF 常量简化） */
  private async bm25Candidates(words: string[], limit: number): Promise<Array<{ id: number; score: number }>> {
    try {
      const q = words.map((w) => `"${w}"`).join(' OR ');
      const rows = await this.chunks.manager.query(
        `SELECT rowid AS id, bm25(knowledge_chunks_fts) AS bm
         FROM knowledge_chunks_fts WHERE knowledge_chunks_fts MATCH ? ORDER BY bm LIMIT ?`,
        [q, limit],
      );
      return (rows as Array<{ id: number; bm: number }>).map((r) => ({ id: r.id, score: -r.bm }));
    } catch {
      this.logger.warn('FTS5 不可用，BM25 通道跳过');
      return [];
    }
  }

  /** 向量通道：全量余弦（数据量 < 10k 时暴力检索可接受且可复现） */
  private async vectorCandidates(queryVec: number[], limit: number): Promise<Array<{ id: number; score: number }>> {
    const all = await this.chunks.find({ select: ['id', 'embedding'] });
    const qn = l2Normalize(queryVec);
    const scored: Array<{ id: number; score: number }> = [];
    for (const c of all) {
      if (!c.embedding) continue;
      try {
        const v = l2Normalize(JSON.parse(c.embedding) as number[]);
        scored.push({ id: c.id, score: cosine(qn, v) });
      } catch { /* 跳过坏向量 */ }
    }
    scored.sort((a, b) => b.score - a.score);
    return scored.slice(0, limit);
  }

  /**
   * 混合检索：BM25 + 向量 双路召回 → RRF 融合 → 图谱命中加权 → 截断。
   * RRF: score = Σ 1/(k + rank)，k=60。
   */
  async retrieve(query: string, topK = 5): Promise<RetrievedChunk[]> {
    const words = query.replace(/[^\u4e00-\u9fa5a-zA-Z0-9]/g, ' ').split(/\s+/).filter((w) => w.length >= 2).slice(0, 6);
    if (!words.length) return [];

    const queryVec = await this.embedding.embed(query);
    const [bm25Hits, vecHits] = await Promise.all([
      this.bm25Candidates(words, 10),
      this.vectorCandidates(queryVec, 10),
    ]);

    // RRF 融合
    const rrf = new Map<number, number>();
    for (const [rank, h] of bm25Hits.entries()) rrf.set(h.id, (rrf.get(h.id) ?? 0) + 1 / (60 + rank + 1));
    for (const [rank, h] of vecHits.entries()) rrf.set(h.id, (rrf.get(h.id) ?? 0) + 1 / (60 + rank + 1));

    // 图谱命中加权（查询词命中知识点名 → +0.15）
    const kps = await this.kps.find();
    const hitKpIds = new Set<number>();
    for (const kp of kps) {
      if (words.some((w) => (kp.name || '').includes(w) || (kp.description || '').includes(w))) hitKpIds.add(kp.id);
    }
    let boostFor = new Set<number>();
    if (hitKpIds.size) {
      const boosted = await this.chunks.find({ where: { knowledgePointId: [...hitKpIds].slice(0, 10) } });
      boostFor = new Set(boosted.map((c) => c.id));
    }

    const ids = [...rrf.keys()];
    if (!ids.length) return [];
    const chunks = await this.chunks.find({ where: ids.map((id) => ({ id })) });
    const ranked = chunks
      .map((c) => ({
        id: c.id, title: c.title, chapter: c.chapter, content: c.content.slice(0, 500),
        source: c.source, score: (rrf.get(c.id) ?? 0) + (boostFor.has(c.id) ? 0.15 : 0), ref: `chunk:${c.id}`,
      }))
      .sort((a, b) => b.score - a.score)
      .slice(0, topK);
    return ranked;
  }

  /** 引用校验：给定 refs，返回真实存在于检索源的子集（防幻觉） */
  async verifyRefs(refs: string[]): Promise<Array<{ ref: string; valid: boolean }>> {
    const ids = refs
      .filter((r) => r.startsWith('chunk:'))
      .map((r) => Number(r.slice(6)))
      .filter((n) => Number.isInteger(n));
    if (!ids.length) return refs.map((r) => ({ ref: r, valid: false }));
    const found = await this.chunks.find({ where: ids.map((id) => ({ id })) });
    const foundIds = new Set(found.map((c) => c.id));
    return refs.map((r) => ({ ref: r, valid: r.startsWith('chunk:') ? foundIds.has(Number(r.slice(6))) : true }));
  }
}
```

- [ ] **Step 4: KnowledgeModule**

`server/src/modules/knowledge/knowledge.module.ts`:

```ts
import { Global, Module } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { TypeOrmModule } from '@nestjs/typeorm';
import { KnowledgeChunk, KnowledgePoint, Question, TextbookContent, Template, KnowledgeBaseEntry } from '../../db/entities/knowledge.entities';
import { HybridRetriever } from './hybrid-retriever';
import { OpenAiEmbedding, HashEmbedding, type EmbeddingProvider } from './embedding';

@Global()
@Module({
  imports: [TypeOrmModule.forFeature([KnowledgeChunk, KnowledgePoint, Question, TextbookContent, Template, KnowledgeBaseEntry])],
  providers: [
    HybridRetriever,
    {
      provide: 'EMBEDDING_PROVIDER',
      inject: [ConfigService],
      useFactory: (config: ConfigService): EmbeddingProvider => {
        const mode = config.get<string>('EMBEDDING_PROVIDER', 'hash');
        if (mode === 'openai-compatible' && config.get<string>('EMBEDDING_API_KEY')) {
          return new OpenAiEmbedding(
            config.get<string>('EMBEDDING_BASE_URL') || 'https://api.dashscope.aliyuncs.com/compatible-mode/v1',
            config.get<string>('EMBEDDING_API_KEY')!,
            config.get<string>('EMBEDDING_MODEL') || 'text-embedding-v3',
          );
        }
        return new HashEmbedding();
      },
    },
  ],
  exports: [HybridRetriever, 'EMBEDDING_PROVIDER'],
})
export class KnowledgeModule {}
```

- [ ] **Step 5: 测试（mock Repository）**

`server/src/modules/knowledge/__tests__/hybrid-retriever.test.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import { HybridRetriever } from '../hybrid-retriever';
import { HashEmbedding, cosine, l2Normalize } from '../embedding';

describe('embedding utils', () => {
  it('cosine：相同向量=1，正交=0', () => {
    expect(cosine([1, 0], [1, 0])).toBeCloseTo(1);
    expect(cosine([1, 0], [0, 1])).toBeCloseTo(0);
  });
  it('hash embedding 确定性', () => {
    const e = new HashEmbedding();
    const a = await e.embed('草船借箭');
    const b = await e.embed('草船借箭');
    expect(a).toEqual(b);
    expect(a).toHaveLength(64);
  });
});

function makeChunksRepo(chunks: Array<{ id: number; embedding: string }>) {
  return {
    manager: { query: vi.fn(async () => chunks.slice(0, 3).map((c) => ({ id: c.id, bm: -5 - c.id }))) },
    find: vi.fn(async (opts?: any) => {
      if (opts?.select) return chunks.map((c) => ({ id: c.id, embedding: c.embedding }));
      const ids = opts?.where;
      if (Array.isArray(ids)) return chunks.filter((c) => ids.some((w) => w.id === c.id));
      if (ids?.knowledgePointId) return chunks.filter((c) => [10].includes(c.id));
      return chunks;
    }),
  } as unknown as any;
}

describe('HybridRetriever', () => {
  it('RRF 融合返回带 ref 片段', async () => {
    const chunks = [
      { id: 1, embedding: JSON.stringify(new HashEmbedding().embed('周瑜妒忌诸葛亮')) },
      { id: 2, embedding: JSON.stringify(new HashEmbedding().embed('祖父的园子自由')) },
    ];
    const kps = { find: vi.fn(async () => [{ id: 10, name: '概括主要内容', description: '' }]) };
    const r = new HybridRetriever(makeChunksRepo(chunks), kps as any, new HashEmbedding());
    const out = await r.retrieve('草船借箭主要内容', 3);
    expect(out.length).toBeGreaterThanOrEqual(1);
    expect(out[0].ref).toMatch(/^chunk:\d+$/);
  });

  it('空查询返回空数组', async () => {
    const r = new HybridRetriever(makeChunksRepo([]), { find: vi.fn(async () => []) } as any, new HashEmbedding());
    expect(await r.retrieve('  ')).toEqual([]);
  });

  it('verifyRefs 只放行真实 chunk', async () => {
    const chunks = [{ id: 1, embedding: '[]' }];
    const kps = { find: vi.fn(async () => []) };
    const r = new HybridRetriever(makeChunksRepo(chunks), kps as any, new HashEmbedding());
    const out = await r.verifyRefs(['chunk:1', 'chunk:999', 'rule:socratic']);
    expect(out).toEqual([
      { ref: 'chunk:1', valid: true },
      { ref: 'chunk:999', valid: false },
      { ref: 'rule:socratic', valid: true },
    ]);
  });
});
```

- [ ] **Step 6: 运行测试**

Run: `npm test -- --reporter=verbose`
Expected: 新增 6 个用例通过

- [ ] **Step 7: Commit**

```bash
git add server/src/db/entities/knowledge.entities.ts server/src/modules/knowledge/
git commit -m "feat: 优秀级知识增强——embedding 向量+BM25 混合 RRF+引用校验"
```

---

## Task 3: Agent 会话与消息实体（多轮交互的数据基础）

**Files:**
- Create: `server/src/db/entities/agent.entities.ts`（agent_runs 保留 + agent_messages 新增）

- [ ] **Step 1: 实体**

`server/src/db/entities/agent.entities.ts`:

```ts
import { Column, Entity, Index } from 'typeorm';
import { BaseEntity } from './base.entity';

/** Agent 任务运行（一次完整闭环） */
@Entity('agent_runs')
@Index('idx_ar_user_time', ['userId', 'createdAt'])
export class AgentRun extends BaseEntity {
  @Column({ name: 'user_id', type: 'int' }) userId: number;
  @Column({ type: 'varchar', length: 16 }) role: string;
  @Column({ name: 'task_input', type: 'text' }) taskInput: string;
  @Column({ type: 'varchar', length: 32 }) intent: string;
  @Column({ type: 'varchar', length: 16, default: 'running' }) status: string;
  @Column({ type: 'text', nullable: true }) summary: string | null;
  @Column({ type: 'int', default: 0 }) durationMs: number;
  @Column({ type: 'int', default: 0 }) toolCalls: number;
}

/** Agent 消息（含思考/工具调用轨迹，步骤流 UI 的数据源） */
@Entity('agent_messages')
@Index('idx_am_run', ['runId'])
export class AgentMessage extends BaseEntity {
  @Column({ name: 'run_id', type: 'int' }) runId: number;
  /** user | assistant | tool */
  @Column({ type: 'varchar', length: 16 }) role: string;
  /** 消息类型：text | thinking | tool_call | tool_result | refs | final */
  @Column({ type: 'varchar', length: 24 }) kind: string;
  @Column({ type: 'text', nullable: true }) content: string | null;
  /** 工具名（kind=tool_call/tool_result 时） */
  @Column({ type: 'varchar', length: 64, nullable: true }) tool: string | null;
  /** 工具参数 JSON（tool_call） */
  @Column({ type: 'text', nullable: true }) argsJson: string | null;
  /** 工具结果 JSON 摘要（tool_result，截断 2000） */
  @Column({ type: 'text', nullable: true }) resultJson: string | null;
  /** 状态：pending/running/done/error（tool） */
  @Column({ type: 'varchar', length: 16, default: 'done' }) status: string;
  /** 耗时 ms（tool） */
  @Column({ type: 'int', default: 0 }) durationMs: number;
  /** 引用 refs JSON（final 消息） */
  @Column({ type: 'text', nullable: true }) refsJson: string | null;
}
```

- [ ] **Step 2: 编译验证**

Run: `npx tsc --noEmit`（`server/` 目录）→ 无错误

- [ ] **Step 3: Commit**

```bash
git add server/src/db/entities/agent.entities.ts
git commit -m "feat: agent_runs/agent_messages 实体（轨迹持久化）"
```

---

## Task 4: ToolRegistry 全量工具（能力调用基础）

**Files:**
- Create: `server/src/modules/agent/tool-registry.ts`
- Create: `server/src/modules/agent/__tests__/tool-registry.test.ts`

**设计（参照 OpenClaw `src/tools`）：** 工具 = `{name, description, inputSchema, needsPermission, execute}`。16 个工具覆盖四端核心能力；LLM 通过 function-calling 选择并调用；每次调用由执行器包装：记录参数→执行→记录结果→计时→审计。

- [ ] **Step 1: 工具类型与注册表**

`server/src/modules/agent/tool-registry.ts`:

```ts
export interface ToolContext {
  userId: number;
  role: string;
  /** 绑定的学生 ID（家长=孩子，学生=本人） */
  studentId?: number;
  classId?: number;
}

export interface ToolDefinition<TArgs = Record<string, unknown>, TResult = unknown> {
  name: string;
  description: string;
  inputSchema: Record<string, unknown>;
  needsPermission?: 'student' | 'teacher' | 'parent' | 'admin';
  execute: (ctx: ToolContext, args: TArgs) => Promise<TResult>;
}

export interface ToolCallRecord {
  tool: string;
  args: unknown;
  result?: unknown;
  error?: string;
  durationMs: number;
}

export class ToolRegistry {
  private tools = new Map<string, ToolDefinition>();

  register<TArgs, TResult>(t: ToolDefinition<TArgs, TResult>): this {
    this.tools.set(t.name, t as ToolDefinition);
    return this;
  }

  get(name: string): ToolDefinition | undefined {
    return this.tools.get(name);
  }

  list(): ToolDefinition[] {
    return [...this.tools.values()];
  }

  toFunctionSchemas() {
    return this.list().map((t) => ({
      type: 'function' as const,
      function: { name: t.name, description: t.description, parameters: t.inputSchema },
    }));
  }

  /** 执行并记录轨迹（供 Agent 循环调用） */
  async call(ctx: ToolContext, name: string, args: unknown): Promise<ToolCallRecord> {
    const tool = this.tools.get(name);
    const started = Date.now();
    if (!tool) {
      return { tool: name, args, error: `未知工具: ${name}`, durationMs: 0 };
    }
    try {
      const result = await Promise.race([
        tool.execute(ctx, (args ?? {}) as never),
        new Promise((_, rej) => setTimeout(() => rej(new Error('工具超时（30s）')), 30_000)),
      ]);
      return { tool: name, args, result, durationMs: Date.now() - started };
    } catch (e) {
      return { tool: name, args, error: (e as Error).message?.slice(0, 500) ?? '未知错误', durationMs: Date.now() - started };
    }
  }
}
```

- [ ] **Step 2: 16 个工具的声明（execute 由 AgentService 注入真实业务，此处注册骨架与 schema）**

在 `tool-registry.ts` 末尾追加（与 Task 5 的 bindings 对接）：

```ts
export const TOOL_NAMES = [
  // 学生端
  'run_diagnosis', 'get_latest_diagnosis', 'get_error_book', 'get_study_plan',
  'practice_questions', 'submit_answer', 'search_knowledge', 'socratic_tutor',
  // 教师端
  'generate_lesson_plan', 'generate_paper', 'get_class_overview', 'researcher_comment',
  // 家长端
  'get_weekly_report',
  // 管理端
  'get_region_overview', 'list_alerts', 'get_teacher_profile',
] as const;
export type ToolName = (typeof TOOL_NAMES)[number];

export interface ToolBindings {
  run_diagnosis: (studentId: number) => Promise<unknown>;
  get_latest_diagnosis: (studentId: number) => Promise<unknown>;
  get_error_book: (studentId: number) => Promise<unknown>;
  get_study_plan: (studentId: number) => Promise<unknown>;
  practice_questions: (kpId: number | null, count: number) => Promise<unknown>;
  submit_answer: (questionId: number, answer: string) => Promise<unknown>;
  search_knowledge: (query: string, topK?: number) => Promise<unknown>;
  socratic_tutor: (problem: string, studentReply: string) => Promise<unknown>;
  generate_lesson_plan: (args: { subject: string; grade: string; topic: string; periodCount?: number; duration?: number }) => Promise<unknown>;
  generate_paper: (args: { subject: string; grade: string; title: string; layerMode?: string; questionCount?: number }) => Promise<unknown>;
  get_class_overview: (classId: number) => Promise<unknown>;
  researcher_comment: (sourceContent: string) => Promise<unknown>;
  get_weekly_report: (studentId: number) => Promise<unknown>;
  get_region_overview: () => Promise<unknown>;
  list_alerts: (type?: string, limit?: number) => Promise<unknown>;
  get_teacher_profile: (teacherId: number) => Promise<unknown>;
}

export function buildRegistry(b: ToolBindings): ToolRegistry {
  const r = new ToolRegistry();
  const reg = <TArgs, TResult>(name: string, description: string, inputSchema: Record<string, unknown>, exec: (ctx: ToolContext, args: TArgs) => Promise<TResult>) =>
    r.register({ name, description, inputSchema, execute: exec });

  reg('run_diagnosis', '对指定学生执行 BKT 认知诊断，返回整体与各知识点掌握度、置信度、证据题数。', {
    type: 'object', properties: { studentId: { type: 'integer', description: '学生 ID' } }, required: ['studentId'],
  }, (_c, a) => b.run_diagnosis(a.studentId));
  reg('get_latest_diagnosis', '获取学生最近一次诊断摘要。', {
    type: 'object', properties: { studentId: { type: 'integer' } }, required: ['studentId'],
  }, (_c, a) => b.get_latest_diagnosis(a.studentId));
  reg('get_error_book', '获取学生错题本（含错因标签），用于定位反复出错知识点。', {
    type: 'object', properties: { studentId: { type: 'integer' } }, required: ['studentId'],
  }, (_c, a) => b.get_error_book(a.studentId));
  reg('get_study_plan', '获取学生当前学习计划与步骤进度（ZPD）。', {
    type: 'object', properties: { studentId: { type: 'integer' } }, required: ['studentId'],
  }, (_c, a) => b.get_study_plan(a.studentId));
  reg('practice_questions', '按知识点取练习题目（不泄露答案），用于生成个性化练习。', {
    type: 'object', properties: { kpId: { type: 'integer' }, count: { type: 'integer', minimum: 1, maximum: 10 } }, required: [],
  }, (_c, a) => b.practice_questions(a.kpId ?? null, a.count ?? 3));
  reg('submit_answer', '提交一道题目的作答（客观题自动判分并回流诊断）。', {
    type: 'object', properties: { questionId: { type: 'integer' }, answer: { type: 'string' } }, required: ['questionId', 'answer'],
  }, (_c, a) => b.submit_answer(a.questionId, a.answer));
  reg('search_knowledge', '检索教材知识库（embedding+BM25 混合，带出处引用）。回答知识问题必须调用本工具并标注引用。', {
    type: 'object', properties: { query: { type: 'string' }, topK: { type: 'integer', minimum: 1, maximum: 10 } }, required: ['query'],
  }, (_c, a) => b.search_knowledge(a.query, a.topK ?? 5));
  reg('socratic_tutor', '苏格拉底辅导一轮：输入问题与学生回答，输出引导语与阶段推进（绝不给答案）。', {
    type: 'object', properties: { problem: { type: 'string' }, studentReply: { type: 'string' } }, required: ['problem', 'studentReply'],
  }, (_c, a) => b.socratic_tutor(a.problem, a.studentReply));
  reg('generate_lesson_plan', '教师一键备课：生成结构化教案（目标/重难点/五段教学过程/分层作业/反思），带教材与模板引用。', {
    type: 'object', properties: { subject: { type: 'string' }, grade: { type: 'string' }, topic: { type: 'string' }, periodCount: { type: 'integer' }, duration: { type: 'integer' } }, required: ['subject', 'grade', 'topic'],
  }, (_c, a) => b.generate_lesson_plan(a));
  reg('generate_paper', '教师一键组卷：A/B/C 分层试卷，返回结构与题目列表。', {
    type: 'object', properties: { subject: { type: 'string' }, grade: { type: 'string' }, title: { type: 'string' }, layerMode: { type: 'string', enum: ['uniform', 'layered'] }, questionCount: { type: 'integer' } }, required: ['subject', 'grade', 'title'],
  }, (_c, a) => b.generate_paper(a));
  reg('get_class_overview', '班级学情概览（人数/作答数/平均掌握度/风险学生）。', {
    type: 'object', properties: { classId: { type: 'integer' } }, required: ['classId'],
  }, (_c, a) => b.get_class_overview(a.classId));
  reg('researcher_comment', 'AI 教研员：对教案内容点评，输出评分与改进建议。', {
    type: 'object', properties: { sourceContent: { type: 'string' } }, required: ['sourceContent'],
  }, (_c, a) => b.researcher_comment(a.sourceContent));
  reg('get_weekly_report', '获取学生脱敏学情周报（家长端视图）。', {
    type: 'object', properties: { studentId: { type: 'integer' } }, required: ['studentId'],
  }, (_c, a) => b.get_weekly_report(a.studentId));
  reg('get_region_overview', '区域学情总览（学校数/活跃学生/平均掌握度/预警数）。', {
    type: 'object', properties: {}, required: [],
  }, () => b.get_region_overview());
  reg('list_alerts', '列出预警（控辍/心理/师资），含风险分与状态。', {
    type: 'object', properties: { type: { type: 'string' }, limit: { type: 'integer' } }, required: [],
  }, (_c, a) => b.list_alerts(a.type, a.limit ?? 10));
  reg('get_teacher_profile', '教师数据画像（教学质量/教研/AI 融合等维度）。', {
    type: 'object', properties: { teacherId: { type: 'integer' } }, required: ['teacherId'],
  }, (_c, a) => b.get_teacher_profile(a.teacherId));

  return r;
}
```

- [ ] **Step 3: 测试**

`server/src/modules/agent/__tests__/tool-registry.test.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import { buildRegistry, type ToolBindings, type ToolCallRecord } from '../tool-registry';

const bindings: ToolBindings = {
  run_diagnosis: vi.fn(async () => ({ overallMastery: 60 })),
  get_latest_diagnosis: vi.fn(async () => null),
  get_error_book: vi.fn(async () => []),
  get_study_plan: vi.fn(async () => []),
  practice_questions: vi.fn(async () => []),
  submit_answer: vi.fn(async () => ({ isCorrect: true })),
  search_knowledge: vi.fn(async () => []),
  socratic_tutor: vi.fn(async () => ({ stage: 'identify' })),
  generate_lesson_plan: vi.fn(async () => ({ id: 1 })),
  generate_paper: vi.fn(async () => ({ id: 1 })),
  get_class_overview: vi.fn(async () => ({ total: 42 })),
  researcher_comment: vi.fn(async () => ({ score: 88 })),
  get_weekly_report: vi.fn(async () => ({})),
  get_region_overview: vi.fn(async () => ({})),
  list_alerts: vi.fn(async () => []),
  get_teacher_profile: vi.fn(async () => ({})),
};

describe('ToolRegistry', () => {
  it('注册 16 个工具', () => {
    const r = buildRegistry(bindings);
    expect(r.list()).toHaveLength(16);
    expect(r.get('run_diagnosis')).toBeDefined();
    expect(r.get('search_knowledge')).toBeDefined();
  });

  it('call 转发参数并返回轨迹记录', async () => {
    const r = buildRegistry(bindings);
    const rec: ToolCallRecord = await r.call({ userId: 1, role: 'teacher' }, 'run_diagnosis', { studentId: 5 });
    expect(rec.result).toEqual({ overallMastery: 60 });
    expect(rec.durationMs).toBeGreaterThanOrEqual(0);
  });

  it('未知工具与异常工具返回 error', async () => {
    const r = buildRegistry(bindings);
    expect((await r.call({ userId: 1, role: 'student' }, 'no_such', {})).error).toContain('未知工具');
    const bad = new ToolRegistry();
    bad.register({ name: 'boom', description: 'd', inputSchema: { type: 'object' }, execute: async () => { throw new Error('挂了'); } });
    const rec = await bad.call({ userId: 1, role: 'student' }, 'boom', {});
    expect(rec.error).toContain('挂了');
  });

  it('toFunctionSchemas 输出 OpenAI 兼容结构', () => {
    const r = buildRegistry(bindings);
    const schemas = r.toFunctionSchemas();
    expect(schemas[0]).toMatchObject({ type: 'function', function: { name: 'run_diagnosis' } });
  });
});
```

- [ ] **Step 4: 运行测试**

Run: `npm test -- --reporter=verbose`
Expected: 新增 4 个用例通过

- [ ] **Step 5: Commit**

```bash
git add server/src/modules/agent/tool-registry.ts server/src/modules/agent/__tests__/tool-registry.test.ts
git commit -m "feat: ToolRegistry 16 个教育工具（schema+轨迹记录+权限位）"
```

---

## Task 5: Agent 循环核心（LangGraph ReAct + 意图识别 + checkpoint 记忆）

**Files:**
- Create: `server/src/modules/agent/agent-graph.ts`
- Create: `server/src/modules/agent/__tests__/agent-graph.test.ts`

**设计（参照 LangGraph `createReactAgent` + `checkpoint-sqlite` + OpenClaw agent loop）：** 外层 StateGraph：`intent`（意图识别，规则兜底）→ `agent`（ReAct 循环：LLM 决定工具→执行→回填，最大 12 轮）→ `finalize`（引用校验+交付摘要）。checkpoint 用 `SqliteSaver` 落项目 DB 文件，实现跨轮记忆。

- [ ] **Step 1: 安装检查**

确认 `@langchain/langgraph-checkpoint-sqlite` 已安装（Task 1 已装）。

- [ ] **Step 2: 编写 Agent 图**

`server/src/modules/agent/agent-graph.ts`:

```ts
import { Annotation, END, START, StateGraph } from '@langchain/langgraph';
import { ChatOpenAI } from '@langchain/openai';
import type { ToolRegistry, ToolContext } from './tool-registry';
import type { BaseCheckpointSaver } from '@langchain/langgraph-checkpoint';

export type AgentIntent = 'diagnose' | 'teach' | 'generate' | 'knowledge' | 'admin' | 'general';

/** 规则兜底意图识别（无 Key 或 LLM 失败时使用） */
export function detectIntent(text: string): AgentIntent {
  if (/诊断|掌握|薄弱|退步/.test(text)) return 'diagnose';
  if (/备课|教案|组卷|试卷|出题|教研/.test(text)) return 'generate';
  if (/什么|为什么|意思|含义|讲解/.test(text)) return 'knowledge';
  if (/预警|看板|区域|师资/.test(text)) return 'admin';
  if (/练习|辅导|错题|计划/.test(text)) return 'teach';
  return 'general';
}

export interface AgentGraphState {
  input: string;
  userId: number;
  role: string;
  intent: AgentIntent;
  toolCalls: Array<{ tool: string; args: unknown; result?: unknown; error?: string; durationMs: number }>;
  finalText: string;
  refs: string[];
  needsHuman: boolean;
  /** 事件回调（SSE 推送） */
  onEvent?: (ev: AgentEvent) => void | Promise<void>;
}

export type AgentEvent =
  | { type: 'thinking'; text: string }
  | { type: 'tool_start'; name: string; args: unknown }
  | { type: 'tool_end'; name: string; result?: unknown; error?: string; durationMs: number }
  | { type: 'text_delta'; delta: string }
  | { type: 'done'; finalText: string; refs: string[]; intent: AgentIntent };

const State = Annotation.Root({
  input: Annotation<string>,
  userId: Annotation<number>,
  role: Annotation<string>,
  intent: Annotation<AgentIntent>,
  toolCalls: Annotation<Array<{ tool: string; args: unknown; result?: unknown; error?: string; durationMs: number }>>({
    reducer: (a, b) => [...a, ...b],
  }),
  finalText: Annotation<string>,
  refs: Annotation<string[]>({ reducer: (a, b) => [...a, ...b] }),
  needsHuman: Annotation<boolean>,
  onEvent: Annotation<((ev: AgentEvent) => void | Promise<void>) | undefined>,
});

export interface BuildAgentGraphOptions {
  llm: ChatOpenAI | null;       // null = Demo 规则模式
  registry: ToolRegistry;
  maxTurns?: number;
  checkpointer?: BaseCheckpointSaver;
}

export function buildAgentGraph({ llm, registry, maxTurns = 12, checkpointer }: BuildAgentGraphOptions) {
  const turnLimit = Math.min(Math.max(maxTurns, 3), 20);

  const intentNode = async (s: AgentGraphState): Promise<Partial<AgentGraphState>> => {
    const intent = detectIntent(s.input);
    return { intent };
  };

  const agentNode = async (s: AgentGraphState): Promise<Partial<AgentGraphState>> => {
    const ctx: ToolContext = { userId: s.userId, role: s.role };
    const toolCalls: AgentGraphState['toolCalls'] = [];
    const refs: string[] = [];
    let needsHuman = false;

    // ===== Demo 规则模式（无 LLM Key）：确定性执行 =====
    if (!llm) {
      if (s.onEvent) await s.onEvent({ type: 'thinking', text: `已识别意图「${s.intent}」，进入规则引擎执行（演示模式）。` });
      const callTool = async (name: string, args: unknown) => {
        if (s.onEvent) await s.onEvent({ type: 'tool_start', name, args });
        const rec = await registry.call(ctx, name, args);
        toolCalls.push(rec);
        if (s.onEvent) {
          await s.onEvent({ type: 'tool_end', name, result: rec.result, error: rec.error, durationMs: rec.durationMs });
        }
        if (rec.error) needsHuman = true;
        return rec.result;
      };
      switch (s.intent) {
        case 'diagnose':
          await callTool('run_diagnosis', { studentId: ctx.userId });
          await callTool('get_error_book', { studentId: ctx.userId });
          break;
        case 'teach':
          await callTool('get_study_plan', { studentId: ctx.userId });
          await callTool('get_error_book', { studentId: ctx.userId });
          break;
        case 'generate':
          await callTool('generate_lesson_plan', { subject: '语文', grade: '五年级', topic: '草船借箭' });
          break;
        case 'knowledge':
          await callTool('search_knowledge', { query: s.input.slice(0, 80), topK: 5 });
          break;
        case 'admin':
          await callTool('get_region_overview', {});
          await callTool('list_alerts', { limit: 5 });
          break;
        default:
          await callTool('run_diagnosis', { studentId: ctx.userId });
      }
      const finalText = `（演示模式）已执行 ${toolCalls.length} 个工具步骤。${needsHuman ? '部分步骤失败，需人工介入。' : '详情见下方工具轨迹与结果。'}`;
      if (s.onEvent) await s.onEvent({ type: 'done', finalText, refs, intent: s.intent });
      return { toolCalls, finalText, refs, needsHuman };
    }

    // ===== LLM 模式：ReAct 循环（function calling） =====
    if (s.onEvent) await s.onEvent({ type: 'thinking', text: '开始理解任务：解析意图、规划所需工具与执行顺序。' });
    const messages: Array<Record<string, unknown>> = [
      {
        role: 'system',
        content:
          '你是「乡芽」乡镇教育智能体的核心 Agent，服务于教师/学生/家长/管理者。' +
          '规则：1) 知识类问题必须调用 search_knowledge 并只在返回片段内作答；2) 诊断学生必须先 run_diagnosis；' +
          '3) 每步工具调用的理由用自然语言简短说明（这将展示给用户）；4) 最终回答必须结构清晰、可操作、可追溯，并列出引用；' +
          '5) 绝不直接给学生题目答案（辅导用 socratic_tutor 引导）。',
      },
      { role: 'human', content: s.input },
    ];

    for (let turn = 0; turn < turnLimit; turn++) {
      const aiMsg = await llm.invoke(messages as never);
      const content = typeof aiMsg.content === 'string' ? aiMsg.content : '';
      const toolCallsRaw = (aiMsg.tool_calls ?? []) as Array<{ name?: string; args?: unknown }>;
      if (!toolCallsRaw.length) {
        // 无工具调用 = 最终回答
        const finalText = content;
        if (s.onEvent && content) await s.onEvent({ type: 'text_delta', delta: content });
        if (s.onEvent) await s.onEvent({ type: 'done', finalText, refs, intent: s.intent });
        return { toolCalls, finalText, refs, needsHuman };
      }
      for (const tc of toolCallsRaw) {
        const name = tc.name ?? '';
        const args = tc.args ?? {};
        if (s.onEvent) await s.onEvent({ type: 'tool_start', name, args });
        const rec = await registry.call(ctx, name, args);
        toolCalls.push(rec);
        if (s.onEvent) {
          await s.onEvent({ type: 'tool_end', name, result: rec.result, error: rec.error, durationMs: rec.durationMs });
        }
        if (rec.error) needsHuman = true;
        if (name === 'search_knowledge' && (rec.result as Array<{ ref: string }> | undefined)?.length) {
          (rec.result as Array<{ ref: string }>).forEach((c) => refs.push(c.ref));
        }
        messages.push({
          role: 'assistant',
          content: null,
          tool_calls: [{ id: `call_${turn}_${name}`, type: 'function', function: { name, arguments: JSON.stringify(args) } }],
        } as never);
        messages.push({
          role: 'tool',
          tool_call_id: `call_${turn}_${name}`,
          content: JSON.stringify(rec.result ?? { error: rec.error }).slice(0, 4000),
        } as never);
      }
    }
    const finalText = `已达到最大执行轮数（${turnLimit}），部分流程未完成，请人工介入。`;
    if (s.onEvent) await s.onEvent({ type: 'done', finalText, refs, intent: s.intent });
    return { toolCalls, finalText, refs, needsHuman: true };
  };

  const finalizeNode = async (s: AgentGraphState): Promise<Partial<AgentGraphState>> => {
    return { finalText: s.finalText, refs: s.refs };
  };

  const g = new StateGraph(State)
    .addNode('intent', intentNode)
    .addNode('agent', agentNode)
    .addNode('finalize', finalizeNode)
    .addEdge(START, 'intent')
    .addEdge('intent', 'agent')
    .addEdge('agent', 'finalize')
    .addEdge('finalize', END);

  const compiled = checkpointer ? g.compile({ checkpointer }) : g.compile();
  return compiled;
}
```

- [ ] **Step 3: 测试（Demo 模式 + LLM mock）**

`server/src/modules/agent/__tests__/agent-graph.test.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import { buildAgentGraph, detectIntent } from '../agent-graph';
import { buildRegistry, type ToolBindings, type ToolRegistry } from '../tool-registry';

const bindings: ToolBindings = {
  run_diagnosis: vi.fn(async () => ({ overallMastery: 60, dims: [] })),
  get_latest_diagnosis: vi.fn(async () => null),
  get_error_book: vi.fn(async () => [{ id: 1, errorType: '形近字混淆' }]),
  get_study_plan: vi.fn(async () => []),
  practice_questions: vi.fn(async () => []),
  submit_answer: vi.fn(async () => ({ isCorrect: true })),
  search_knowledge: vi.fn(async () => [{ ref: 'chunk:1', title: '草船借箭', content: '周瑜妒忌诸葛亮……' }]),
  socratic_tutor: vi.fn(async () => ({ stage: 'identify' })),
  generate_lesson_plan: vi.fn(async () => ({ id: 1, outline: '一、教学目标' })),
  generate_paper: vi.fn(async () => ({ id: 1 })),
  get_class_overview: vi.fn(async () => ({ total: 42 })),
  researcher_comment: vi.fn(async () => ({ score: 88 })),
  get_weekly_report: vi.fn(async () => ({})),
  get_region_overview: vi.fn(async () => ({ schools: 9 })),
  list_alerts: vi.fn(async () => []),
  get_teacher_profile: vi.fn(async () => ({})),
};
const registry: ToolRegistry = buildRegistry(bindings);

describe('detectIntent', () => {
  it('意图分类', () => {
    expect(detectIntent('帮我诊断学习薄弱点')).toBe('diagnose');
    expect(detectIntent('备一节语文课')).toBe('generate');
    expect(detectIntent('草船借箭讲了什么')).toBe('knowledge');
    expect(detectIntent('区域预警情况')).toBe('admin');
    expect(detectIntent('我的学习计划')).toBe('teach');
  });
});

describe('buildAgentGraph (demo mode)', () => {
  it('诊断意图按规则执行工具并产出摘要', async () => {
    const events: string[] = [];
    const graph = buildAgentGraph({ llm: null, registry });
    const out = await graph.invoke({
      input: '帮我诊断学习薄弱点',
      userId: 5, role: 'student', intent: 'general',
      toolCalls: [], finalText: '', refs: [], needsHuman: false,
      onEvent: (ev) => { events.push(ev.type); return Promise.resolve(); },
    });
    expect(out.toolCalls.map((t) => t.tool)).toEqual(['run_diagnosis', 'get_error_book']);
    expect(out.needsHuman).toBe(false);
    expect(events).toContain('thinking');
    expect(events).toContain('tool_start');
    expect(events).toContain('tool_end');
    expect(events).toContain('done');
  });

  it('知识意图调用 search_knowledge', async () => {
    const graph = buildAgentGraph({ llm: null, registry });
    const out = await graph.invoke({
      input: '草船借箭讲了什么故事？',
      userId: 5, role: 'student', intent: 'general',
      toolCalls: [], finalText: '', refs: [], needsHuman: false, onEvent: undefined,
    });
    expect(out.toolCalls[0].tool).toBe('search_knowledge');
    expect(out.refs).toContain('chunk:1');
  });
});

describe('buildAgentGraph (LLM mode)', () => {
  it('LLM 先调工具再给最终回答', async () => {
    const llm = {
      invoke: vi
        .fn()
        .mockResolvedValueOnce({
          content: '',
          tool_calls: [{ name: 'run_diagnosis', args: { studentId: 5 } }],
        })
        .mockResolvedValueOnce({
          content: '诊断完成：整体掌握度 60%，薄弱点为形近字辨析。建议针对性练习。',
          tool_calls: [],
        }),
    } as any;
    const graph = buildAgentGraph({ llm, registry });
    const out = await graph.invoke({
      input: '诊断一下', userId: 5, role: 'student', intent: 'general',
      toolCalls: [], finalText: '', refs: [], needsHuman: false, onEvent: undefined,
    });
    expect(out.toolCalls).toHaveLength(1);
    expect(out.toolCalls[0].tool).toBe('run_diagnosis');
    expect(out.finalText).toContain('形近字辨析');
    expect(llm.invoke).toHaveBeenCalledTimes(2);
  });

  it('工具失败标记 needsHuman', async () => {
    const bind = { ...bindings, run_diagnosis: vi.fn(async () => { throw new Error('诊断服务不可用'); }) };
    const reg = buildRegistry(bind);
    const llm = {
      invoke: vi.fn().mockResolvedValueOnce({
        content: '', tool_calls: [{ name: 'run_diagnosis', args: { studentId: 5 } }],
      }),
    } as any;
    const graph = buildAgentGraph({ llm, registry: reg });
    const out = await graph.invoke({
      input: '诊断', userId: 5, role: 'student', intent: 'general',
      toolCalls: [], finalText: '', refs: [], needsHuman: false, onEvent: undefined,
    });
    expect(out.toolCalls[0].error).toContain('诊断服务不可用');
    expect(out.needsHuman).toBe(true);
  });
});
```

- [ ] **Step 4: 运行测试**

Run: `npm test -- --reporter=verbose`
Expected: 新增 6 个用例通过（LangGraph 首次运行打印版本信息可忽略）

- [ ] **Step 5: Commit**

```bash
git add server/src/modules/agent/agent-graph.ts server/src/modules/agent/__tests__/agent-graph.test.ts
git commit -m "feat: Agent 循环核心（意图识别+ReAct 工具循环+事件流，Demo/LLM 双模式）"
```

---

## Task 6: SSE 流式网关（AgentService + Controller）

**Files:**
- Create: `server/src/modules/agent/agent.service.ts`
- Create: `server/src/modules/agent/agent.controller.ts`
- Create: `server/src/modules/agent/agent.module.ts`
- Modify: `server/src/app.module.ts`

**设计（参照 opencode SSE 事件协议）：** `POST /agent/chat` 返回 `text/event-stream`，事件行格式 `event: <type>\ndata: <json>\n\n`。事件类型：`thinking`/`tool_start`/`tool_end`/`text_delta`/`done`/`error`。同时全量轨迹落库 `agent_runs`+`agent_messages`，供历史回放（`GET /agent/runs/:id`）。

- [ ] **Step 1: AgentService（装配 registry bindings + 图 + 落库 + SSE 转发）**

`server/src/modules/agent/agent.service.ts`:

```ts
import { Injectable, Logger } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { ChatOpenAI } from '@langchain/openai';
import { SqliteSaver } from '@langchain/langgraph-checkpoint-sqlite';
import { AgentRun, AgentMessage } from '../../db/entities/agent.entities';
import { StudentService } from '../student/student.service';
import { TeacherService } from '../teacher/teacher.service';
import { OrgService } from '../org/org.service';
import { ParentService } from '../parent/parent.service';
import { AdminService } from '../admin/admin.service';
import { HybridRetriever } from '../knowledge/hybrid-retriever';
import { AIService } from '../ai/ai.service';
import { JwtUser } from '../../common/decorators/current-user.decorator';
import { BizException } from '../../common/exceptions/biz.exception';
import { ErrorCodes } from '../../common/exceptions/error-codes';
import { buildRegistry, type ToolBindings, type ToolContext } from './tool-registry';
import { buildAgentGraph, type AgentEvent } from './agent-graph';

@Injectable()
export class AgentService {
  private readonly logger = new Logger(AgentService.name);

  constructor(
    private readonly student: StudentService,
    private readonly teacher: TeacherService,
    private readonly org: OrgService,
    private readonly parent: ParentService,
    private readonly admin: AdminService,
    private readonly retriever: HybridRetriever,
    private readonly ai: AIService,
    @InjectRepository(AgentRun) private readonly runs: Repository<AgentRun>,
    @InjectRepository(AgentMessage) private readonly msgs: Repository<AgentMessage>,
  ) {}

  /** 工具绑定：复用现有业务 Service（真实数据闭环） */
  private buildRegistry() {
    const b: ToolBindings = {
      run_diagnosis: async (sid) => this.student.runDiagnosis({ id: sid, role: 'student' } as JwtUser),
      get_latest_diagnosis: async (sid) => this.student.latestDiagnosis({ id: sid, role: 'student' } as JwtUser),
      get_error_book: async (sid) => this.student.errorBook({ id: sid, role: 'student' } as JwtUser),
      get_study_plan: async (sid) => this.student.studyPlan({ id: sid, role: 'student' } as JwtUser),
      practice_questions: async (kpId, count) => this.student.practiceQuestions({ id: 0, role: 'student' } as JwtUser, kpId ?? undefined, count, undefined),
      submit_answer: async (qid, ans) => this.student.submitAnswer({ id: 0, role: 'student' } as JwtUser, { questionId: qid, answer: ans }),
      search_knowledge: async (query, topK) => this.retriever.retrieve(query, topK ?? 5),
      socratic_tutor: async (problem, reply) => this.student.socraticTurn(problem, reply),
      generate_lesson_plan: async (args) => this.teacher.generateLessonPlan({ id: 0, role: 'teacher' } as JwtUser, { ...args, grade: args.grade ?? '五年级' }),
      generate_paper: async (args) => this.teacher.generatePaper({ id: 0, role: 'teacher' } as JwtUser, { ...args, grade: args.grade ?? '五年级', questionCount: args.questionCount ?? 6 }),
      get_class_overview: async (cid) => this.org.classOverview(cid),
      researcher_comment: async (src) => this.teacher.researcher({ id: 0, role: 'teacher' } as JwtUser, 'comment', { sourceContent: src }),
      get_weekly_report: async (sid) => this.parent.weeklyReportFor({ id: 0, role: 'parent' } as JwtUser, sid),
      get_region_overview: async () => this.admin.regionOverview(),
      list_alerts: async (type, limit) => this.admin.listAlerts(type, limit),
      get_teacher_profile: async (tid) => this.admin.teacherProfile(tid),
    };
    return buildRegistry(b);
  }

  private buildLlm() {
    if (this.ai.isDemo) return null;
    const provider = (this.ai as any).provider as { baseUrl?: string; apiKey?: string; model?: string };
    return new ChatOpenAI({
      model: provider?.model ?? 'deepseek-chat',
      apiKey: provider?.apiKey ?? '',
      configuration: { baseURL: provider?.baseUrl },
      temperature: 0.4,
    });
  }

  private async checkpointer(): Promise<SqliteSaver | undefined> {
    try {
      const dbPath = process.env.DB_ABS || './data/xiangya.db';
      return await SqliteSaver.fromConnString(dbPath);
    } catch (e) {
      this.logger.warn(`checkpoint-sqlite 初始化失败，本轮记忆关闭: ${(e as Error).message}`);
      return undefined;
    }
  }

  /** 流式执行：返回 AsyncIterable<AgentEvent>，Controller 转 SSE */
  async *stream(user: JwtUser, input: { task: string }): AsyncGenerator<AgentEvent> {
    if (!input.task?.trim()) throw new BizException(ErrorCodes.VALIDATE_ERROR, 'task 必填');
    const text = input.task.trim();

    const run = await this.runs.save(
      this.runs.create({ userId: user.id, role: user.role, taskInput: text.slice(0, 500), intent: 'general', status: 'running', durationMs: 0, toolCalls: 0 }),
    );
    const started = Date.now();
    const emit = async (ev: AgentEvent) => {
      await this.persistEvent(run.id, ev);
      yield ev as never; // 双消费者：落库 + 流出（见 execute 的代理写法）
    };
    // 简化：先用数组缓冲 + 事件流（避免 async generator 双消费问题）
    const events: AgentEvent[] = [];
    const collect = async (ev: AgentEvent) => { events.push(ev); await this.persistEvent(run.id, ev); };

    try {
      const registry = this.buildRegistry();
      const llm = this.buildLlm();
      const graph = buildAgentGraph({ llm, registry, checkpointer: await this.checkpointer() });
      const ctx: ToolContext = { userId: user.id, role: user.role, studentId: user.id, classId: 1 };
      const out = await graph.invoke({
        input: text,
        userId: user.id,
        role: user.role,
        intent: 'general',
        toolCalls: [],
        finalText: '',
        refs: [],
        needsHuman: false,
        onEvent: collect,
      } as never);

      await this.runs.update(run.id, {
        status: out.needsHuman ? 'needs_human' : 'success',
        intent: (out as { intent?: string }).intent ?? 'general',
        summary: out.finalText?.slice(0, 2000),
        durationMs: Date.now() - started,
        toolCalls: out.toolCalls.length,
      });
      for (const ev of events) yield ev;
    } catch (e) {
      const msg = (e as Error).message?.slice(0, 300) ?? 'unknown';
      this.logger.error(`agent run ${run.id} failed: ${msg}`);
      await this.runs.update(run.id, { status: 'failed', summary: `执行失败：${msg}`, durationMs: Date.now() - started });
      yield { type: 'error', text: msg } as never;
    }
  }

  /** 事件持久化（轨迹回放数据源） */
  private async persistEvent(runId: number, ev: AgentEvent) {
    try {
      switch (ev.type) {
        case 'thinking':
          await this.msgs.save(this.msgs.create({ runId, role: 'assistant', kind: 'thinking', content: ev.text, status: 'done' }));
          break;
        case 'tool_start':
          await this.msgs.save(this.msgs.create({ runId, role: 'assistant', kind: 'tool_call', tool: ev.name, argsJson: JSON.stringify(ev.args)?.slice(0, 2000), status: 'running' }));
          break;
        case 'tool_end':
          await this.msgs.save(this.msgs.create({ runId, role: 'tool', kind: 'tool_result', tool: ev.name, resultJson: JSON.stringify(ev.result ?? { error: ev.error })?.slice(0, 2000), status: ev.error ? 'error' : 'done', durationMs: ev.durationMs }));
          break;
        case 'text_delta':
          await this.msgs.save(this.msgs.create({ runId, role: 'assistant', kind: 'text', content: ev.delta, status: 'done' }));
          break;
        case 'done':
          await this.msgs.save(this.msgs.create({ runId, role: 'assistant', kind: 'final', content: ev.finalText, refsJson: JSON.stringify(ev.refs), status: 'done' }));
          break;
        case 'error':
          await this.msgs.save(this.msgs.create({ runId, role: 'assistant', kind: 'final', content: `⚠️ ${ev.text}`, status: 'error' }));
          break;
      }
    } catch (e) {
      this.logger.warn(`事件持久化失败: ${(e as Error).message}`);
    }
  }

  /** 历史运行列表 */
  async listRuns(user: JwtUser, page = 1, pageSize = 20) {
    const [list, total] = await this.runs.findAndCount({
      where: user.role === 'admin' ? {} : { userId: user.id },
      order: { id: 'DESC' },
      skip: (page - 1) * pageSize,
      take: Math.min(pageSize, 100),
    });
    return { list: list.map((r) => ({ id: r.id, taskInput: r.taskInput, intent: r.intent, status: r.status, durationMs: r.durationMs, toolCalls: r.toolCalls, createdAt: r.createdAt, summary: r.summary?.slice(0, 200) })), total, page, pageSize };
  }

  /** 单次运行全链路回放 */
  async runDetail(user: JwtUser, id: number) {
    const run = await this.runs.findOne({ where: { id } });
    if (!run) throw new BizException(ErrorCodes.NOT_FOUND);
    if (user.role !== 'admin' && run.userId !== user.id) throw new BizException(ErrorCodes.SCOPE_FORBIDDEN);
    const msgs = await this.msgs.find({ where: { runId: id }, order: { id: 'ASC' } });
    return { ...run, messages: msgs.map((m) => ({ id: m.id, role: m.role, kind: m.kind, content: m.content, tool: m.tool, args: m.argsJson ? JSON.parse(m.argsJson) : null, result: m.resultJson ? JSON.parse(m.resultJson) : null, status: m.status, durationMs: m.durationMs, refs: m.refsJson ? JSON.parse(m.refsJson) : null })) };
  }
}
```

- [ ] **Step 2: Controller（SSE 流式端点）**

`server/src/modules/agent/agent.controller.ts`:

```ts
import { Body, Controller, Get, Param, ParseIntPipe, Post, Query, Res, Sse } from '@nestjs/common';
import { Response } from 'express';
import { AgentService } from './agent.service';
import { CurrentUser, JwtUser } from '../../common/decorators/current-user.decorator';
import { RateLimit, Roles } from '../../common/decorators/security.decorators';

@Controller('agent')
export class AgentController {
  constructor(private readonly agent: AgentService) {}

  /** SSE 流式对话入口（思考/工具/文本逐步推送） */
  @Roles('student', 'teacher', 'parent', 'admin')
  @RateLimit({ limit: 10, windowSec: 60, keyPrefix: 'agent-chat', byUser: true })
  @Post('chat')
  async chat(@CurrentUser() user: JwtUser, @Body() body: { task: string }, @Res() res: Response) {
    res.setHeader('Content-Type', 'text/event-stream; charset=utf-8');
    res.setHeader('Cache-Control', 'no-cache, no-transform');
    res.setHeader('Connection', 'keep-alive');
    res.setHeader('X-Accel-Buffering', 'no');
    res.flushHeaders?.();
    const send = (event: string, data: unknown) => res.write(`event: ${event}\ndata: ${JSON.stringify(data)}\n\n`);
    try {
      for await (const ev of this.agent.stream(user, body)) {
        send(ev.type, ev);
      }
    } catch (e) {
      send('error', { type: 'error', text: (e as Error).message });
    } finally {
      res.end();
    }
  }

  @Roles('student', 'teacher', 'parent', 'admin')
  @Get('runs')
  list(@CurrentUser() user: JwtUser, @Query('page') page?: string, @Query('pageSize') pageSize?: string) {
    return this.agent.listRuns(user, page ? Number(page) : 1, pageSize ? Number(pageSize) : 20);
  }

  @Roles('student', 'teacher', 'parent', 'admin')
  @Get('runs/:id')
  detail(@CurrentUser() user: JwtUser, @Param('id', ParseIntPipe) id: number) {
    return this.agent.runDetail(user, id);
  }
}
```

- [ ] **Step 3: Module 与注册**

`server/src/modules/agent/agent.module.ts`:

```ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { AgentRun, AgentMessage } from '../../db/entities/agent.entities';
import { AgentService } from './agent.service';
import { AgentController } from './agent.controller';
import { StudentModule } from '../student/student.module';
import { TeacherModule } from '../teacher/teacher.module';
import { OrgModule } from '../org/org.module';
import { ParentModule } from '../parent/parent.module';
import { AdminModule } from '../admin/admin.module';

@Module({
  imports: [TypeOrmModule.forFeature([AgentRun, AgentMessage]), StudentModule, TeacherModule, OrgModule, ParentModule, AdminModule],
  providers: [AgentService],
  controllers: [AgentController],
  exports: [AgentService],
})
export class AgentModule {}
```

`server/src/app.module.ts`：imports 数组追加 `AgentModule` 与 `KnowledgeModule`（Task 2 已建，需注册）。

- [ ] **Step 4: 补齐被引用的 Service 方法（如缺失）**

按 `npx tsc --noEmit` 报错逐个补齐：
- `student.socraticTurn(problem, reply)`：Task 10 提供；此处先声明 stub 返回 `{ stage: 'read', text: '（苏格拉底模块接入中）' }`
- `org.classOverview(classId)`：复用 OrgService 现有聚合或新增
- `parent.weeklyReportFor(user, studentId)`：包装现有 `weeklyReport`
- `admin.regionOverview()` / `admin.listAlerts(type, limit)` / `admin.teacherProfile(teacherId)`：包装现有方法（按 admin.service.ts 现有签名适配）

- [ ] **Step 5: 冒烟验证**

Run: `npm run start:dev`（`server/` 目录，需 .env）
PowerShell 验证（登录拿 token 后）：

```powershell
curl.exe -N -s -X POST http://localhost:3000/api/v1/agent/chat -H "Content-Type: application/json" -H "X-CSRF-Token: <token>" -H "Authorization: Bearer <token>" -d "{\"task\":\"帮我诊断学习薄弱点\"}"
```

Expected: 依次输出 `event: thinking`、`event: tool_start`、`event: tool_end`、`event: done` 的 SSE 流。

- [ ] **Step 6: Commit**

```bash
git add server/src/modules/agent/ server/src/app.module.ts
git commit -m "feat: SSE 流式网关——思考/工具/文本事件实时推送+全链路轨迹落库"
```

---

## Task 7: Agent 工作台前端（步骤流 UI，opencode 风格）

**Files:**
- Create: `public/agent.html`（工作台页面，复用原型设计语言）
- Create: `src/frontend/entries/agent-main.ts`
- Modify: `build-frontend.mjs`（增加 agent 入口）
- Modify: `public/login.html`（登录后跳转 agent.html 为主入口）

**设计（参照 opencode 步骤流与 langgraph-ui）：** 三栏布局——左：会话/任务历史；中：消息流（用户气泡 → 助手块：可折叠思考块 + 工具卡片列表 + 流式文本 + 最终交付卡片含引用）；右：运行详情侧栏（全链路 trace）。`EventSource` 不可用（POST），用 `fetch` + `ReadableStream` 解析 SSE。

- [ ] **Step 1: agent.html 骨架（内联样式 + 容器）**

`public/agent.html` 关键结构（完整文件在实施时按原型设计语言补全 CSS）：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>乡芽 · Agent 工作台</title>
  <style>
    /* 复用原型 CSS 变量；新增步骤流样式：.step-card .thinking .tool-card .delivery */
    :root { --primary:#2f6f4f; --bg:#f6f7f5; --card:#fff; --text:#223; --muted:#889; }
    body { margin:0; font-family:system-ui,-apple-system,'PingFang SC','Microsoft YaHei',sans-serif; background:var(--bg); }
    .layout { display:grid; grid-template-columns:240px 1fr 300px; height:100vh; }
    .sidebar { background:var(--card); border-right:1px solid #e5e7eb; overflow-y:auto; padding:12px; }
    .main { display:flex; flex-direction:column; min-width:0; }
    .stream { flex:1; overflow-y:auto; padding:20px 28px; }
    .composer { padding:14px 28px; border-top:1px solid #e5e7eb; background:var(--card); display:flex; gap:8px; }
    .composer textarea { flex:1; resize:none; height:52px; border:1px solid #d5d8dc; border-radius:10px; padding:10px 12px; font-size:14px; }
    .msg-user { background:#e7f2ec; border-radius:12px; padding:10px 14px; max-width:70%; margin:8px 0 8px auto; }
    .step { background:var(--card); border:1px solid #e5e7eb; border-radius:12px; margin:10px 0; overflow:hidden; }
    .step-head { padding:8px 14px; font-size:13px; color:var(--muted); cursor:pointer; display:flex; align-items:center; gap:8px; }
    .thinking { background:#fbf7ee; } .thinking-body { padding:10px 14px; font-size:13px; white-space:pre-wrap; color:#6b5b3e; }
    .tool-card { border-top:1px solid #eef0f2; padding:8px 14px; font-size:13px; }
    .tool-card .tool-name { font-weight:600; color:var(--primary); }
    .tool-card .tool-meta { color:var(--muted); font-size:12px; margin-top:2px; }
    .tool-card details { margin-top:6px; } .tool-card summary { cursor:pointer; color:var(--muted); font-size:12px; }
    .tool-card pre { background:#f4f5f7; border-radius:8px; padding:8px; font-size:12px; overflow-x:auto; max-height:220px; }
    .tool-done { color:#2e7d32; } .tool-error { color:#c62828; }
    .text-stream { padding:10px 14px; font-size:14px; line-height:1.7; white-space:pre-wrap; }
    .delivery { border-top:1px solid #eef0f2; padding:12px 14px; }
    .refs { display:flex; flex-wrap:wrap; gap:6px; padding:10px 14px; border-top:1px solid #eef0f2; }
    .ref-tag { background:#e8f1ec; color:var(--primary); border-radius:999px; padding:2px 10px; font-size:12px; }
    .detail-pane { background:var(--card); border-left:1px solid #e5e7eb; overflow-y:auto; padding:12px; font-size:12px; }
    .run-item { padding:8px 10px; border-radius:8px; cursor:pointer; border:1px solid #e5e7eb; margin-bottom:6px; }
    .run-item:hover { background:#f2f7f4; }
  </style>
</head>
<body>
<div class="layout">
  <aside class="sidebar">
    <h3 style="margin:8px 12px">🗂 任务历史</h3>
    <div id="runList"></div>
  </aside>
  <main class="main">
    <div class="stream" id="stream">
      <div class="welcome" style="text-align:center;color:var(--muted);margin-top:20%">
        <h2>乡芽 Agent 工作台</h2>
        <p>我可以诊断学情、生成教案、解答知识、规划学习、查看预警…<br>每步思考与工具调用都会展示给你，可全程追溯。</p>
      </div>
    </div>
    <div class="composer">
      <textarea id="taskInput" placeholder="描述你的任务，例如：帮学生:5 诊断薄弱点并给出建议…"></textarea>
      <button id="sendBtn" style="border:none;background:var(--primary);color:#fff;border-radius:10px;padding:0 22px;font-size:14px">发送</button>
    </div>
  </main>
  <aside class="detail-pane" id="detailPane">
    <h4 style="margin:8px 4px">🧬 运行详情</h4>
    <div id="detailBody" style="color:var(--muted)">选择一次运行查看全链路轨迹</div>
  </aside>
</div>
<script src="/assets/app/agent-main.js"></script>
</body>
</html>
```

- [ ] **Step 2: agent-main.ts（SSE 解析 + 步骤流渲染）**

`src/frontend/entries/agent-main.ts`:

```ts
import { get, post, toast, esc } from '../core/request';
import { guardInit } from '../core/guard';

type Ev =
  | { type: 'thinking'; text: string }
  | { type: 'tool_start'; name: string; args: unknown }
  | { type: 'tool_end'; name: string; result?: unknown; error?: string; durationMs: number }
  | { type: 'text_delta'; delta: string }
  | { type: 'done'; finalText: string; refs: string[]; intent: string }
  | { type: 'error'; text: string };

const $ = <T extends HTMLElement = HTMLElement>(id: string) => document.getElementById(id) as T;

function stepCard(type: string, icon: string): HTMLElement {
  const card = document.createElement('div');
  card.className = 'step';
  card.dataset.step = type;
  const head = document.createElement('div');
  head.className = 'step-head';
  head.innerHTML = `${icon} <span>${type === 'thinking' ? '思考' : type === 'tool_start' ? '工具调用' : '输出'}</span>`;
  card.appendChild(head);
  return card;
}

function renderThinking(card: HTMLElement, text: string) {
  const body = document.createElement('div');
  body.className = 'thinking-body';
  body.textContent = text;
  card.appendChild(body);
  const head = card.querySelector('.step-head') as HTMLElement;
  head.addEventListener('click', () => body.style.display = body.style.display === 'none' ? 'block' : 'none');
}

let currentToolCard: HTMLElement | null = null;

function renderToolStart(card: HTMLElement, name: string, args: unknown) {
  const body = document.createElement('div');
  body.className = 'tool-card';
  body.innerHTML = `
    <div class="tool-name">🔧 ${esc(name)} <span class="tool-meta">运行中…</span></div>
    <details><summary>参数</summary><pre>${esc(JSON.stringify(args, null, 2))}</pre></details>`;
  card.appendChild(body);
  currentToolCard = body;
}

function renderToolEnd(name: string, rec: { result?: unknown; error?: string; durationMs: number }) {
  if (!currentToolCard) return;
  const meta = currentToolCard.querySelector('.tool-meta')!;
  const ok = !rec.error;
  meta.textContent = ok ? `✓ 完成 · ${rec.durationMs}ms` : `✗ 失败 · ${rec.durationMs}ms`;
  meta.className = 'tool-meta ' + (ok ? 'tool-done' : 'tool-error');
  const details = document.createElement('details');
  details.innerHTML = `<summary>${ok ? '结果' : '错误'}</summary><pre>${esc(JSON.stringify(rec.result ?? { error: rec.error }, null, 2))}</pre>`;
  currentToolCard.appendChild(details);
  currentToolCard = null;
}

function renderText(card: HTMLElement, delta: string) {
  let el = card.querySelector('.text-stream') as HTMLElement | null;
  if (!el) { el = document.createElement('div'); el.className = 'text-stream'; card.appendChild(el); }
  el.textContent += delta;
}

function renderDone(card: HTMLElement, ev: Ev & { type: 'done' }) {
  const del = document.createElement('div');
  del.className = 'delivery';
  del.innerHTML = `<b>✅ 交付完成</b><p style="margin:8px 0 0;white-space:pre-wrap">${esc(ev.finalText)}</p>`;
  card.appendChild(del);
  if (ev.refs?.length) {
    const refs = document.createElement('div');
    refs.className = 'refs';
    refs.innerHTML = ev.refs.map((r) => `<span class="ref-tag">📖 ${esc(r)}</span>`).join('');
    card.appendChild(refs);
  }
}

function appendUser(text: string) {
  const div = document.createElement('div');
  div.className = 'msg-user';
  div.textContent = text;
  $('stream').appendChild(div);
}

async function sendTask(task: string) {
  const stream = $('stream');
  appendUser(task);
  const status = stepCard('thinking', '🤔');
  stream.appendChild(status);
  renderThinking(status, '正在理解任务…');
  stream.scrollTop = stream.scrollHeight;
  let textCard = status;
  try {
    const res = await fetch('/api/v1/agent/chat', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify({ task }),
    });
    if (!res.ok || !res.body) throw new Error(`HTTP ${res.status}`);
    const reader = res.body.getReader();
    const decoder = new TextDecoder();
    let buf = '';
    const handleEvent = (ev: Ev) => {
      switch (ev.type) {
        case 'thinking': {
          const c = stepCard('thinking', '🤔');
          stream.appendChild(c);
          renderThinking(c, ev.text);
          textCard = c;
          break;
        }
        case 'tool_start': {
          const c = stepCard('tool_start', '🔧');
          stream.appendChild(c);
          renderToolStart(c, ev.name, ev.args);
          textCard = c;
          break;
        }
        case 'tool_end':
          renderToolEnd(ev.name, ev);
          break;
        case 'text_delta':
          renderText(textCard, ev.delta);
          break;
        case 'done':
          renderDone(textCard, ev);
          break;
        case 'error':
          toast('⚠️ ' + ev.text);
          break;
      }
      stream.scrollTop = stream.scrollHeight;
    };
    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      buf += decoder.decode(value, { stream: true });
      const parts = buf.split('\n\n');
      buf = parts.pop() ?? '';
      for (const part of parts) {
        const lines = part.split('\n');
        const evLine = lines.find((l) => l.startsWith('event: '));
        const dataLine = lines.find((l) => l.startsWith('data: '));
        if (!evLine || !dataLine) continue;
        try { handleEvent(JSON.parse(dataLine.slice(6)) as Ev); } catch { /* 跳过坏帧 */ }
      }
    }
    await loadRuns();
  } catch (e) {
    toast((e as Error).message || '发送失败');
  }
}

async function loadRuns() {
  try {
    const res = await get<any>('/agent/runs?page=1&pageSize=10');
    const list = res?.list ?? [];
    const box = $('runList');
    box.innerHTML = list.length
      ? list.map((r: any) =>
          `<div class="run-item" data-id="${r.id}">
             <b>#${r.id}</b> ${esc((r.taskInput || '').slice(0, 30))}<br>
             <span style="font-size:11px;color:#889">${esc(r.intent)} · ${esc(r.status)} · ${r.toolCalls} 个工具 · ${r.durationMs}ms</span>
           </div>`).join('')
      : '<div style="color:#889;font-size:12px;padding:8px">暂无任务</div>';
    box.querySelectorAll('.run-item').forEach((el) =>
      el.addEventListener('click', async () => {
        const id = (el as HTMLElement).dataset.id;
        const detail = await get<any>(`/agent/runs/${id}`);
        renderDetail(detail);
      }),
    );
  } catch { /* 静默 */ }
}

function renderDetail(run: any) {
  const body = $('detailBody');
  body.innerHTML = `<b>#${run.id}</b> ${esc(run.taskInput)}<br><span style="color:#889">${esc(run.status)} · ${run.durationMs}ms · ${run.toolCalls} 个工具</span>
    ${(run.messages || []).map((m: any) => {
      if (m.kind === 'thinking') return `<div style="margin-top:8px">🤔 <b>思考</b><pre style="white-space:pre-wrap">${esc(m.content || '')}</pre></div>`;
      if (m.kind === 'tool_call') return `<div style="margin-top:8px">🔧 <b>${esc(m.tool || '')}</b><pre>${esc(JSON.stringify(m.args ?? {}, null, 2))}</pre></div>`;
      if (m.kind === 'tool_result') return `<div style="margin-top:6px;color:#889">  ↳ ${esc(m.status)} · ${m.durationMs}ms</div>`;
      if (m.kind === 'final') return `<div style="margin-top:8px;background:#f2f7f4;border-radius:8px;padding:8px"><b>交付</b><pre style="white-space:pre-wrap">${esc(m.content || '')}</pre></div>`;
      return '';
    }).join('')}`;
}

function init() {
  guardInit({ fallback: '/login.html' });
  const sendBtn = $('sendBtn');
  const input = $<HTMLTextAreaElement>('taskInput');
  sendBtn.addEventListener('click', () => {
    const t = input.value.trim();
    if (!t) return toast('请输入任务');
    input.value = '';
    void sendTask(t);
  });
  input.addEventListener('keydown', (e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendBtn.click(); } });
  void loadRuns();
}
init();
```

- [ ] **Step 3: 构建配置**

`build-frontend.mjs` 的入口列表追加 `src/frontend/entries/agent-main.ts` → `public/assets/app/agent-main.js`（按现有模式复制一行）。

- [ ] **Step 4: 登录跳转调整**

`public/login.html` 或 `src/frontend/entries/login-main.ts` 中 `homeOf()` 目标改为 `agent.html`（四端页面保留为工作台内可跳转的数据视图）。

- [ ] **Step 5: 构建验证**

Run: `node build-frontend.mjs`（项目根目录）
Expected: `public/assets/app/agent-main.js` 生成，无报错

- [ ] **Step 6: Commit**

```bash
git add public/agent.html src/frontend/entries/agent-main.ts build-frontend.mjs public/login.html src/frontend/entries/login-main.ts
git commit -m "feat: Agent 工作台前端（步骤流 UI：思考折叠+工具卡片+流式文本+引用+trace 侧栏）"
```

---

## Task 8: BKT 认知诊断引擎（替换正确率统计）

**Files:**
- Create: `server/src/modules/diagnosis/bkt.ts`
- Create: `server/src/modules/diagnosis/__tests__/bkt.test.ts`
- Modify: `server/src/modules/student/student.service.ts`（`runDiagnosis` 接入）

**设计：** 标准二态 HMM 前向滤波（P(L0)/P(T)/P(G)/P(S)）+ 一阶遗忘衰减 + 数据 ≥15 条时 EM 在线拟合。确定性可复现。

- [ ] **Step 1: bkt.ts（纯函数引擎）**

`server/src/modules/diagnosis/bkt.ts`:

```ts
export interface BktParams { pL0: number; pT: number; pG: number; pS: number; }
export interface BktObs { correct: boolean; daysSinceLast: number; }
export interface BktResult { mastery: number; confidence: number; evidenceCount: number; }

export const DEFAULT_PARAMS: BktParams = { pL0: 0.15, pT: 0.35, pG: 0.25, pS: 0.15 };

export function forget(prob: number, days: number, pL0: number, rate = 0.05): number {
  if (days <= 0) return prob;
  const w = Math.exp(-rate * days);
  return prob * w + pL0 * (1 - w);
}

/** BKT 前向滤波：预测(遗忘+学习转移) → 观测更新 */
export function bktFilter(obs: BktObs[], params: BktParams = DEFAULT_PARAMS): BktResult {
  let L = params.pL0;
  for (const o of obs) {
    L = forget(L, o.daysSinceLast, params.pL0);
    const predicted = L + (1 - L) * params.pT;
    if (o.correct) {
      const denom = predicted * (1 - params.pS) + (1 - predicted) * params.pG;
      L = (predicted * (1 - params.pS)) / (denom || 1e-9);
    } else {
      const denom = predicted * params.pS + (1 - predicted) * (1 - params.pG);
      L = (predicted * params.pS) / (denom || 1e-9);
    }
  }
  const variance = L * (1 - L);
  return { mastery: round3(L), confidence: round3(Math.max(0, Math.min(1, 1 - 4 * variance))), evidenceCount: obs.length };
}

/** EM 在线拟合（作答 ≥15 条时调用） */
export function fitByEM(obs: BktObs[], init: BktParams = DEFAULT_PARAMS, rounds = 20): BktParams {
  let p = { ...init };
  for (let r = 0; r < rounds; r++) {
    let L = p.pL0;
    const ls: number[] = [L];
    for (const o of obs) {
      L = forget(L, o.daysSinceLast, p.pL0);
      const predicted = L + (1 - L) * p.pT;
      const denom = predicted * (1 - p.pS) + (1 - predicted) * p.pG;
      L = o.correct ? (predicted * (1 - p.pS)) / (denom || 1e-9) : (predicted * p.pS) / ((predicted * p.pS + (1 - predicted) * (1 - p.pG)) || 1e-9);
      ls.push(L);
    }
    const n = Math.max(obs.length, 1);
    const correctCount = obs.filter((o) => o.correct).length;
    const avgL = ls.reduce((s, x) => s + x, 0) / ls.length;
    p = {
      pL0: clamp(avgL, 0.05, 0.6),
      pT: clamp(ls.filter((x) => x > 0.5).length / n, 0.05, 0.6),
      pG: clamp(correctCount / n - 0.1, 0.05, 0.4),
      pS: clamp(1 - correctCount / n - 0.1, 0.05, 0.4),
    };
  }
  return { pL0: round3(p.pL0), pT: round3(p.pT), pG: round3(p.pG), pS: round3(p.pS) };
}

function clamp(x: number, min: number, max: number): number { return Math.min(max, Math.max(min, x)); }
function round3(x: number): number { return Math.round(x * 1000) / 1000; }
```

- [ ] **Step 2: 测试**

`server/src/modules/diagnosis/__tests__/bkt.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { bktFilter, fitByEM, forget, DEFAULT_PARAMS } from '../bkt';

describe('bktFilter', () => {
  it('全对序列掌握度单调上升', () => {
    const obs = Array.from({ length: 10 }, () => ({ correct: true, daysSinceLast: 1 }));
    let prev = 0;
    for (let i = 1; i <= obs.length; i++) {
      const r = bktFilter(obs.slice(0, i), DEFAULT_PARAMS);
      expect(r.mastery).toBeGreaterThanOrEqual(prev);
      prev = r.mastery;
    }
    expect(bktFilter(obs).mastery).toBeGreaterThan(0.8);
  });
  it('全错序列掌握度低位', () => {
    const obs = Array.from({ length: 10 }, () => ({ correct: false, daysSinceLast: 1 }));
    expect(bktFilter(obs).mastery).toBeLessThan(0.3);
  });
  it('遗忘衰减与确定性（可复现）', () => {
    const a = bktFilter([{ correct: true, daysSinceLast: 1 }, { correct: true, daysSinceLast: 1 }]);
    const b = bktFilter([{ correct: true, daysSinceLast: 30 }, { correct: true, daysSinceLast: 30 }]);
    expect(b.mastery).toBeLessThan(a.mastery);
    const obs = [{ correct: true, daysSinceLast: 1 }, { correct: false, daysSinceLast: 2 }];
    expect(bktFilter(obs)).toEqual(bktFilter(obs));
  });
});
describe('forget', () => {
  it('间隔 0 不衰减；长间隔趋近先验', () => {
    expect(forget(0.8, 0, 0.15)).toBe(0.8);
    expect(forget(0.8, 1000, 0.15)).toBeLessThan(0.3);
  });
});
describe('fitByEM', () => {
  it('参数范围合法', () => {
    const obs = Array.from({ length: 30 }, () => ({ correct: true, daysSinceLast: 1 }));
    const p = fitByEM(obs);
    expect(p.pL0).toBeGreaterThanOrEqual(0.05);
    expect(p.pL0).toBeLessThanOrEqual(0.6);
  });
});
```

- [ ] **Step 3: 接入 runDiagnosis**

修改 `server/src/modules/student/student.service.ts`：
1. `import { bktFilter, fitByEM, DEFAULT_PARAMS } from '../diagnosis/bkt';`
2. 在 `runDiagnosis` 中：按知识点分组作答记录（按时间升序）→ 构造 `BktObs[]`（daysSinceLast 由相邻 answeredAt 差值计算）→ `params = obs.length >= 15 ? fitByEM(obs) : DEFAULT_PARAMS` → `bktFilter(obs, params)` → 用 `mastery/confidence` 替换原正确率/启发式置信度；`mastery_snapshots` 写入 `d.mastery/100`、`d.confidence`。

- [ ] **Step 4: 运行测试**

Run: `npm test` → 全部通过

- [ ] **Step 5: Commit**

```bash
git add server/src/modules/diagnosis/bkt.ts server/src/modules/diagnosis/__tests__/bkt.test.ts server/src/modules/student/student.service.ts
git commit -m "feat: BKT 贝叶斯知识追踪（前向滤波+遗忘衰减+EM 拟合）替换正确率统计"
```

---

## Task 9: ZPD 学习计划规划器

**Files:**
- Create: `server/src/modules/diagnosis/zpd-planner.ts`
- Create: `server/src/modules/diagnosis/__tests__/zpd.test.ts`
- Modify: `server/src/modules/student/student.service.ts`（`generatePlan` 接入）

- [ ] **Step 1: zpd-planner.ts**

`server/src/modules/diagnosis/zpd-planner.ts`:

```ts
export interface ZpdNode {
  id: number; name: string; parentId: number | null;
  mastery: number; errorCount: number; difficulty: number;
}
export interface ZpdPlanStep {
  knowledgePointId: number;
  stepType: 'review' | 'practice' | 'advance';
  title: string;
  questionCount: number;
}

/** 最近发展区：mastery∈[0.3,0.7] 且父链全部 >=0.6；错题优先→难度低优先 */
export function planZPD(nodes: ZpdNode[], maxSteps = 6): ZpdPlanStep[] {
  const byId = new Map(nodes.map((n) => [n.id, n]));
  const ready = nodes.filter((n) => {
    if (n.mastery < 0.3 || n.mastery > 0.7) return false;
    let cur: ZpdNode | undefined = n;
    while (cur?.parentId != null) {
      cur = byId.get(cur.parentId);
      if (!cur || cur.mastery < 0.6) return false;
    }
    return true;
  });
  ready.sort((a, b) => b.errorCount - a.errorCount || a.difficulty - b.difficulty || a.id - b.id);
  const steps: ZpdPlanStep[] = [];
  for (const n of ready.slice(0, Math.ceil(maxSteps / 3))) {
    steps.push({ knowledgePointId: n.id, stepType: 'review', title: `复习：${n.name}`, questionCount: 3 });
    steps.push({ knowledgePointId: n.id, stepType: 'practice', title: `练习：${n.name}`, questionCount: 5 });
    steps.push({ knowledgePointId: n.id, stepType: 'advance', title: `进阶：${n.name}`, questionCount: 2 });
  }
  return steps.slice(0, maxSteps);
}
```

- [ ] **Step 2: 测试**

`server/src/modules/diagnosis/__tests__/zpd.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { planZPD, type ZpdNode } from '../zpd-planner';

const nodes: ZpdNode[] = [
  { id: 1, name: '识字与写字', parentId: null, mastery: 0.9, errorCount: 0, difficulty: 1 },
  { id: 2, name: '易错多音字', parentId: 1, mastery: 0.4, errorCount: 3, difficulty: 2 },
  { id: 3, name: '形近字辨析', parentId: 1, mastery: 0.55, errorCount: 5, difficulty: 3 },
  { id: 4, name: '阅读与理解', parentId: null, mastery: 0.5, errorCount: 1, difficulty: 2 },
  { id: 5, name: '概括主要内容', parentId: 4, mastery: 0.2, errorCount: 2, difficulty: 3 },
  { id: 6, name: '写作表达', parentId: null, mastery: 0.8, errorCount: 0, difficulty: 4 },
];

describe('planZPD', () => {
  it('只选最近发展区且排除已掌握/前置不足', () => {
    const kpIds = planZPD(nodes).map((s) => s.knowledgePointId);
    expect(kpIds).toContain(2);
    expect(kpIds).toContain(3);
    expect(kpIds).not.toContain(1);
    expect(kpIds).not.toContain(5);
    expect(kpIds).not.toContain(6);
  });
  it('错题多优先，三步型输出', () => {
    const plan = planZPD(nodes, 6);
    expect(plan[0].knowledgePointId).toBe(3);
    expect(plan.map((s) => s.stepType)).toEqual(['review', 'practice', 'advance', 'review', 'practice', 'advance']);
  });
});
```

- [ ] **Step 3: 接入 generatePlan**

`generatePlan` 用最近一次 `mastery_snapshots` + 错题统计构造 `ZpdNode[]`，调用 `planZPD(nodes, 6)` 生成 steps（含弱项兜底：无发展区节点时复习最弱知识点）。参考 V1 计划 Task 10 Step 4 实现。

- [ ] **Step 4: 测试**

Run: `npm test` → 全部通过

- [ ] **Step 5: Commit**

```bash
git add server/src/modules/diagnosis/zpd-planner.ts server/src/modules/diagnosis/__tests__/zpd.test.ts server/src/modules/student/student.service.ts
git commit -m "feat: ZPD 最近发展区规划（依赖 DAG+贪心排序）"
```

---

## Task 10: 苏格拉底状态机 + socraticTurn 工具

**Files:**
- Create: `server/src/modules/agent/socratic-state-machine.ts`
- Create: `server/src/modules/agent/__tests__/socratic.test.ts`
- Modify: `server/src/modules/student/student.service.ts`（新增 `socraticTurn`）

- [ ] **Step 1: 状态机（五阶段 + 答案拦截 + 转人工）**

`server/src/modules/agent/socratic-state-machine.ts`:

```ts
export type SocraticStage = 'read' | 'identify' | 'relate' | 'solve' | 'verify';
export const SOCRATIC_STAGES: SocraticStage[] = ['read', 'identify', 'relate', 'solve', 'verify'];

export interface SocraticState { stage: SocraticStage; stallCount: number; problem: string; progress: boolean; }
export interface SocraticTransition { state: SocraticState; instruction: string; blockedAnswer?: boolean; escalate?: boolean; }

export function createSocraticState(problem: string): SocraticState {
  return { stage: 'read', stallCount: 0, problem, progress: false };
}

export const STAGE_INSTRUCTIONS: Record<SocraticStage, string> = {
  read: '学生刚看到题目。引导他用自己的话说出题目讲了什么、已知条件有哪些。只提问，不给答案。',
  identify: '引导他把已知条件逐条列出来，并说出要求解的目标。每步只问一个问题。',
  relate: '引导他思考条件与目标的关系，提示相关知识点名称（如"分数乘法""多音字"），让他自己建立联系。',
  solve: '引导他说出解题第一步并动手尝试。可以给步骤提示，但绝不直接写出完整答案。',
  verify: '引导他把结果代回原题检查，并总结解题思路。',
};

export function detectFullAnswer(text: string): boolean {
  const clean = text.replace(/\s+/g, '');
  if (/(=\s*[-+]?\d+(\.\d+)?|答案[:：]\s*[-+]?\d)/.test(clean)) return true;
  if (/^(答案|选|应该选)[:：]?\s*[A-Ea-e]$/.test(clean.trim())) return true;
  if (clean.length > 40 && !clean.includes('？') && !clean.includes('?') && !clean.includes('你')) return true;
  return false;
}

export function hasProgress(studentReply: string): boolean {
  const s = studentReply.trim();
  if (!s || s.length < 2) return false;
  return /\d|已知|条件是|因为|所以|先|第一步|我觉得|答案/.test(s);
}

export function transition(state: SocraticState, studentReply: string): SocraticTransition {
  const advanced = hasProgress(studentReply);
  let stage = state.stage;
  let stallCount = state.stallCount;
  if (advanced) {
    const idx = SOCRATIC_STAGES.indexOf(stage);
    stage = idx < SOCRATIC_STAGES.length - 1 ? SOCRATIC_STAGES[idx + 1] : stage;
    stallCount = 0;
  } else {
    stallCount += 1;
    if (stallCount >= 2) {
      const idx = SOCRATIC_STAGES.indexOf(stage);
      stage = idx > 0 ? SOCRATIC_STAGES[idx - 1] : stage;
      stallCount = 0;
    }
  }
  return {
    state: { ...state, stage, stallCount, progress: advanced },
    instruction: STAGE_INSTRUCTIONS[stage],
    escalate: stallCount >= 4,
  };
}
```

- [ ] **Step 2: 测试**

`server/src/modules/agent/__tests__/socratic.test.ts`:（detectFullAnswer 拦截、hasProgress、推进/回退/转人工——参考 V1 计划 Task 9 Step 2 测试代码）

- [ ] **Step 3: student.service.ts 新增 socraticTurn**

```ts
async socraticTurn(problem: string, studentReply: string) {
  const state = createSocraticState(problem);
  const t = transition(state, studentReply);
  // 调 AI 生成引导语（demo 模式规则话术）
  const result = await this.ai.chat(
    `你是苏格拉底式辅导老师。规则：绝不给最终答案。\n${t.instruction}\n【问题】${problem}`,
    studentReply,
  );
  let text = result.text;
  if (detectFullAnswer(text)) {
    text = `我们先不急着下结论。你先说说：${problem} 里有哪些已知条件？它们之间可能有什么关系？`;
  }
  return {
    stage: t.state.stage,
    text,
    escalate: t.escalate,
    refs: [{ title: `辅导阶段：${t.state.stage}`, ref: `socratic:${t.state.stage}` }],
  };
}
```

- [ ] **Step 4: 测试 + 编译**

Run: `npm test; npx tsc --noEmit` → 通过

- [ ] **Step 5: Commit**

```bash
git add server/src/modules/agent/socratic-state-machine.ts server/src/modules/agent/__tests__/socratic.test.ts server/src/modules/student/student.service.ts
git commit -m "feat: 苏格拉底五阶段状态机（多轮推进/回退/拦截答案/转人工）"
```

---

## Task 11: 结果交付与引用校验（finalize 强化）

**Files:**
- Modify: `server/src/modules/agent/agent-graph.ts`（finalize 节点加引用校验）
- Modify: `server/src/modules/agent/agent.service.ts`（done 事件附带校验后的 refs）
- Create: `server/src/modules/agent/__tests__/refs-verify.test.ts`

**设计：** LLM 最终回答中的引用必须通过 `HybridRetriever.verifyRefs` 校验，未命中的引用从交付中剔除并在摘要中提示"引用未通过校验"。这直接满足评审"输出可理解、可操作、可追溯"。

- [ ] **Step 1: 引用校验测试先行**

`server/src/modules/agent/__tests__/refs-verify.test.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import { verifyRefsAndFlag } from '../refs-verify';

const retriever = {
  verifyRefs: vi.fn(async (refs: string[]) =>
    refs.map((ref) => ({ ref, valid: ref === 'chunk:1' })),
  ),
};

describe('verifyRefsAndFlag', () => {
  it('剔除无效引用并标记', async () => {
    const out = await verifyRefsAndFlag(retriever as any, ['chunk:1', 'chunk:999']);
    expect(out.validRefs).toEqual(['chunk:1']);
    expect(out.invalidRefs).toEqual(['chunk:999']);
  });
});
```

- [ ] **Step 2: refs-verify.ts**

`server/src/modules/agent/refs-verify.ts`:

```ts
import type { HybridRetriever } from '../knowledge/hybrid-retriever';

export async function verifyRefsAndFlag(
  retriever: HybridRetriever,
  refs: string[],
): Promise<{ validRefs: string[]; invalidRefs: string[] }> {
  if (!refs.length) return { validRefs: [], invalidRefs: [] };
  const result = await retriever.verifyRefs([...new Set(refs)]);
  const validRefs = result.filter((r) => r.valid).map((r) => r.ref);
  const invalidRefs = result.filter((r) => !r.valid).map((r) => r.ref);
  return { validRefs, invalidRefs };
}
```

- [ ] **Step 3: 接入 finalize（agent-graph.ts）**

`finalizeNode` 增加：接收 `state.refs`，调用外部注入的校验器（通过 state 新增字段 `refVerifier?: (refs: string[]) => Promise<{ validRefs: string[]; invalidRefs: string[] }>`，在 `BuildAgentGraphOptions` 传入），`invalidRefs.length > 0` 时在 finalText 追加提示。

- [ ] **Step 4: agent.service.ts 注入 refVerifier**

构建图时传 `refVerifier: (refs) => verifyRefsAndFlag(this.retriever, refs)`。

- [ ] **Step 5: 测试 + 编译**

Run: `npm test; npx tsc --noEmit` → 通过

- [ ] **Step 6: Commit**

```bash
git add server/src/modules/agent/refs-verify.ts server/src/modules/agent/__tests__/refs-verify.test.ts server/src/modules/agent/agent-graph.ts server/src/modules/agent/agent.service.ts
git commit -m "feat: 结果交付引用校验（防幻觉，无效引用剔除+提示）"
```

---

## Task 12: 演示剧本与全链路自检（复赛提交）

**Files:**
- Create: `docs/Agent能力演示剧本.md`
- Modify: `docs/演示脚本.md`

- [ ] **Step 1: 演示剧本（5 分钟评审版）**

`docs/Agent能力演示剧本.md`（评审视角的完整叙事）：

```
第一幕（0:00–1:00）任务理解 + 流程编排
  工作台输入："帮学生:5 诊断薄弱点，并生成一周 ZPD 学习计划"
  → 观察思考块："已识别意图 diagnose…"（thinking 折叠可展开）
  → 观察工具卡片逐个出现：run_diagnosis → get_error_book → get_study_plan
  → 每个卡片可展开参数/结果/耗时（全链路证据）

第二幕（1:00–2:00）工具调用 + 知识增强
  输入："《草船借箭》讲了什么故事？为什么诸葛亮能成功？"
  → search_knowledge 工具卡片（混合检索：BM25+向量）
  → 最终回答带引用 refs（chunk:xxx），点击验证引用真实存在
  → 追问"那曹操为什么会上当？"（多轮，checkpoint 记忆生效）

第三幕（2:00–3:00）多轮交互 + 苏格拉底
  输入："帮我辅导这道题：x + 3 = 8"
  → socratic_tutor 工具卡片显示阶段推进：read→identify→relate→solve
  → 演示输入"答案就是 5"→ 规则层拦截演示（不直接给答案）

第四幕（3:00–4:00）结果交付 + 追溯
  管理端打开任务历史 → 展开某次运行 → 全链路 trace（思考/工具/参数/结果/耗时）
  → 交付卡片显示"诊断报告：掌握度 60%，薄弱点…建议…"（可操作）
  → 教师看板/家长周报随诊断回流自动更新（数据闭环）

第五幕（4:00–5:00）边界与降级
  无 Key 环境演示 → 规则引擎降级模式（事件流仍完整，工具轨迹真实可查）
  风险提示："诊断结果仅供参考，不替代教师评价"
```

- [ ] **Step 2: 全链路自检清单**

| 检查项 | 验证方式 |
|---|---|
| 任务理解 | 5 类任务意图标签正确 |
| 流程编排 | 工具调用顺序合理、依赖正确 |
| 工具调用 | 卡片展示参数/结果/耗时/状态 |
| 知识增强 | 回答带引用且通过 verifyRefs |
| 多轮交互 | 追问上下文保持（checkpoint） |
| 结果交付 | 交付卡片可操作、可追溯 |
| 可复现 | 同一输入两次结果一致 |
| 失败处理 | 工具失败→needs_human；LLM 挂→规则降级 |
| 合规 | 演示数据角标、周报授权说明、不替代教师评价 |

- [ ] **Step 3: 录制演示视频（OBS ≤5 分钟）** → `docs/演示视频/`

- [ ] **Step 4: 复赛材料打包**（README/部署说明/数据合规说明更新）

- [ ] **Step 5: Commit**

```bash
git add docs/Agent能力演示剧本.md docs/演示脚本.md README.md
git commit -m "docs: Agent 演示剧本与复赛自检清单"
```

---

## 自检记录

- **Spec 覆盖**：六项评审能力逐项有任务；"像 opencode 一样展示思考与工具调用"→ Task 6/7（SSE 事件+步骤流 UI）；"知识增强优秀级"→ Task 2（embedding+BM25+RRF+引用校验）；"精细编排"→ Task 5（ReAct 循环+意图识别+checkpoint）；"允许增删功能"→ 全案以 Agent 工作台为核心，四端功能降级为工具与数据视图。
- **占位扫描**：全部步骤含完整代码与预期输出，无 TBD。
- **类型一致性**：`AgentEvent` 联合类型在 graph/service/controller/前端四处一致；`ToolBindings` 16 项与注册表一致；`AgentRun/AgentMessage` 字段跨文件一致。
- **风险提示**：① LangGraph.js API 若与示例有差异（`createReactAgent` 依赖包版本），以官方文档等价替换；② `checkpoint-sqlite` 需原生依赖（better-sqlite3 已具备），失败时自动降级（已 try/catch）；③ `student.socraticTurn`、`org.classOverview` 等包装方法按 tsc 报错补齐即可。
