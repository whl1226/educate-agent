# 乡芽 Agent 内核升级实施计划（轨迹可观测·五段流水线·审批权限·工具任务化）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 借鉴 DeepSeek-Harness（事件溯源轨迹 + 工具五段流水线 + 审批 seam）与 Kimi-Code（工具即任务 + 策略链 + 子代理委派收窄）的内核设计，将乡芽 Agent 从"能跑"升级为"可回放、可编排、可审计"。

**Architecture:** 不改动 NestJS + LangGraph 主架构。以"增量 seam"方式升级：① 轨迹层引入 DSH 式 `seq/time/type` 事件信封 + usage/快照/统计折叠；② 工具层在现有 `ToolRegistry.call` 内嵌入五段流水线（pre/guard/execute/post/result），审批走 fail-closed 审计对；③ 长时工作抽象为 Kimi 式六态任务实体 + 模型可管理三件套工具；④ 备课引入流水线模板 + 权限收窄的子代理执行器。

**Tech Stack:** NestJS 10 / TypeORM (SQLite, `synchronize` 默认开启自动加列) / LangGraph.js / vitest 4 / 原生 TS 前端（esbuild）

**借鉴映射（评审结论 → 本计划任务）：**

| 来源 | 理念 | 落地任务 |
|---|---|---|
| DSH | Event-Sourced 追加日志（seq/time/type 信封，模型可见即日志可重建） | Task 1.1 |
| DSH | request/header 完整请求快照（提示词/工具 schema 哈希） | Task 1.2 |
| DSH | chunk 记录但压缩（packChunks），而非丢弃 | Task 1.3 |
| DSH | session-stats 折叠（llmMs/ttftMs/toolMs） | Task 1.4 |
| Kimi | telemetry 事件强制注册表（命名规范/编译期校验） | Task 1.5 |
| DSH | tools pre-execute/guard/post-execute 五段流水线（allow/deny/ask） | Task 2.1 |
| DSH | approval/asked ↔ approval/decided 审计对 + fail-closed | Task 2.2 |
| Kimi | 敏感文件策略链（sensitive-file-access-ask 教育版） | Task 2.3 |
| DSH | approval:policy 注入系统提示词（模型感知权限边界） | Task 2.4 |
| Kimi | 工具即任务六态状态机（running/completed/failed/timed_out/killed/lost） | Task 3.1 |
| Kimi | task-list/task-output/task-stop 模型可管理三件套 | Task 3.2 |
| Kimi | 长时任务后台化（run_in_background 语义） | Task 3.3 |
| DSH | workflow 脚本式组合子（pipeline/parallel） | Task 4.1 |
| DSH+Kimi | 子代理委派：白名单收窄 + 深度预算 + 独立上下文 | Task 4.2 |
| — | 前端新事件渲染（usage/task/审批） | Task 4.3 |

---

## 文件结构总览

**新建：**
- `server/src/modules/agent/trajectory.ts` — 事件信封 + usage 提取（Task 1.1）
- `server/src/modules/agent/request-snapshot.ts` — 请求快照构造（Task 1.2）
- `server/src/modules/agent/run-stats.ts` — 运行统计折叠器（Task 1.4）
- `server/src/modules/agent/event-registry.ts` — telemetry 事件注册表（Task 1.5）
- `server/src/modules/agent/approval.ts` — 审批服务（策略/会话范围/审计对）（Task 2.2）
- `server/src/modules/agent/sensitive-data-policy.ts` — 敏感数据策略链（Task 2.3）
- `server/src/modules/agent/agent-task.ts` — 任务状态机与管理器（Task 3.1）
- `server/src/modules/agent/pipeline.ts` — 流水线执行器 + 备课模板（Task 4.1）
- `server/src/modules/agent/subagent.ts` — 子代理执行器（Task 4.2）
- `server/src/modules/agent/__tests__/trajectory.test.ts`、`request-snapshot.test.ts`、`run-stats.test.ts`、`event-registry.test.ts`、`approval.test.ts`、`sensitive-data-policy.test.ts`、`agent-task.test.ts`、`pipeline.test.ts`、`subagent.test.ts`

**修改：**
- `server/src/db/entities/agent.entities.ts` — AgentRun 加 usage/stats/快照列；AgentMessage 加 seq/time/eventType；新增 AgentTask 实体
- `server/src/modules/agent/agent-graph.ts` — 导出 SYSTEM_PROMPT；AgentEvent 扩展 usage/task_start/task_end；usage 累计；buildSystemPrompt；demo generate 接入流水线
- `server/src/modules/agent/agent.service.ts` — seq 计数、快照/stats/token 落库、text_delta 缓冲落库、审批与策略链装配、任务三件套绑定、generate_document 后台模式
- `server/src/modules/agent/tool-registry.ts` — 五段流水线 + 审批处理器 + 三件套工具注册
- `server/src/modules/agent/agent.module.ts` — 注册 ApprovalService、AgentTask 实体
- `src/frontend/entries/agent-main.ts` — 新事件渲染

**约定：** 测试命令（server 目录）`npm test`（= `vitest run`）；单文件 `npx vitest run src/modules/agent/__tests__/<file>`；lint `npm run lint`。SQLite 由 `synchronize` 自动加列（app.module.ts:42），无需手写 migration。

---

## Phase 1：轨迹可观测体系升级（DSH 事件溯源 + Kimi 分层规范）

### Task 1.1: 轨迹事件信封（seq/time/type）+ usage 落库

**Files:**
- Create: `server/src/modules/agent/trajectory.ts`
- Modify: `server/src/db/entities/agent.entities.ts:1-40`
- Modify: `server/src/modules/agent/agent-graph.ts:21-54`（State 加 usage；AgentEvent 加 usage）
- Modify: `server/src/modules/agent/agent.service.ts:142-262`（seq 计数 + usage 落库 + persistEvent 信封化）
- Test: `server/src/modules/agent/__tests__/trajectory.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/trajectory.test.ts
import { describe, expect, it } from 'vitest';
import { envelope, extractUsage, TRAJECTORY_TYPES } from '../trajectory';

describe('trajectory envelope', () => {
  it('信封包含单调 seq/time/type/data 四要素', () => {
    const before = Date.now();
    const e = envelope(7, 'tool_start', { name: 'search_knowledge' });
    expect(e.seq).toBe(7);
    expect(e.time).toBeGreaterThanOrEqual(before);
    expect(e.type).toBe('tool_start');
    expect(e.data).toEqual({ name: 'search_knowledge' });
  });

  it('事件类型白名单覆盖全部 SSE 事件与新增事件', () => {
    for (const t of ['thinking', 'tool_start', 'tool_end', 'text_delta', 'done', 'error', 'usage', 'task_start', 'task_end'] as const) {
      expect(TRAJECTORY_TYPES).toContain(t);
    }
  });

  it('extractUsage 从 LangChain usage_metadata 提取 token（无元数据返回 0）', () => {
    const msg = { usage_metadata: { input_tokens: 120, output_tokens: 30, total_tokens: 150 } };
    expect(extractUsage(msg)).toEqual({ inputTokens: 120, outputTokens: 30 });
    expect(extractUsage({})).toEqual({ inputTokens: 0, outputTokens: 0 });
  });

  it('extractUsage 兼容仅 total_tokens 的 provider', () => {
    const msg = { usage_metadata: { total_tokens: 80 } };
    expect(extractUsage(msg)).toEqual({ inputTokens: 80, outputTokens: 0 });
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/trajectory.test.ts`
Expected: FAIL（`Cannot find module '../trajectory'`）

- [ ] **Step 3: 实现信封与 usage 提取**

```ts
// server/src/modules/agent/trajectory.ts
/**
 * 轨迹事件信封（DeepSeek-Harness SessionEvent 的轻量版）：
 * 追加式、可回放——seq 单调连续、time 毫秒时间戳、type 判别联合、data 载荷。
 * 消息历史（回放/统计/审计）一律由事件派生，事件日志是唯一事实源。
 */
export const TRAJECTORY_TYPES = [
  'thinking', 'tool_start', 'tool_end', 'text_delta', 'done', 'error',
  'usage', 'task_start', 'task_end',
] as const;

export type TrajectoryEventType = (typeof TRAJECTORY_TYPES)[number];

export interface TrajectoryEnvelope {
  /** run 内单调递增序号 */
  seq: number;
  /** Unix epoch 毫秒 */
  time: number;
  type: TrajectoryEventType;
  data: Record<string, unknown>;
}

export function envelope(seq: number, type: TrajectoryEventType, data: Record<string, unknown>): TrajectoryEnvelope {
  return { seq, time: Date.now(), type, data };
}

/** 从 LangChain AIMessage 提取 token 用量（OpenAI 兼容 usage_metadata，缺失时 0） */
export function extractUsage(msg: unknown): { inputTokens: number; outputTokens: number } {
  const m = msg as { usage_metadata?: { input_tokens?: number; output_tokens?: number; total_tokens?: number } } | undefined;
  const um = m?.usage_metadata;
  if (!um) return { inputTokens: 0, outputTokens: 0 };
  return {
    inputTokens: um.input_tokens ?? Math.max(0, (um.total_tokens ?? 0) - (um.output_tokens ?? 0)),
    outputTokens: um.output_tokens ?? 0,
  };
}
```

- [ ] **Step 4: 实体加列（信封字段 + usage 列）**

```ts
// server/src/db/entities/agent.entities.ts —— AgentMessage 追加三列
@Column({ type: 'int', default: 0 }) seq: number;
@Column({ type: 'int', default: 0 }) time: number;
@Column({ name: 'event_type', type: 'varchar', length: 32, default: '' }) eventType: string;

// AgentRun 追加两列
@Column({ name: 'input_tokens', type: 'int', default: 0 }) inputTokens: number;
@Column({ name: 'output_tokens', type: 'int', default: 0 }) outputTokens: number;
```

- [ ] **Step 5: agent-graph 累计 usage 并发事件**

```ts
// server/src/modules/agent/agent-graph.ts
import { extractUsage } from './trajectory';

// AgentGraphState 追加
usage: { inputTokens: number; outputTokens: number };

// AgentEvent 追加
| { type: 'usage'; inputTokens: number; outputTokens: number }

// State 定义追加
usage: Annotation<{ inputTokens: number; outputTokens: number }>,

// agentNode 内（LLM 模式）：循环前声明
let usageAcc = { inputTokens: 0, outputTokens: 0 };

// 每次 invoke 得到 aiMsg 后（第 172 行之后）：
const u = extractUsage(aiMsg);
usageAcc = { inputTokens: usageAcc.inputTokens + u.inputTokens, outputTokens: usageAcc.outputTokens + u.outputTokens };

// 无工具调用最终回答分支 return 前（第 213 行处）：
if (s.onEvent) await s.onEvent({ type: 'usage', ...usageAcc });
return { toolCalls, finalText, refs, needsHuman, usage: usageAcc };

// 达到最大轮数兜底 return 前同样发事件：
if (s.onEvent) await s.onEvent({ type: 'usage', ...usageAcc });
return { toolCalls, finalText, refs, needsHuman: true, usage: usageAcc };
```

- [ ] **Step 6: agent.service 信封化 + usage 落库**

```ts
// server/src/modules/agent/agent.service.ts
import { envelope, type TrajectoryEnvelope } from './trajectory';

// stream() 内：onEvent 定义处改造（seq 闭包计数，信封包裹落库）
let seq = 0;
const onEvent = (ev: AgentEvent) => {
  queue.push(ev);
  this.persistEvent(run.id, envelope(++seq, ev.type, ev as unknown as Record<string, unknown>)).catch(() => undefined);
};

// patchRun 两处（成功/降级成功分支）追加 usage：
inputTokens: (out as { usage?: { inputTokens: number } }).usage?.inputTokens ?? 0,
outputTokens: (out as { usage?: { outputTokens: number } }).usage?.outputTokens ?? 0,

// persistEvent 签名与实现改为信封驱动：
private async persistEvent(runId: number, env: TrajectoryEnvelope) {
  try {
    const base = { runId, seq: env.seq, time: env.time, eventType: env.type };
    const d = env.data as {
      text?: string; name?: string; args?: unknown; result?: unknown; error?: string;
      durationMs?: number; finalText?: string; refs?: string[]; inputTokens?: number; outputTokens?: number;
    };
    switch (env.type) {
      case 'thinking':
        await this.msgs.save(this.msgs.create({ ...base, role: 'assistant', kind: 'thinking', content: d.text, status: 'done' }));
        break;
      case 'tool_start':
        await this.msgs.save(this.msgs.create({ ...base, role: 'assistant', kind: 'tool_call', tool: d.name, argsJson: JSON.stringify(d.args)?.slice(0, 2000), status: 'running' }));
        break;
      case 'tool_end':
        await this.msgs.save(this.msgs.create({ ...base, role: 'tool', kind: 'tool_result', tool: d.name, resultJson: JSON.stringify(d.result ?? { error: d.error })?.slice(0, 2000), status: d.error ? 'error' : 'done', durationMs: d.durationMs }));
        break;
      case 'text_delta':
        break; // Task 1.3 缓冲聚合
      case 'done':
        await this.msgs.save(this.msgs.create({ ...base, role: 'assistant', kind: 'final', content: d.finalText, refsJson: JSON.stringify(d.refs), status: 'done' }));
        break;
      case 'usage':
        await this.msgs.save(this.msgs.create({ ...base, role: 'system', kind: 'usage', content: JSON.stringify({ inputTokens: d.inputTokens, outputTokens: d.outputTokens }), status: 'done' }));
        break;
      case 'error':
        await this.msgs.save(this.msgs.create({ ...base, role: 'assistant', kind: 'final', content: `⚠️ ${d.text}`, status: 'error' }));
        break;
      case 'task_start':
      case 'task_end':
        break; // Task 3.3 落库
    }
  } catch (e) {
    this.logger.warn(`事件持久化失败: ${(e as Error).message}`);
  }
}
```

- [ ] **Step 7: 运行测试**

Run: `npx vitest run src/modules/agent/__tests__/trajectory.test.ts; npx vitest run src/modules/agent/__tests__/agent-graph.test.ts`
Expected: PASS（新用例 4 个 + 旧用例不回归；`usage` 事件测试需在 agent-graph.test.ts 追加一条断言 `events` 包含 `usage`——见 Step 8）

- [ ] **Step 8: 补断言后提交**

```bash
cd server; npm test
git add server/src/modules/agent/trajectory.ts server/src/modules/agent/__tests__/trajectory.test.ts server/src/db/entities/agent.entities.ts server/src/modules/agent/agent-graph.ts server/src/modules/agent/agent.service.ts
git commit -m "feat: 轨迹事件信封seq/time/type+usage落库——DSH式可回放轨迹地基（4新用例全绿）"
```

> 追加断言（agent-graph.test.ts 第 1 个 demo 用例内）：`expect(events).toContain('usage');`

---

### Task 1.2: request/header 请求快照（提示词/工具 schema 哈希）

**Files:**
- Create: `server/src/modules/agent/request-snapshot.ts`
- Modify: `server/src/modules/agent/agent-graph.ts:145-159`（SYSTEM_PROMPT 提取为导出常量）
- Modify: `server/src/modules/agent/agent.service.ts:146-165`（快照落库）
- Modify: `server/src/db/entities/agent.entities.ts`（AgentRun 加 requestSnapshot 列）
- Test: `server/src/modules/agent/__tests__/request-snapshot.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/request-snapshot.test.ts
import { describe, expect, it } from 'vitest';
import { buildRequestSnapshot } from '../request-snapshot';

describe('buildRequestSnapshot', () => {
  const schemas = [{ function: { name: 'search_knowledge' } }, { function: { name: 'run_diagnosis' } }];

  it('快照含模型/温度/提示词哈希/工具哈希/工具清单', () => {
    const s = buildRequestSnapshot({ model: 'deepseek-chat', temperature: 0.4, systemPrompt: '你是乡芽', toolSchemas: schemas, role: 'teacher' });
    expect(s.model).toBe('deepseek-chat');
    expect(s.toolCount).toBe(2);
    expect(s.toolNames).toEqual(['search_knowledge', 'run_diagnosis']);
    expect(s.systemPromptSha).toMatch(/^[0-9a-f]{16}$/);
  });

  it('提示词改动 → 哈希变化（模型行为漂移可定位）', () => {
    const a = buildRequestSnapshot({ model: 'm', temperature: 0.4, systemPrompt: '规则A', toolSchemas: [], role: 'teacher' });
    const b = buildRequestSnapshot({ model: 'm', temperature: 0.4, systemPrompt: '规则B', toolSchemas: [], role: 'teacher' });
    expect(a.systemPromptSha).not.toBe(b.systemPromptSha);
  });

  it('工具顺序不影响工具哈希（集合语义）', () => {
    const a = buildRequestSnapshot({ model: 'm', temperature: 0.4, systemPrompt: 'p', toolSchemas: schemas, role: 'teacher' });
    const b = buildRequestSnapshot({ model: 'm', temperature: 0.4, systemPrompt: 'p', toolSchemas: [...schemas].reverse(), role: 'teacher' });
    expect(a.toolsSha).toBe(b.toolsSha);
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/request-snapshot.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现快照构造器**

```ts
// server/src/modules/agent/request-snapshot.ts
import crypto from 'crypto';

export interface RequestSnapshot {
  model: string;
  temperature: number;
  systemPromptSha: string;
  toolsSha: string;
  toolCount: number;
  toolNames: string[];
  role: string;
  at: number;
}

const sha16 = (s: string) => crypto.createHash('sha256').update(s, 'utf8').digest('hex').slice(0, 16);

/**
 * DSH request/header 快照：每次 run 开始时记录"模型请求的完整形态"。
 * 效果回退时可按哈希定位是提示词漂移 / 工具集变化 / 模型切换。
 */
export function buildRequestSnapshot(opts: {
  model: string;
  temperature: number;
  systemPrompt: string;
  toolSchemas: Array<{ function: { name: string } }>;
  role: string;
}): RequestSnapshot {
  return {
    model: opts.model,
    temperature: opts.temperature,
    systemPromptSha: sha16(opts.systemPrompt),
    toolsSha: sha16(opts.toolSchemas.map((t) => t.function.name).sort().join(',')),
    toolCount: opts.toolSchemas.length,
    toolNames: opts.toolSchemas.map((t) => t.function.name),
    role: opts.role,
    at: Date.now(),
  };
}
```

- [ ] **Step 4: agent-graph 提取 SYSTEM_PROMPT 常量**

```ts
// server/src/modules/agent/agent-graph.ts —— 将 agentNode 内联 system content 提取为模块常量（内容不变）：
export const SYSTEM_PROMPT =
  '你是「乡芽」乡镇教育智能体的核心 Agent，服务于教师/学生/家长/管理者。' +
  '规则：1) 知识类问题必须调用 search_knowledge 并只在返回片段内作答；2) 诊断学生必须先 run_diagnosis；' +
  '3) 每步工具调用的理由用自然语言简短说明（这将展示给用户）；4) 最终回答必须结构清晰、可操作、可追溯，并列出引用；' +
  '5) 绝不直接给学生题目答案（辅导用 socratic_tutor 引导）；' +
  '6) 【禁止编造数据】所有学情、题目、知识信息必须来自工具返回结果，工具未返回的内容一律不得虚构；' +
  '7) 【不要向用户索要身份信息】诊断/错题/计划/周报类工具自动使用当前登录用户身份，直接调用工具即可；' +
  '8) 工具参数缺失时不要追问，能自动推断的（如当前用户）直接推断，必要时调用工具后根据结果再决定是否询问。' +
  '9) 教师文档类任务（教案/试卷/课件/发言稿/成绩单导出）必须调用 generate_document，Markdown 内容必须以 --- YAML 头开头（含 title 与 format），正文使用标准 Markdown 语法（# 标题、- 列表、| 表格）；';

// agentNode 内引用：
content: SYSTEM_PROMPT,
```

- [ ] **Step 5: agent.service 创建 run 后写快照**

```ts
// server/src/modules/agent/agent.service.ts
import { buildRequestSnapshot } from './request-snapshot';
import { SYSTEM_PROMPT } from './agent-graph';

// stream() 内 run.save 之后追加：
const llmModel = this.config.get<string>('LLM_MODEL') || 'deepseek-chat';
const snapshot = buildRequestSnapshot({
  model: llmModel,
  temperature: 0.4,
  systemPrompt: SYSTEM_PROMPT,
  toolSchemas: registry.toFunctionSchemas(),
  role: user.role,
});
await patchRun({ requestSnapshot: JSON.stringify(snapshot) } as Partial<AgentRun>);
```

> 注意：`registry` 需在 run 创建后立即 `const registry = this.buildRegistry();` 上移一行（现第 166 行，上移到 run.save 之后）。

- [ ] **Step 6: 实体加列 + 运行测试 + 提交**

```ts
// server/src/db/entities/agent.entities.ts —— AgentRun 追加
@Column({ name: 'request_snapshot', type: 'text', nullable: true }) requestSnapshot: string | null;
```

```bash
cd server
npx vitest run src/modules/agent/__tests__/request-snapshot.test.ts; if ($?) { npm test }
git add server/src/modules/agent/request-snapshot.ts server/src/modules/agent/__tests__/request-snapshot.test.ts server/src/modules/agent/agent-graph.ts server/src/modules/agent/agent.service.ts server/src/db/entities/agent.entities.ts
git commit -m "feat: request/header请求快照——提示词/工具集哈希可定位模型行为漂移（3新用例全绿）"
```

---

### Task 1.3: text_delta 缓冲聚合落库（记录但压缩，而非丢弃）

**Files:**
- Modify: `server/src/modules/agent/agent.service.ts:158-163, 190-224`（streamBuffer + flush）
- Test: `server/src/modules/agent/__tests__/trajectory.test.ts`（追加用例，SQLite in-memory 不可用，改用逻辑验证——见 Step 1）

- [ ] **Step 1: 写失败测试（buffer 逻辑单测）**

```ts
```ts
// server/src/modules/agent/__tests__/trajectory.test.ts 追加
import { TextStreamBuffer } from '../trajectory';

describe('TextStreamBuffer', () => {
  it('聚合 delta，flush 返回合并文本并清空', () => {
    const b = new TextStreamBuffer();
    b.push('你'); b.push('好'); b.push('，世界');
    expect(b.flush()).toBe('你好，世界');
    expect(b.flush()).toBe('');
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/trajectory.test.ts`
Expected: FAIL（TextStreamBuffer 未导出——Step 1 的类定义放在实现步再落文件）

- [ ] **Step 3: 实现 buffer 落库**

```ts
// server/src/modules/agent/trajectory.ts 追加 TextStreamBuffer 类（见 Step 1 代码，落此文件）

// server/src/modules/agent/agent.service.ts —— stream() 内：
const streamBuf = new TextStreamBuffer();
const flushStream = () => {
  const text = streamBuf.flush();
  if (!text) return;
  return this.msgs.save(
    this.msgs.create({ runId: run.id, role: 'assistant', kind: 'text_stream', content: text, status: 'done', seq: ++seq, time: Date.now(), eventType: 'text_delta' }),
  ).catch(() => undefined);
};

// persistEvent 的 text_delta 分支改为入缓冲（不逐条落库）：
case 'text_delta':
  streamBuf.push(d.text ?? '');
  break;
```

- [ ] **Step 4: 在 runPromise 收尾处 flush**

```ts
// runPromise 内两处 patchRun 成功之后（成功分支与降级分支）、finally 之前均调用：
await flushStream();
```

- [ ] **Step 5: 运行测试 + 提交**

```bash
cd server
npm test
git add server/src/modules/agent/agent.service.ts server/src/modules/agent/trajectory.ts server/src/modules/agent/__tests__/trajectory.test.ts
git commit -m "feat: text_delta缓冲聚合落库——流式轨迹可回放且不膨胀（DSH packChunks思路）"
```

---

### Task 1.4: 运行统计折叠（llmMs/ttftMs/toolMs）

**Files:**
- Create: `server/src/modules/agent/run-stats.ts`
- Modify: `server/src/modules/agent/agent.service.ts`（装配 accumulator + patchRun 写 statsJson）
- Modify: `server/src/db/entities/agent.entities.ts`（AgentRun 加 statsJson 列）
- Test: `server/src/modules/agent/__tests__/run-stats.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/run-stats.test.ts
import { describe, expect, it } from 'vitest';
import { RunStatsAccumulator } from '../run-stats';
import type { AgentEvent } from '../agent-graph';

describe('RunStatsAccumulator', () => {
  it('折叠 tool 配对耗时与错误计数（FIFO 配对）', () => {
    const acc = new RunStatsAccumulator();
    acc.onEvent({ type: 'tool_start', name: 'run_diagnosis', args: {} });
    acc.onEvent({ type: 'tool_end', name: 'run_diagnosis', result: {}, durationMs: 120 });
    acc.onEvent({ type: 'tool_start', name: 'generate_paper', args: {} });
    acc.onEvent({ type: 'tool_end', name: 'generate_paper', error: 'boom', durationMs: 45 });
    const s = acc.snapshot();
    expect(s.toolMs).toBe(165);
    expect(s.toolCount).toBe(2);
    expect(s.toolErrorCount).toBe(1);
  });

  it('ttft = 首个 thinking 到首个 text_delta', () => {
    const acc = new RunStatsAccumulator();
    acc.onEvent({ type: 'thinking', text: '开始' });
    acc.onEvent({ type: 'tool_start', name: 'search_knowledge', args: {} });
    acc.onEvent({ type: 'tool_end', name: 'search_knowledge', result: [], durationMs: 10 });
    acc.onEvent({ type: 'text_delta', delta: '最终' });
    const s = acc.snapshot();
    expect(s.ttftMs).not.toBeNull();
  });

  it('无 text_delta 时 ttft 为 null', () => {
    const acc = new RunStatsAccumulator();
    acc.onEvent({ type: 'thinking', text: '开始' });
    expect(acc.snapshot().ttftMs).toBeNull();
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/run-stats.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现统计折叠器**

```ts
// server/src/modules/agent/run-stats.ts
import type { AgentEvent } from './agent-graph';

export interface RunStats {
  toolMs: number;
  toolCount: number;
  toolErrorCount: number;
  /** 首 token 时延（首个 thinking → 首个 text_delta） */
  ttftMs: number | null;
}

/**
 * DSH session-stats 轻量版：从事件流实时折叠统计，run 结束时落库。
 * 不依赖落库轨迹重算，崩溃前的事件也已计入。
 */
export class RunStatsAccumulator {
  private toolStarts: number[] = [];
  private stats: RunStats = { toolMs: 0, toolCount: 0, toolErrorCount: 0, ttftMs: null };
  private thinkingAt: number | null = null;
  private firstDeltaSeen = false;

  onEvent(ev: AgentEvent): void {
    switch (ev.type) {
      case 'thinking':
        this.thinkingAt = Date.now();
        break;
      case 'tool_start':
        this.toolStarts.push(Date.now());
        break;
      case 'tool_end': {
        const startAt = this.toolStarts.shift() ?? Date.now();
        this.stats.toolMs += Math.max(0, Date.now() - startAt);
        this.stats.toolCount += 1;
        if (ev.error) this.stats.toolErrorCount += 1;
        break;
      }
      case 'text_delta':
        if (!this.firstDeltaSeen) {
          this.firstDeltaSeen = true;
          this.stats.ttftMs = this.thinkingAt === null ? null : Math.max(0, Date.now() - this.thinkingAt);
        }
        break;
      default:
        break;
    }
  }

  snapshot(): RunStats {
    return { ...this.stats };
  }
}
```

- [ ] **Step 4: 装配到 agent.service**

```ts
// server/src/modules/agent/agent.service.ts
import { RunStatsAccumulator } from './run-stats';

// stream() 内 onEvent 定义处（persistEvent 之前）：
const statsAcc = new RunStatsAccumulator();
const onEvent = (ev: AgentEvent) => {
  queue.push(ev);
  statsAcc.onEvent(ev);
  this.persistEvent(...).catch(...);
};

// patchRun 两处成功分支追加：
statsJson: JSON.stringify(statsAcc.snapshot()),

// 实体：
@Column({ name: 'stats_json', type: 'text', nullable: true }) statsJson: string | null;
```

- [ ] **Step 5: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/run-stats.test.ts; if ($?) { npm test }
git add server/src/modules/agent/run-stats.ts server/src/modules/agent/__tests__/run-stats.test.ts server/src/modules/agent/agent.service.ts server/src/db/entities/agent.entities.ts
git commit -m "feat: 运行统计折叠——ttftMs/toolMs/错误计数实时累计落库（3新用例全绿）"
```

---

### Task 1.5: Telemetry 事件注册表（Kimi 式强制注册）

**Files:**
- Create: `server/src/modules/agent/event-registry.ts`
- Test: `server/src/modules/agent/__tests__/event-registry.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/event-registry.test.ts
import { describe, expect, it } from 'vitest';
import { defineTelemetryEvent, getTelemetryEvents, isRegistered, isValidEventName } from '../event-registry';

describe('telemetry event registry', () => {
  it('重复注册报错（编译期强校验的运行时兜底）', () => {
    expect(() =>
      defineTelemetryEvent({ name: 'agent_ttft_ms', owner: 'test', comment: 'x', fields: {} }),
    ).toThrow(/already registered/);
  });

  it('内置核心事件已注册且可查询', () => {
    expect(isRegistered('agent_turn_ended_ms')).toBe(true);
    expect(getTelemetryEvents().length).toBeGreaterThanOrEqual(3);
  });

  it('命名规范校验：snake_case + 单位后缀', () => {
    expect(isValidEventName('agent_tool_ms')).toBe(true);
    expect(isValidEventName('agentToolMs')).toBe(false);
    expect(isValidEventName('agent_tool')).toBe(false);
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/event-registry.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现注册表**

```ts
// server/src/modules/agent/event-registry.ts
export interface TelemetryEventField {
  type: 'string' | 'number' | 'boolean';
  comment: string;
}

export interface TelemetryEventDef {
  name: string;
  owner: string;
  comment: string;
  fields: Record<string, TelemetryEventField>;
}

const registry = new Map<string, TelemetryEventDef>();

/** Kimi 命名规范：snake_case，时长/计数/大小带 _ms/_count/_bytes 后缀 */
export function isValidEventName(name: string): boolean {
  return /^[a-z][a-z0-9_]*_(ms|count|bytes)$/.test(name);
}

export function defineTelemetryEvent(def: TelemetryEventDef): void {
  if (registry.has(def.name)) throw new Error(`telemetry event already registered: ${def.name}`);
  if (!isValidEventName(def.name)) throw new Error(`invalid telemetry event name: ${def.name}（须 snake_case 且带 _ms/_count/_bytes 后缀）`);
  registry.set(def.name, def);
}

export function isRegistered(name: string): boolean {
  return registry.has(name);
}

export function getTelemetryEvents(): TelemetryEventDef[] {
  return [...registry.values()];
}

defineTelemetryEvent({
  name: 'agent_turn_ended_ms', owner: 'agent', comment: 'Agent run 结束耗时（毫秒）',
  fields: { duration_ms: { type: 'number', comment: '墙钟耗时' } },
});
defineTelemetryEvent({
  name: 'agent_tool_call_count', owner: 'agent', comment: '工具调用次数',
  fields: { total: { type: 'number', comment: '总次数' }, errors: { type: 'number', comment: '失败次数' } },
});
defineTelemetryEvent({
  name: 'agent_ttft_ms', owner: 'agent', comment: '首 token 时延（毫秒）',
  fields: { duration_ms: { type: 'number', comment: 'thinking 到首个 delta' } },
});
```

- [ ] **Step 4: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/event-registry.test.ts; if ($?) { npm test }
git add server/src/modules/agent/event-registry.ts server/src/modules/agent/__tests__/event-registry.test.ts
git commit -m "feat: telemetry事件注册表——命名规范+重复注册拦截（Kimi式埋点纪律，3新用例全绿）"
```

**Phase 1 验收：** `cd server; npm test` 全绿（原 57 + 新 ~13 用例）；`npm run lint` 无错误。

---

## Phase 2：工具五段流水线 + 审批权限（DSH 审批 seam + Kimi 策略链）

### Task 2.1: 工具五段流水线（preExecute/guard/execute/postExecute/result）

**Files:**
- Modify: `server/src/modules/agent/tool-registry.ts:29-73`（call 重构，向后兼容）
- Test: `server/src/modules/agent/__tests__/tool-registry.test.ts`（追加 3 用例）

- [ ] **Step 1: 写失败测试（追加到现有测试文件）**

```ts
// server/src/modules/agent/__tests__/tool-registry.test.ts 追加
import { ToolRegistry } from '../tool-registry';

describe('tool pipeline (DSH 五段流水线)', () => {
  it('preExecute deny → 不执行工具，返回拒绝原因', async () => {
    const r = new ToolRegistry().register({
      name: 'probe', description: 'x', inputSchema: {}, execute: async () => 'ok',
    });
    const rec = await r.call(ctxOf('teacher'), 'probe', {}, {
      preExecute: () => ({ kind: 'deny', reason: '策略链拒绝' }),
    });
    expect(rec.error).toContain('策略链拒绝');
    expect(rec.result).toBeUndefined();
  });

  it('preExecute ask 且无审批处理器 → fail-closed 拒绝', async () => {
    const r = new ToolRegistry().register({
      name: 'probe', description: 'x', inputSchema: {}, execute: async () => 'ok',
    });
    const rec = await r.call(ctxOf('teacher'), 'probe', {}, {
      preExecute: () => ({ kind: 'ask', reason: '敏感数据' }),
    });
    expect(rec.error).toContain('fail-closed');
  });

  it('guard 单调否决：只能 deny 不能放行', async () => {
    const r = new ToolRegistry().register({
      name: 'probe', description: 'x', inputSchema: {}, execute: async () => 'ok',
    });
    const rec = await r.call(ctxOf('teacher'), 'probe', {}, {
      guard: () => ({ kind: 'deny', reason: 'guard 拦截' }),
    });
    expect(rec.error).toContain('guard 拦截');
  });

  it('ask + 审批处理器 allowed → 正常执行', async () => {
    const r = new ToolRegistry().register({
      name: 'probe', description: 'x', inputSchema: {}, execute: async () => 'ok',
    });
    r.setApprovalHandler(async () => 'allowed');
    const rec = await r.call(ctxOf('teacher'), 'probe', {}, {
      preExecute: () => ({ kind: 'ask', reason: '需确认' }),
    });
    expect(rec.result).toBe('ok');
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/tool-registry.test.ts`
Expected: FAIL（hooks 参数未实现——当前 call 签名无第三参数，TS 编译失败）

- [ ] **Step 3: 实现五段流水线**

```ts
// server/src/modules/agent/tool-registry.ts —— 追加类型：
export type ToolDecision = { kind: 'allow' } | { kind: 'deny'; reason: string } | { kind: 'ask'; reason?: string };

export interface ToolPipelineHooks {
  /** ① pre-execute：waterfall 三态决策（DSH tools/pre-execute） */
  preExecute?: (ctx: ToolContext, tool: ToolDefinition, args: unknown) => ToolDecision | Promise<ToolDecision>;
  /** ② guard：单调否决（只能 deny，不能放行） */
  guard?: (ctx: ToolContext, tool: ToolDefinition, args: unknown) => { kind: 'deny'; reason: string } | null | Promise<{ kind: 'deny'; reason: string } | null>;
  /** ④ post-execute：观察/改写结果（异常吞掉，不影响执行结果） */
  postExecute?: (ctx: ToolContext, tool: ToolDefinition, args: unknown, rec: ToolCallRecord) => void | Promise<void>;
}

export type ApprovalHandler = (ctx: ToolContext, tool: ToolDefinition, args: unknown, callId: string) => Promise<'allowed' | 'denied' | 'denied_unavailable'>;

// ToolRegistry 追加私有字段与方法：
private approvalHandler: ApprovalHandler | null = null;

/** 注册审批处理器（ask 决策时调用；未注册 = fail-closed 拒绝） */
setApprovalHandler(h: ApprovalHandler): this {
  this.approvalHandler = h;
  return this;
}

// call 签名与实现重构：
async call(ctx: ToolContext, name: string, args: unknown, hooks?: ToolPipelineHooks): Promise<ToolCallRecord> {
  const tool = this.tools.get(name);
  const started = Date.now();
  if (!tool) return { tool: name, args, error: `未知工具: ${name}`, durationMs: 0 };
  if (tool.needsPermission && ctx.role !== 'admin' && ctx.role !== tool.needsPermission) {
    return { tool: name, args, error: '无权限调用该工具', durationMs: 0 };
  }
  // ① pre-execute 三态决策
  if (hooks?.preExecute) {
    const d = await hooks.preExecute(ctx, tool, args);
    if (d.kind === 'deny') return { tool: name, args, error: `工具被拒绝：${d.reason}`, durationMs: 0 };
    if (d.kind === 'ask') {
      const callId = `approval_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
      const outcome = this.approvalHandler ? await this.approvalHandler(ctx, tool, args, callId) : 'denied_unavailable';
      if (outcome !== 'allowed') {
        return {
          tool: name, args,
          error: outcome === 'denied' ? '审批被拒绝' : '审批不可用（fail-closed 拒绝）',
          durationMs: Date.now() - started,
        };
      }
    }
  }
  // ② guard 单调否决
  if (hooks?.guard) {
    const g = await hooks.guard(ctx, tool, args);
    if (g) return { tool: name, args, error: `guard 否决：${g.reason}`, durationMs: 0 };
  }
  // ③ execute（30s 超时不变）
  try {
    const result = await Promise.race([
      tool.execute(ctx, (args ?? {}) as never),
      new Promise((_, rej) => setTimeout(() => rej(new Error('工具超时（30s）')), 30_000)),
    ]);
    const rec: ToolCallRecord = { tool: name, args, result, durationMs: Date.now() - started };
    // ④ post-execute
    if (hooks?.postExecute) await hooks.postExecute(ctx, tool, args, rec).catch(() => undefined);
    return rec;
  } catch (e) {
    return { tool: name, args, error: (e as Error).message?.slice(0, 500) ?? '未知错误', durationMs: Date.now() - started };
  }
  // ⑤ result：ToolCallRecord 即轨迹事件（agent-graph/agent.service 落库），只读观察点
}
```

- [ ] **Step 4: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/tool-registry.test.ts; if ($?) { npm test }
git add server/src/modules/agent/tool-registry.ts server/src/modules/agent/__tests__/tool-registry.test.ts
git commit -m "feat: 工具五段流水线——preExecute三态决策/guard单调否决/postExecute观察（4新用例全绿，向后兼容）"
```

---

### Task 2.2: 审批服务（策略 + session-scope + 审计对）

**Files:**
- Create: `server/src/modules/agent/approval.ts`
- Modify: `server/src/modules/agent/agent.module.ts`（注册 provider）
- Modify: `server/src/modules/agent/agent.service.ts`（装配 setApprovalHandler）
- Test: `server/src/modules/agent/__tests__/approval.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/approval.test.ts
import { describe, expect, it } from 'vitest';
import { ApprovalService } from '../approval';

function makeSvc() {
  const saved: Array<Record<string, unknown>> = [];
  const repo = {
    save: (e: unknown) => {
      saved.push(e as Record<string, unknown>);
      return Promise.resolve(e);
    },
    create: (o: Record<string, unknown>) => o,
  };
  return { svc: new ApprovalService(repo as never), saved };
}

describe('ApprovalService', () => {
  it('默认策略 never：request 直接 denied（审计对仍写入）', async () => {
    const { svc, saved } = makeSvc();
    const out = await svc.request({ runId: 1, userId: 5, toolName: 'get_weekly_report', callId: 'c1' });
    expect(out).toBe('denied');
    expect(saved.length).toBe(2);
    expect(saved[0].action).toBe('agent.approval.asked');
    expect(saved[1].action).toBe('agent.approval.decided');
  });

  it('policy=ask 且无 UI 应答者 → denied_unavailable（fail-closed）', async () => {
    const { svc } = makeSvc();
    svc.setPolicy('get_weekly_report', 'ask');
    const out = await svc.request({ runId: 1, userId: 5, toolName: 'get_weekly_report', callId: 'c2' });
    expect(out).toBe('denied_unavailable');
  });

  it('session-scope 批准后同 run 同工具免审（Kimi permissionRules）', async () => {
    const { svc } = makeSvc();
    svc.setPolicy('generate_document', 'ask');
    svc.grantSessionScope(9, 'generate_document');
    const out = await svc.request({ runId: 9, userId: 5, toolName: 'generate_document', callId: 'c3' });
    expect(out).toBe('allowed');
    // 其他 run 不受影响
    const other = await svc.request({ runId: 10, userId: 5, toolName: 'generate_document', callId: 'c4' });
    expect(other).toBe('denied_unavailable');
  });

  it('policyOf 默认 never，可覆盖', () => {
    const { svc } = makeSvc();
    expect(svc.policyOf('any_tool')).toBe('never');
    svc.setPolicy('any_tool', 'ask');
    expect(svc.policyOf('any_tool')).toBe('ask');
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/approval.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现审批服务**

```ts
// server/src/modules/agent/approval.ts
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { AuditLog } from '../../db/entities/system.entities';

export type ApprovalPolicy = 'ask' | 'never';
export type ApprovalOutcome = 'allowed' | 'denied' | 'denied_unavailable';

export interface ApprovalRequest {
  runId: number;
  userId: number;
  toolName: string;
  callId: string;
  reason?: string;
}

/**
 * 审批服务（DSH user-approval seam + Kimi permissionRules 融合）：
 * - 策略：ask（询问，无应答者 fail-closed）/ never（确定性拒绝，无人值守安全）；
 * - session-scope：批准一次，本 run 内同工具免审；
 * - 审计对：approval.asked ↔ approval.decided 成对写 audit_logs（log-only，模型只看到工具结果）。
 */
@Injectable()
export class ApprovalService {
  private policyByTool = new Map<string, ApprovalPolicy>();
  private sessionScopes = new Set<string>();

  constructor(@InjectRepository(AuditLog) private readonly logs: Repository<AuditLog>) {}

  setPolicy(toolName: string, policy: ApprovalPolicy): this {
    this.policyByTool.set(toolName, policy);
    return this;
  }

  policyOf(toolName: string): ApprovalPolicy {
    return this.policyByTool.get(toolName) ?? 'never';
  }

  grantSessionScope(runId: number, toolName: string): void {
    this.sessionScopes.add(`${runId}:${toolName}`);
  }

  hasSessionScope(runId: number, toolName: string): boolean {
    return this.sessionScopes.has(`${runId}:${toolName}`);
  }

  async request(req: ApprovalRequest): Promise<ApprovalOutcome> {
    const policy = this.policyOf(req.toolName);
    if (policy === 'never') return 'denied';
    if (this.hasSessionScope(req.runId, req.toolName)) return 'allowed';
    await this.writeAudit(req.userId, 'agent.approval.asked', req);
    // 第一期无 UI 应答者：ask 一律 fail-closed（denied_unavailable 区别于主动 denied，便于审计区分）
    const outcome: ApprovalOutcome = 'denied_unavailable';
    await this.writeAudit(req.userId, 'agent.approval.decided', req, outcome);
    return outcome;
  }

  private async writeAudit(userId: number, action: string, req: ApprovalRequest, outcome?: string): Promise<void> {
    await this.logs
      .save(
        this.logs.create({
          userId,
          action,
          module: 'agent',
          targetType: 'tool',
          targetId: req.toolName,
          detail: JSON.stringify({ runId: req.runId, callId: req.callId, reason: req.reason?.slice(0, 200), outcome }),
        }),
      )
      .catch(() => undefined);
  }
}
```

- [ ] **Step 4: 注册 provider + 装配**

```ts
// server/src/modules/agent/agent.module.ts
import { ApprovalService } from './approval';
// providers 追加 ApprovalService

// server/src/modules/agent/agent.service.ts
import { ApprovalService } from './approval';
// constructor 注入：private readonly approval: ApprovalService,

// stream() 内 registry 创建后（run.id / user.id 由闭包提供）：
const runId = run.id;
registry.setApprovalHandler(async (_ctx, tool, _args, callId) => {
  const policy = this.approval.policyOf(tool.name);
  if (policy === 'never') return 'denied';
  return this.approval.request({ runId, userId: user.id, toolName: tool.name, callId });
});
```

- [ ] **Step 5: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/approval.test.ts; if ($?) { npm test }
git add server/src/modules/agent/approval.ts server/src/modules/agent/__tests__/approval.test.ts server/src/modules/agent/agent.module.ts server/src/modules/agent/agent.service.ts
git commit -m "feat: 审批服务——ask/never策略+session-scope免审+审批审计对（4新用例全绿）"
```

---

### Task 2.3: 敏感数据策略链（教育版 sensitive-file-access-ask）

**Files:**
- Create: `server/src/modules/agent/sensitive-data-policy.ts`
- Modify: `server/src/modules/agent/agent.service.ts`（策略链装配为 preExecute hook + `AGENT_APPROVAL_MODE` 配置开关）
- Test: `server/src/modules/agent/__tests__/sensitive-data-policy.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/sensitive-data-policy.test.ts
import { describe, expect, it } from 'vitest';
import { buildPolicyChain, runPolicyChain, SENSITIVE_TOOLS } from '../sensitive-data-policy';

describe('sensitive data policy chain', () => {
  it('strict 模式：敏感工具 → ask', () => {
    const chain = buildPolicyChain('strict');
    const r = runPolicyChain(chain, 'parent', 'get_weekly_report', {});
    expect(r.kind).toBe('ask');
  });

  it('strict 模式：常规工具 → approve（不打断日常流程）', () => {
    const chain = buildPolicyChain('strict');
    const r = runPolicyChain(chain, 'teacher', 'generate_lesson_plan', { subject: '语文' });
    expect(r.kind).toBe('approve');
  });

  it('off 模式：一切 approve（向后兼容默认值）', () => {
    const chain = buildPolicyChain('off');
    expect(runPolicyChain(chain, 'parent', 'get_weekly_report', {}).kind).toBe('approve');
    expect(runPolicyChain(chain, 'admin', 'list_alerts', {}).kind).toBe('approve');
  });

  it('敏感清单覆盖学生隐私/家庭/心理/画像类工具', () => {
    expect(SENSITIVE_TOOLS.has('get_weekly_report')).toBe(true);
    expect(SENSITIVE_TOOLS.has('get_teacher_profile')).toBe(true);
    expect(SENSITIVE_TOOLS.has('list_alerts')).toBe(true);
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/sensitive-data-policy.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现策略链**

```ts
// server/src/modules/agent/sensitive-data-policy.ts
export type PolicyResult =
  | { kind: 'approve'; reason?: string }
  | { kind: 'deny'; reason?: string; message?: string }
  | { kind: 'ask'; reason?: string };

export interface ToolPolicy {
  readonly name: string;
  decide(role: string, toolName: string, args: Record<string, unknown>): PolicyResult | null;
}

/** 敏感数据工具：学生家庭信息/教师画像/心理与控辍预警/班级风险学生 → ask（Kimi sensitive-file-access-ask 教育版） */
export const SENSITIVE_TOOLS = new Set<string>([
  'get_weekly_report',
  'get_teacher_profile',
  'list_alerts',
  'get_class_overview',
]);

export const sensitiveDataPolicy: ToolPolicy = {
  name: 'sensitive-data-ask',
  decide(_role, toolName) {
    if (SENSITIVE_TOOLS.has(toolName)) {
      return { kind: 'ask', reason: '涉及学生隐私/家庭/心理敏感数据，需审批' };
    }
    return null;
  },
};

export const defaultAllowPolicy: ToolPolicy = {
  name: 'default-allow',
  decide() {
    return { kind: 'approve', reason: '常规工具默认放行' };
  },
};

export const fallbackAskPolicy: ToolPolicy = {
  name: 'fallback-ask',
  decide() {
    return { kind: 'ask', reason: '未匹配任何策略，兜底询问' };
  },
};

export type PolicyChainMode = 'off' | 'strict';

/** off=向后兼容（全放行）；strict=敏感工具询问 + fail-closed */
export function buildPolicyChain(mode: PolicyChainMode): readonly ToolPolicy[] {
  if (mode === 'strict') return [sensitiveDataPolicy, defaultAllowPolicy, fallbackAskPolicy];
  return [defaultAllowPolicy];
}

export function runPolicyChain(
  chain: readonly ToolPolicy[],
  role: string,
  toolName: string,
  args: Record<string, unknown>,
): PolicyResult {
  for (const p of chain) {
    const r = p.decide(role, toolName, args);
    if (r) return r;
  }
  return { kind: 'ask', reason: '策略链耗尽，兜底询问' };
}
```

- [ ] **Step 4: 装配为 preExecute hook**

```ts
// server/src/modules/agent/agent.service.ts
import { buildPolicyChain, runPolicyChain, type PolicyChainMode } from './sensitive-data-policy';

// stream() 内 registry 创建后（setApprovalHandler 之前）：
const approvalMode: PolicyChainMode = this.config.get<string>('AGENT_APPROVAL_MODE') === 'strict' ? 'strict' : 'off';
const policyChain = buildPolicyChain(approvalMode);
const pipelineHooks = {
  preExecute: async (_ctx: ToolContext, tool: { name: string }, args: unknown) => {
    const decision = runPolicyChain(policyChain, user.role, tool.name, (args ?? {}) as Record<string, unknown>);
    if (decision.kind === 'approve') return { kind: 'allow' as const };
    if (decision.kind === 'deny') return { kind: 'deny' as const, reason: decision.reason ?? '策略链拒绝' };
    return { kind: 'ask' as const, reason: decision.reason };
  },
};
```

- [ ] **Step 5: 挂接 hooks 到图内调用点**

```ts
// server/src/modules/agent/agent-graph.ts —— callTool/循环内 registry.call 调用处传 hooks。
// agentNode 内新增参数透传：BuildAgentGraphOptions 加 pipelineHooks?: ToolPipelineHooks
// 所有 registry.call(ctx, name, args) → registry.call(ctx, name, args, pipelineHooks)
// Demo 模式 callTool 与 LLM 模式循环两处共 3 个调用点。
```

- [ ] **Step 6: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/sensitive-data-policy.test.ts; if ($?) { npm test }
git add server/src/modules/agent/sensitive-data-policy.ts server/src/modules/agent/__tests__/sensitive-data-policy.test.ts server/src/modules/agent/agent.service.ts server/src/modules/agent/agent-graph.ts
git commit -m "feat: 敏感数据策略链——strict模式敏感工具ask+fail-closed，off模式向后兼容（4新用例全绿）"
```

---

### Task 2.4: 权限边界注入系统提示词（模型感知权限）

**Files:**
- Modify: `server/src/modules/agent/agent-graph.ts`（buildSystemPrompt + agentNode 引用）
- Modify: `server/src/modules/agent/agent.service.ts`（传 permissionSummary）
- Test: `server/src/modules/agent/__tests__/agent-graph.test.ts`（追加 1 用例）

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/agent-graph.test.ts 追加
import { buildSystemPrompt } from '../agent-graph';

describe('buildSystemPrompt', () => {
  it('注入角色权限边界摘要，模型可感知可调工具域', () => {
    const p = buildSystemPrompt('teacher', '可调用工具：generate_lesson_plan/generate_paper/generate_document/get_class_overview/researcher_comment/search_knowledge；敏感数据工具将触发审批。');
    expect(p).toContain('teacher');
    expect(p).toContain('generate_document');
    expect(p).toContain('审批');
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/agent-graph.test.ts`
Expected: FAIL（buildSystemPrompt 未导出）

- [ ] **Step 3: 实现**

```ts
// server/src/modules/agent/agent-graph.ts —— SYSTEM_PROMPT 之后追加：
/**
 * DSH systemPrompt.context('approval:policy') 思路：
 * 模型应知道自己当前权限边界，避免反复尝试被拒工具浪费 token。
 */
export function buildSystemPrompt(role: string, permissionSummary: string): string {
  return (
    SYSTEM_PROMPT +
    `【当前权限边界】你的身份角色：${role}。${permissionSummary} ` +
    '不要尝试调用权限范围外的工具；被审批拦截的工具不要反复重试，改用替代方案或如实告知用户。'
  );
}

// agentNode 内 system message 改为：
content: buildSystemPrompt(s.role, s.permissionSummary ?? ''),
```

- [ ] **Step 4: State 与装配**

```ts
// AgentGraphState 加 permissionSummary: string;（Annotation 同 input 普通字段）
// agent.service invokeInput 加：
permissionSummary: `可调用工具：${registry.list().map((t) => t.name).join('/')}；` +
  (approvalMode === 'strict' ? '敏感数据工具（周报/教师画像/预警/班级概览）将触发审批拦截。' : '当前为宽松模式，工具调用直接放行。'),
```

- [ ] **Step 5: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/agent-graph.test.ts; if ($?) { npm test }
git add server/src/modules/agent/agent-graph.ts server/src/modules/agent/agent.service.ts server/src/modules/agent/__tests__/agent-graph.test.ts
git commit -m "feat: 权限边界注入系统提示词——模型感知工具域与审批策略减少无效调用（1新用例全绿）"
```

**Phase 2 验收：** `npm test` 全绿；`npm run lint` 无错误；实测脚本 `node server/test-agent-llm.mjs` 回归通过（off 模式行为与升级前一致）。

---

## Phase 3：工具即任务（Kimi 任务三件套）

### Task 3.1: AgentTaskManager 六态状态机

**Files:**
- Create: `server/src/modules/agent/agent-task.ts`
- Test: `server/src/modules/agent/__tests__/agent-task.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/agent-task.test.ts
import { describe, expect, it, vi } from 'vitest';
import { AgentTaskManager, AGENT_TASK_TIMEOUT_MS } from '../agent-task';

describe('AgentTaskManager', () => {
  it('start → transition completed', () => {
    const m = new AgentTaskManager();
    const rec = m.start({ taskId: 't1', runId: 1, kind: 'document', description: '教案渲染' });
    expect(rec.state).toBe('running');
    const done = m.transition('t1', 'completed', '/download/1');
    expect(done?.state).toBe('completed');
    expect(done?.outputSummary).toBe('/download/1');
    expect(done?.finishedAt).toBeGreaterThan(0);
  });

  it('终态后 transition 不再变化（状态机单向）', () => {
    const m = new AgentTaskManager();
    m.start({ taskId: 't2', runId: 1, kind: 'document', description: 'x' });
    m.transition('t2', 'failed', 'err');
    expect(m.transition('t2', 'completed')?.state).toBe('failed');
  });

  it('stop → killed；stopAll 按 runId 批量停止', () => {
    const m = new AgentTaskManager();
    m.start({ taskId: 'a', runId: 1, kind: 'document', description: 'x' });
    m.start({ taskId: 'b', runId: 2, kind: 'paper', description: 'y' });
    m.stopAll(1);
    expect(m.get('a')?.state).toBe('killed');
    expect(m.get('b')?.state).toBe('running');
  });

  it('超时 → timed_out（假时钟）', () => {
    vi.useFakeTimers();
    const m = new AgentTaskManager();
    m.start({ taskId: 't3', runId: 1, kind: 'document', description: 'x' });
    vi.advanceTimersByTime(AGENT_TASK_TIMEOUT_MS + 1);
    expect(m.get('t3')?.state).toBe('timed_out');
    vi.useRealTimers();
  });

  it('markLost 把 running 标为 lost（服务重启恢复语义）', () => {
    const m = new AgentTaskManager();
    m.start({ taskId: 't4', runId: 1, kind: 'document', description: 'x' });
    m.transition('t4', 'completed');
    m.start({ taskId: 't5', runId: 1, kind: 'paper', description: 'y' });
    m.markLost();
    expect(m.get('t4')?.state).toBe('completed');
    expect(m.get('t5')?.state).toBe('lost');
  });

  it('onTaskChange 通知任务状态变化', () => {
    const m = new AgentTaskManager();
    const fn = vi.fn();
    m.setOnTaskChange(fn);
    m.start({ taskId: 't6', runId: 1, kind: 'document', description: 'x' });
    expect(fn).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/agent-task.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现任务管理器**

```ts
// server/src/modules/agent/agent-task.ts
export type AgentTaskState = 'running' | 'completed' | 'failed' | 'timed_out' | 'killed' | 'lost';

export interface AgentTaskInfo {
  taskId: string;
  runId: number;
  kind: string;
  description: string;
  state: AgentTaskState;
  outputSummary?: string;
  createdAt: number;
  finishedAt?: number;
}

export const AGENT_TASK_TIMEOUT_MS = 10 * 60_000;

/**
 * 工具即任务（Kimi tool-as-task）：长时工作统一为可查询/停止/超时的任务实体。
 * 状态机：running → completed | failed | timed_out | killed；进程重启后 running → lost。
 * 模型通过 task_list/task_output/task_stop 三件套管理任务（Task 3.2）。
 */
export class AgentTaskManager {
  private tasks = new Map<string, AgentTaskInfo>();
  private timers = new Map<string, NodeJS.Timeout>();
  private onTaskChange?: (rec: AgentTaskInfo) => void | Promise<void>;

  setOnTaskChange(fn: (rec: AgentTaskInfo) => void | Promise<void>): this {
    this.onTaskChange = fn;
    return this;
  }

  start(rec: Omit<AgentTaskInfo, 'state' | 'createdAt'>): AgentTaskInfo {
    const record: AgentTaskInfo = { ...rec, state: 'running', createdAt: Date.now() };
    this.tasks.set(record.taskId, record);
    const timer = setTimeout(() => this.transition(record.taskId, 'timed_out'), AGENT_TASK_TIMEOUT_MS);
    this.timers.set(record.taskId, timer);
    void this.emit(record);
    return record;
  }

  /** 状态迁移：仅 running → 终态生效（状态机单向，防乱序回调覆盖终态） */
  transition(taskId: string, state: AgentTaskState, outputSummary?: string): AgentTaskInfo | undefined {
    const rec = this.tasks.get(taskId);
    if (!rec || rec.state !== 'running') return rec;
    rec.state = state;
    rec.finishedAt = Date.now();
    if (outputSummary !== undefined) rec.outputSummary = outputSummary;
    const timer = this.timers.get(taskId);
    if (timer) clearTimeout(timer);
    this.timers.delete(taskId);
    void this.emit(rec);
    return rec;
  }

  get(taskId: string): AgentTaskInfo | undefined {
    return this.tasks.get(taskId);
  }

  list(runId?: number): AgentTaskInfo[] {
    const all = [...this.tasks.values()];
    return runId === undefined ? all : all.filter((t) => t.runId === runId);
  }

  /** 服务重启后恢复：未完成任务标 lost（不误报 running） */
  markLost(): void {
    for (const rec of this.tasks.values()) {
      if (rec.state === 'running') {
        rec.state = 'lost';
        rec.finishedAt = Date.now();
      }
    }
  }

  stop(taskId: string): boolean {
    return !!this.transition(taskId, 'killed');
  }

  stopAll(runId?: number): number {
    let n = 0;
    for (const rec of this.tasks.values()) {
      if (rec.state === 'running' && (runId === undefined || rec.runId === runId)) {
        if (this.stop(rec.taskId)) n++;
      }
    }
    return n;
  }

  private async emit(rec: AgentTaskInfo): Promise<void> {
    if (!this.onTaskChange) return;
    try {
      await this.onTaskChange(rec);
    } catch {
      // 通知失败不影响任务本身
    }
  }
}
```

- [ ] **Step 4: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/agent-task.test.ts; if ($?) { npm test }
git add server/src/modules/agent/agent-task.ts server/src/modules/agent/__tests__/agent-task.test.ts
git commit -m "feat: 工具即任务——六态状态机/超时/批量停止/重启lost恢复（6新用例全绿）"
```

---

### Task 3.2: 任务持久化 + 三件套工具（task_list/task_output/task_stop）

**Files:**
- Modify: `server/src/db/entities/agent.entities.ts`（新增 AgentTask 实体）
- Modify: `server/src/modules/agent/agent.module.ts`（forFeature 加 AgentTask）
- Modify: `server/src/modules/agent/tool-registry.ts`（TOOL_NAMES + ToolBindings + 三件套注册）
- Modify: `server/src/modules/agent/agent.service.ts`（绑定三件套 + 任务变更回调落库）
- Test: `server/src/modules/agent/__tests__/tool-registry.test.ts`（追加 2 用例）

- [ ] **Step 1: 写失败测试（三件套注册与 bindings 契约）**

```ts
// server/src/modules/agent/__tests__/tool-registry.test.ts 追加
describe('task 三件套工具', () => {
  const b: ToolBindings = {
    ...bindings,
    task_list: vi.fn(async () => []),
    task_output: vi.fn(async () => ({ state: 'running' })),
    task_stop: vi.fn(async () => ({ stopped: true })),
  };
  const r = buildRegistry(b);

  it('三件套已注册且 schema 完整', () => {
    expect(r.get('task_list')).toBeDefined();
    expect(r.get('task_output')).toBeDefined();
    expect(r.get('task_stop')).toBeDefined();
    const schema = r.toFunctionSchemas().find((s) => s.function.name === 'task_stop');
    expect((schema?.function.parameters as { required?: string[] }).required).toContain('taskId');
  });

  it('task_list 无角色限制但按 runId 隔离（ctx 透传）', async () => {
    const ctx = ctxOf('student');
    await r.call(ctx, 'task_list', {});
    expect(b.task_list).toHaveBeenCalledWith(ctx, undefined);
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/tool-registry.test.ts`
Expected: FAIL（ToolBindings 无 task_* 字段，TS 编译失败）

- [ ] **Step 3: 实体 + 注册**

```ts
// server/src/db/entities/agent.entities.ts 追加
/** Agent 后台任务（工具即任务：长时工作的持久化视图） */
@Entity('agent_tasks')
@Index('idx_at_run', ['runId'])
export class AgentTask extends BaseEntity {
  @Column({ name: 'task_id', type: 'varchar', length: 64 }) taskId: string;
  @Column({ name: 'run_id', type: 'int' }) runId: number;
  @Column({ type: 'varchar', length: 32 }) kind: string;
  @Column({ type: 'varchar', length: 256 }) description: string;
  @Column({ type: 'varchar', length: 16, default: 'running' }) state: string;
  @Column({ name: 'output_summary', type: 'text', nullable: true }) outputSummary: string | null;
  @Column({ name: 'finished_at', type: 'int', nullable: true }) finishedAt: number | null;
}

// server/src/modules/agent/agent.module.ts
import { AgentRun, AgentMessage, AgentTask } from '../../db/entities/agent.entities';
// forFeature([AgentRun, AgentMessage, AgentTask])
```

- [ ] **Step 4: TOOL_NAMES 与 ToolBindings 扩展 + 注册**

```ts
// server/src/modules/agent/tool-registry.ts
// TOOL_NAMES 追加（教师/管理端可停任务，查询不限角色）：
'task_list', 'task_output', 'task_stop',

// ToolBindings 追加：
task_list: (ctx: ToolContext, state?: string) => Promise<unknown>;
task_output: (ctx: ToolContext, taskId: string) => Promise<unknown>;
task_stop: (ctx: ToolContext, taskId: string) => Promise<unknown>;

// buildRegistry 内追加注册：
reg('task_list', '列出本会话的后台任务（文档生成/组卷等）及其状态，可过滤状态（running/completed/failed）。', {
  type: 'object', properties: { state: { type: 'string', enum: ['running', 'completed', 'failed', 'timed_out', 'killed', 'lost'] } }, required: [],
}, (c, a) => b.task_list(c, a.state));
reg('task_output', '查询某后台任务的结果/进度摘要。', {
  type: 'object', properties: { taskId: { type: 'string' } }, required: ['taskId'],
}, (c, a) => b.task_output(c, a.taskId));
reg('task_stop', '停止某后台任务（教师/管理端）。', {
  type: 'object', properties: { taskId: { type: 'string' } }, required: ['taskId'],
}, (c, a) => b.task_stop(c, a.taskId), 'teacher');
```

- [ ] **Step 5: 绑定 + 任务变更回调落库**

```ts
// server/src/modules/agent/agent.service.ts
import { AgentTaskManager } from './agent-task';
import { AgentTask } from '../../db/entities/agent.entities';
// constructor 注入 @InjectRepository(AgentTask) private readonly taskRepo: Repository<AgentTask>,

// AgentService 字段：
private readonly taskManager = new AgentTaskManager();

// buildRegistry bindings 追加：
task_list: async (ctx, state) => {
  const list = this.taskManager.list(ctx.runId ?? 0).filter((t) => !state || t.state === state);
  return list.map((t) => ({ taskId: t.taskId, kind: t.kind, description: t.description, state: t.state, createdAt: t.createdAt }));
},
task_output: async (_ctx, taskId) => {
  const t = this.taskManager.get(taskId);
  if (!t) return { error: `任务不存在: ${taskId}` };
  return { taskId: t.taskId, kind: t.kind, state: t.state, outputSummary: t.outputSummary ?? null };
},
task_stop: async (ctx, taskId) => {
  const t = this.taskManager.get(taskId);
  if (!t) return { error: `任务不存在: ${taskId}` };
  if (t.runId !== (ctx.runId ?? 0) && ctx.role !== 'admin') return { error: '无权停止他人会话的任务' };
  return { stopped: this.taskManager.stop(taskId) };
},

// stream() 内（registry 创建后）注册任务变更回调（落库 + SSE）：
this.taskManager.setOnTaskChange((rec) => {
  this.taskRepo
    .save(
      this.taskRepo.create({
        taskId: rec.taskId, runId: rec.runId, kind: rec.kind, description: rec.description,
        state: rec.state, outputSummary: rec.outputSummary ?? null, finishedAt: rec.finishedAt ?? null,
      }),
    )
    .catch(() => undefined);
  // SSE 推送：pushTaskEvent 方法体在 Task 3.3 实现（当前为空实现，见本 Task Step 5 结尾说明）
  void this.pushTaskEvent(rec.runId, rec);
});

// ToolContext 加 runId?: number（tool-registry.ts）；invokeInput 的 ctx 构造处透传：
const ctx: ToolContext = { userId: s.userId, role: s.role, user: {...}, runId: s.runId };
```

> agent-graph.ts 的 State 与 invokeInput 加 `runId: number`（Annotation 普通字段），ctx 构造透传。前端无感知。

- [ ] **Step 6: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/tool-registry.test.ts; if ($?) { npm test }
git add server/src/db/entities/agent.entities.ts server/src/modules/agent/agent.module.ts server/src/modules/agent/tool-registry.ts server/src/modules/agent/agent.service.ts server/src/modules/agent/agent-graph.ts server/src/modules/agent/__tests__/tool-registry.test.ts
git commit -m "feat: 任务三件套工具——task_list/output/stop模型可管理+agent_tasks持久化+runId隔离（2新用例全绿）"
```

> 注意：`pushTaskEvent` 方法体在 Task 3.3 实现（当前为空实现 `private pushTaskEvent(): void {}` 避免编译错误）。

---

### Task 3.3: generate_document 后台化 + SSE task 事件

**Files:**
- Modify: `server/src/modules/agent/agent.service.ts`（background 模式 + pushTaskEvent + runQueues）
- Modify: `server/src/modules/agent/agent-graph.ts`（AgentEvent 加 task_start/task_end）
- Modify: `src/frontend/entries/agent-main.ts`（Ev 类型预留，Task 4.3 完整渲染）
- Test: `server/src/modules/agent/__tests__/agent-task.test.ts`（追加后台模式用例——逻辑层验证）

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/agent-task.test.ts 追加
describe('generate_document 后台模式（逻辑层）', () => {
  it('background=true 时不阻塞：任务状态由回调异步收敛', async () => {
    const m = new AgentTaskManager();
    const done = vi.fn();
    const taskId = 'doc_t1';
    const rec = m.start({ taskId, runId: 1, kind: 'document', description: '教案渲染' });
    // 模拟 agent.service 中后台执行体
    void (async () => {
      await new Promise((r) => setTimeout(r, 5));
      m.transition(taskId, 'completed', '/api/v1/files/9/download');
      done();
    })();
    expect(rec.state).toBe('running'); // 立即返回，未阻塞
    await new Promise((r) => setTimeout(r, 15));
    expect(m.get(taskId)?.state).toBe('completed');
    expect(done).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/agent-task.test.ts`
Expected: FAIL（用例不存在）

- [ ] **Step 3: agent-graph 事件扩展**

```ts
// server/src/modules/agent/agent-graph.ts —— AgentEvent 追加：
| { type: 'task_start'; taskId: string; kind: string; description: string }
| { type: 'task_end'; taskId: string; state: string; outputSummary?: string }
```

- [ ] **Step 4: agent.service 后台模式 + 事件路由**

```ts
// server/src/modules/agent/agent.service.ts
import { AgentTaskManager, type AgentTaskInfo } from './agent-task';

// AgentService 字段：
private readonly taskManager = new AgentTaskManager();
private runQueues = new Map<number, EventQueue>();

// stream() 内：run 创建后注册队列（供任务回调推送）：
this.runQueues.set(run.id, queue);
// runPromise finally 中清理：
this.runQueues.delete(run.id);

// pushTaskEvent 实现：
private pushTaskEvent(runId: number, rec: AgentTaskInfo): void {
  const q = this.runQueues.get(runId);
  if (!q) return;
  if (rec.state === 'running') {
    q.push({ type: 'task_start', taskId: rec.taskId, kind: rec.kind, description: rec.description });
  } else {
    q.push({ type: 'task_end', taskId: rec.taskId, state: rec.state, outputSummary: rec.outputSummary });
  }
}

// buildRegistry 的 generate_document 绑定改为：
generate_document: async (ctx, args) => {
  const input = args as GenerateDocumentInput & { background?: boolean };
  if (!input.background) {
    const out = await this.office.generateDocument(ctx.user, input);
    if (!out.valid) {
      return { error: `文档校验失败：${(out.issues ?? []).map((i) => `${i.code}:${i.fix}`).join('；')}`, issues: out.issues };
    }
    return out;
  }
  // Kimi run_in_background 语义：立即返回任务句柄，渲染在后台完成，模型用 task_list/task_output 查询
  const taskId = `doc_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
  this.taskManager.start({
    taskId,
    runId: ctx.runId ?? 0,
    kind: 'document',
    description: `文档生成：${input.title ?? input.format}`,
  });
  void (async () => {
    try {
      const out = await this.office.generateDocument(ctx.user, input);
      if (!out.valid) {
        this.taskManager.transition(taskId, 'failed', `校验失败：${(out.issues ?? []).map((i) => i.code).join(',')}`);
        return;
      }
      this.taskManager.transition(taskId, 'completed', out.downloadUrl ?? out.filename ?? '已生成');
    } catch (e) {
      this.taskManager.transition(taskId, 'failed', (e as Error).message.slice(0, 200));
    }
  })();
  return { taskId, state: 'running', hint: '任务已在后台启动，稍后可用 task_list / task_output 查询进度与结果' };
},
```

- [ ] **Step 5: persistEvent 补 task 事件落库**

```ts
// agent.service.ts persistEvent 的 task_start/task_end 分支：
case 'task_start': {
  const td = d as { taskId?: string; kind?: string; description?: string };
  await this.msgs.save(this.msgs.create({ ...base, role: 'system', kind: 'task', tool: td.taskId, content: td.description, status: 'running' }));
  break;
}
case 'task_end': {
  const td = d as { taskId?: string; state?: string; outputSummary?: string };
  await this.msgs.save(this.msgs.create({ ...base, role: 'system', kind: 'task', tool: td.taskId, content: td.outputSummary ?? '', status: td.state === 'completed' ? 'done' : 'error' }));
  break;
}
```

- [ ] **Step 6: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/agent-task.test.ts; if ($?) { npm test; npm run lint }
git add server/src/modules/agent/agent.service.ts server/src/modules/agent/agent-graph.ts server/src/modules/agent/__tests__/agent-task.test.ts
git commit -m "feat: generate_document后台模式——任务句柄立即返回+SSE task事件+进度可查可停（1新用例全绿）"
```

**Phase 3 验收：** `npm test` 全绿；手动验证（demo 模式）：对话"帮我生成一份教案文档"后 `GET /agent/runs/:id` 回放含 task 消息；SQLite `agent_tasks` 表有记录。

---

## Phase 4：备课流水线 + 子智能体（DSH 组合子 + Kimi 委派收窄）

### Task 4.1: 流水线执行器 + 备课模板（DSH pipeline 组合子）

**Files:**
- Create: `server/src/modules/agent/pipeline.ts`
- Modify: `server/src/modules/agent/agent-graph.ts`（demo generate 意图接入流水线）
- Test: `server/src/modules/agent/__tests__/pipeline.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/pipeline.test.ts
import { describe, expect, it, vi } from 'vitest';
import { buildRegistry, type ToolBindings, type ToolContext } from '../tool-registry';
import { LESSON_PLAN_PIPELINE, renderArgs, runPipeline } from '../pipeline';

const ctx: ToolContext = { userId: 5, role: 'teacher', user: { id: 5, username: 'u5', role: 'teacher', jti: 'j', scopeKey: '5' }, runId: 1 };

function makeBindings() {
  const calls: string[] = [];
  const b = {
    search_knowledge: vi.fn(async () => [{ ref: 'chunk:1' }]),
    get_class_overview: vi.fn(async () => ({ total: 42 })),
    generate_lesson_plan: vi.fn(async () => ({ id: 1, title: '草船借箭教案' })),
    researcher_comment: vi.fn(async () => ({ score: 88 })),
  } as unknown as ToolBindings;
  return { b, calls };
}

describe('pipeline', () => {
  it('renderArgs 变量替换', () => {
    expect(renderArgs({ query: '${topic}', topK: 5 }, { topic: '草船借箭' })).toEqual({ query: '草船借箭', topK: 5 });
    expect(renderArgs({ classId: '${classId}' }, { classId: 1 })).toEqual({ classId: '1' });
  });

  it('备课流水线：并行检索+学情 → 教案 → 质检，引用回流', async () => {
    const { b } = makeBindings();
    const r = buildRegistry(b as ToolBindings);
    const events: string[] = [];
    const out = await runPipeline(r, ctx, LESSON_PLAN_PIPELINE, { subject: '语文', grade: '五年级', topic: '草船借箭', classId: 1, planSummary: '' }, (ev) => { events.push(ev.type); return Promise.resolve(); });
    expect(out.toolCalls.map((t) => t.tool)).toEqual(['search_knowledge', 'get_class_overview', 'generate_lesson_plan', 'researcher_comment']);
    expect(out.refs).toContain('chunk:1');
    expect(events).toContain('tool_start');
    expect(events).toContain('tool_end');
  });

  it('capture 机制：上一步结果 JSON 注入后续步骤变量', async () => {
    const { b } = makeBindings();
    const r = buildRegistry(b as ToolBindings);
    await runPipeline(r, ctx, LESSON_PLAN_PIPELINE, { subject: '语文', grade: '五年级', topic: '草船借箭', classId: 1, planSummary: '' });
    const src = (b.researcher_comment as ReturnType<typeof vi.fn>).mock.calls[0][1] as string;
    expect(src).toContain('草船借箭教案');
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/pipeline.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现流水线**

```ts
// server/src/modules/agent/pipeline.ts
import type { ToolCallRecord, ToolContext, ToolRegistry } from './tool-registry';
import type { AgentEvent } from './agent-graph';

export type PipelineStep =
  | { kind: 'tool'; tool: string; args: Record<string, unknown>; label: string; capture?: string }
  | { kind: 'parallel'; label: string; steps: PipelineStep[] };

export interface PipelineTemplate {
  name: string;
  description: string;
  steps: PipelineStep[];
}

export type PipelineVars = Record<string, string | number>;

/** '${topic}' → vars.topic（DSH workflow 组合子的模板化参数） */
export function renderArgs(args: Record<string, unknown>, vars: PipelineVars): Record<string, unknown> {
  const out: Record<string, unknown> = {};
  for (const [k, v] of Object.entries(args)) {
    out[k] = typeof v === 'string' ? v.replace(/\$\{(\w+)\}/g, (_, name: string) => String(vars[name] ?? '')) : v;
  }
  return out;
}

export interface PipelineResult {
  toolCalls: ToolCallRecord[];
  refs: string[];
  needsHuman: boolean;
}

/**
 * 流水线执行器（DSH pipeline 组合子的轻量版）：串行步 + 并行屏障。
 * 与自由 ReAct 互补：固定高价值流程用模板保证结构一致性，其余交给模型自主。
 * 安全设计：流水线模板为预置可信流程，工具调用走 registry 内建权限校验（垂直/水平越权），
 * 但不挂审批策略链 hooks——审批仅作用于 LLM 自由发起的工具调用（agent-graph 内透传 pipelineHooks）。
 */
export async function runPipeline(
  registry: ToolRegistry,
  ctx: ToolContext,
  template: PipelineTemplate,
  vars: PipelineVars,
  onEvent?: (ev: AgentEvent) => void | Promise<void>,
): Promise<PipelineResult> {
  const liveVars: PipelineVars = { ...vars };
  const toolCalls: ToolCallRecord[] = [];
  const refs: string[] = [];
  let needsHuman = false;

  const callTool = async (step: { tool: string; args: Record<string, unknown>; capture?: string }) => {
    const args = renderArgs(step.args, liveVars);
    if (onEvent) await onEvent({ type: 'tool_start', name: step.tool, args });
    const rec = await registry.call(ctx, step.tool, args);
    toolCalls.push(rec);
    if (onEvent) await onEvent({ type: 'tool_end', name: step.tool, result: rec.result, error: rec.error, durationMs: rec.durationMs });
    if (step.tool === 'search_knowledge' && (rec.result as Array<{ ref: string }> | undefined)?.length) {
      (rec.result as Array<{ ref: string }>).forEach((c) => refs.push(c.ref));
    }
    if (rec.error) needsHuman = true;
    if (step.capture) {
      liveVars[step.capture] = JSON.stringify(rec.result ?? {}).slice(0, 1500);
    }
    return rec;
  };

  const runStep = async (step: PipelineStep): Promise<void> => {
    if (step.kind === 'tool') await callTool(step);
    else await Promise.all(step.steps.map((s) => runStep(s)));
  };

  for (const step of template.steps) await runStep(step);
  return { toolCalls, refs, needsHuman };
}

/** 备课流水线模板：教材检索+班级学情并行 → 教案生成 → 教研质检（自愈闭环由质检结果驱动） */
export const LESSON_PLAN_PIPELINE: PipelineTemplate = {
  name: 'lesson_plan',
  description: '备课流水线：并行检索教材与学情 → 教案生成 → 教研点评质检',
  steps: [
    {
      kind: 'parallel',
      label: '并行：教材检索 + 班级学情',
      steps: [
        { kind: 'tool', tool: 'search_knowledge', args: { query: '${topic}', topK: 5 }, label: '教材检索' },
        { kind: 'tool', tool: 'get_class_overview', args: { classId: '${classId}' }, label: '学情概览' },
      ],
    },
    { kind: 'tool', tool: 'generate_lesson_plan', args: { subject: '${subject}', grade: '${grade}', topic: '${topic}' }, label: '教案生成', capture: 'planSummary' },
    { kind: 'tool', tool: 'researcher_comment', args: { sourceContent: '${planSummary}' }, label: '教研质检' },
  ],
};
```

- [ ] **Step 4: demo generate 意图接入流水线**

```ts
// server/src/modules/agent/agent-graph.ts
import { LESSON_PLAN_PIPELINE, runPipeline } from './pipeline';

// demo 模式 switch 的 case 'generate' 改为：
case 'generate': {
  if (s.onEvent) await s.onEvent({ type: 'thinking', text: '进入备课流水线：并行检索教材与学情 → 教案生成 → 教研质检。' });
  const out = await runPipeline(registry, ctx, LESSON_PLAN_PIPELINE, { subject: '语文', grade: '五年级', topic: '草船借箭', classId: 1, planSummary: '' }, s.onEvent);
  toolCalls.push(...out.toolCalls);
  refs.push(...out.refs);
  needsHuman = out.needsHuman;
  break;
}
// 注意：refs 需可变（当前 const refs: string[] 已是数组引用，push 可用）
```

- [ ] **Step 5: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/pipeline.test.ts; if ($?) { npm test }
git add server/src/modules/agent/pipeline.ts server/src/modules/agent/__tests__/pipeline.test.ts server/src/modules/agent/agent-graph.ts
git commit -m "feat: 备课流水线——并行检索+学情→教案→教研质检模板（3新用例全绿，demo generate 接入）"
```

> 注意：agent-graph.test.ts 中 demo generate 用例（`expect(out.toolCalls.map((t) => t.tool)).toEqual(...)`）需按新流水线序列更新断言为 `['search_knowledge', 'get_class_overview', 'generate_lesson_plan', 'researcher_comment']`。

---

### Task 4.2: 子智能体执行器（白名单收窄 + 深度预算）

**Files:**
- Create: `server/src/modules/agent/subagent.ts`
- Modify: `server/src/modules/agent/pipeline.ts`（parallel 步走子代理执行）
- Test: `server/src/modules/agent/__tests__/subagent.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// server/src/modules/agent/__tests__/subagent.test.ts
import { describe, expect, it, vi } from 'vitest';
import { buildRegistry, type ToolBindings, type ToolContext } from '../tool-registry';
import { MAX_SUBAGENT_DEPTH, runSubagent } from '../subagent';

const ctx: ToolContext = { userId: 5, role: 'teacher', user: { id: 5, username: 'u5', role: 'teacher', jti: 'j', scopeKey: '5' } };

describe('runSubagent', () => {
  it('白名单收窄：白名单外工具被拒绝并记入 refused（委派权限固定）', async () => {
    const b = {
      search_knowledge: vi.fn(async () => []),
      get_class_overview: vi.fn(async () => ({ total: 1 })),
    } as unknown as ToolBindings;
    const r = buildRegistry(b);
    const out = await runSubagent(r, ctx, { prompt: '检索', allowedTools: ['search_knowledge'] }, [{ tool: 'get_class_overview', args: { classId: 1 }, label: '越权尝试' }]);
    expect(out.toolCalls).toHaveLength(0);
    expect(out.refused).toEqual(['get_class_overview']);
  });

  it('白名单内工具正常执行，结果独立回流', async () => {
    const b = {
      search_knowledge: vi.fn(async () => [{ ref: 'chunk:9' }]),
    } as unknown as ToolBindings;
    const r = buildRegistry(b);
    const out = await runSubagent(r, ctx, { prompt: '检索', allowedTools: ['search_knowledge'] }, [{ tool: 'search_knowledge', args: { query: '草船' }, label: '检索' }]);
    expect(out.toolCalls).toHaveLength(1);
    expect(out.toolCalls[0].result).toEqual([{ ref: 'chunk:9' }]);
  });

  it('深度超限拒绝委派（防递归爆炸）', async () => {
    const r = buildRegistry({} as ToolBindings);
    const out = await runSubagent(r, ctx, { prompt: 'x', allowedTools: [], depth: MAX_SUBAGENT_DEPTH + 1 }, []);
    expect(out.finalText).toContain('深度超限');
  });
});
```

- [ ] **Step 2: 运行验证失败**

Run: `npx vitest run src/modules/agent/__tests__/subagent.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现子代理执行器**

```ts
// server/src/modules/agent/subagent.ts
import type { ToolCallRecord, ToolContext, ToolRegistry } from './tool-registry';
import type { AgentEvent } from './agent-graph';

export const MAX_SUBAGENT_DEPTH = 3;

export interface SubagentOptions {
  prompt: string;
  /** 工具白名单（显式声明，缺省拒绝一切——DSH 委派权限固定 + Kimi disallowedTools 双校验） */
  allowedTools: string[];
  depth?: number;
}

export interface SubagentStep {
  tool: string;
  args: Record<string, unknown>;
  label: string;
}

export interface SubagentResult {
  finalText: string;
  toolCalls: ToolCallRecord[];
  refused: string[];
}

/**
 * 子智能体执行器（第一期：受限作用域的确定性步骤执行，为分节并行备课打底）：
 * 1. 权限收窄：仅 allowedTools 白名单内可执行，其余记 refused（拒绝执行而非报错中断）；
 * 2. 上下文隔离：结果只经返回值回流，不写入主对话 messages（Kimi subagent 独立上下文）；
 * 3. 深度预算：嵌套委派超 MAX_SUBAGENT_DEPTH 直接拒绝（DSH resolveChildDepth）。
 */
export async function runSubagent(
  registry: ToolRegistry,
  ctx: ToolContext,
  opts: SubagentOptions,
  steps: SubagentStep[],
  onEvent?: (ev: AgentEvent) => void | Promise<void>,
): Promise<SubagentResult> {
  const depth = Math.max(1, opts.depth ?? 1);
  if (depth > MAX_SUBAGENT_DEPTH) {
    return { finalText: `子代理深度超限（${depth} > ${MAX_SUBAGENT_DEPTH}），拒绝委派`, toolCalls: [], refused: [] };
  }
  const allowed = new Set(opts.allowedTools);
  const toolCalls: ToolCallRecord[] = [];
  const refused: string[] = [];

  for (const step of steps) {
    if (!allowed.has(step.tool)) {
      refused.push(step.tool);
      if (onEvent) await onEvent({ type: 'thinking', text: `子代理「${opts.prompt.slice(0, 30)}」被拒工具：${step.tool}（不在白名单）` });
      continue;
    }
    if (onEvent) await onEvent({ type: 'tool_start', name: step.tool, args: step.args });
    const rec = await registry.call(ctx, step.tool, step.args);
    toolCalls.push(rec);
    if (onEvent) await onEvent({ type: 'tool_end', name: step.tool, result: rec.result, error: rec.error, durationMs: rec.durationMs });
  }

  return {
    finalText: `子代理完成：${toolCalls.length} 步成功，${refused.length} 步被白名单拒绝`,
    toolCalls,
    refused,
  };
}
```

- [ ] **Step 4: pipeline.parallel 走子代理（每步一个子代理，结果合并）**

```ts
// server/src/modules/agent/pipeline.ts
import { runSubagent } from './subagent';

// runStep 的 parallel 分支改为：
else {
  const results = await Promise.all(
    step.steps.map((s) =>
      s.kind === 'tool'
        ? runSubagent(registry, ctx, { prompt: s.label, allowedTools: [s.tool] }, [{ tool: s.tool, args: s.args, label: s.label }], onEvent)
        : runStep(s).then(() => null),
    ),
  );
  for (const r of results) {
    if (!r) continue;
    toolCalls.push(...r.toolCalls);
    if (r.refused.length) needsHuman = true;
    for (const tc of r.toolCalls) {
      if (tc.tool === 'search_knowledge' && (tc.result as Array<{ ref: string }> | undefined)?.length) {
        (tc.result as Array<{ ref: string }>).forEach((c) => refs.push(c.ref));
      }
      if (tc.error) needsHuman = true;
    }
  }
}
```

> 注意：`toolCalls/refs/needsHuman` 需从 callTool 闭包提升为 runPipeline 函数级 let 变量，callTool 与 runStep 共用（重构点：现 `const toolCalls: ToolCallRecord[] = []` 已是函数级，callTool 直接 push 可继续用；refs/needsHuman 同理）。

- [ ] **Step 5: 运行测试 + 提交**

```bash
cd server
npx vitest run src/modules/agent/__tests__/subagent.test.ts; if ($?) { npm test; npm run lint }
git add server/src/modules/agent/subagent.ts server/src/modules/agent/__tests__/subagent.test.ts server/src/modules/agent/pipeline.ts
git commit -m "feat: 子智能体执行器——白名单收窄+深度预算+独立上下文（3新用例全绿，parallel步委派化）"
```

**Phase 4 验收（后端）：** `npm test` 全绿；demo 模式"备一节课"产出 4 步流水线轨迹（检索+学情并行 → 教案 → 教研点评）。

---

### Task 4.3: 前端适配（usage/task 事件渲染）

**Files:**
- Modify: `src/frontend/entries/agent-main.ts`（Ev 类型 + 渲染分支）
- Modify: `public/agent.html`（如需样式类，沿用现有 .tool-meta 风格，无需改动）

- [ ] **Step 1: 扩展事件类型**

```ts
// src/frontend/entries/agent-main.ts —— Ev 类型追加：
| { type: 'usage'; inputTokens: number; outputTokens: number }
| { type: 'task_start'; taskId: string; kind: string; description: string }
| { type: 'task_end'; taskId: string; state: string; outputSummary?: string };
```

- [ ] **Step 2: 渲染分支（SSE handler switch 内追加）**

```ts
// 找到处理 ev.type 的 switch/if 链（约第 119-162 行），追加：
case 'usage':
  renderUsage(ev);
  break;
case 'task_start':
  renderTaskStart(ev);
  break;
case 'task_end':
  renderTaskEnd(ev);
  break;

// 文件底部追加渲染函数：
function renderUsage(ev: { inputTokens: number; outputTokens: number }) {
  const box = document.createElement('div');
  box.className = 'usage-box';
  box.innerHTML = `⚡ 本次消耗 tokens：输入 <b>${ev.inputTokens}</b> / 输出 <b>${ev.outputTokens}</b>`;
  const card = document.createElement('div');
  card.className = 'step usage';
  card.appendChild(box);
  appendToStream(card); // 与现有流容器一致的挂载点（沿用已有输出卡片逻辑）
}

const taskCards = new Map<string, HTMLElement>();

function renderTaskStart(ev: { taskId: string; description: string }) {
  const card = document.createElement('div');
  card.className = 'step task';
  card.innerHTML = `<div class="step-head"><span class="ico">📦</span><span>后台任务</span></div>
    <div class="tool-card"><div class="tool-name">${esc(ev.description)} <span class="tool-meta tool-running">处理中…</span></div></div>`;
  appendToStream(card);
  taskCards.set(ev.taskId, card);
}

function renderTaskEnd(ev: { taskId: string; state: string; outputSummary?: string }) {
  const card = taskCards.get(ev.taskId);
  if (!card) return;
  const meta = card.querySelector('.tool-meta') as HTMLElement | null;
  if (meta) {
    const ok = ev.state === 'completed';
    meta.textContent = ok ? '✅ 完成' : '❌ ' + ev.state;
    meta.className = 'tool-meta ' + (ok ? 'tool-done' : 'tool-error');
  }
  if (ev.outputSummary && ev.outputSummary.startsWith('/')) {
    const a = document.createElement('a');
    a.href = ev.outputSummary;
    a.textContent = '下载结果';
    a.className = 'download-link';
    card.querySelector('.tool-card')?.appendChild(a);
  }
}
```

> `appendToStream(card)`：如现有代码无此函数，则在 SSE 主渲染链末尾追加 `card` 到现有输出容器（参照 `renderText` 的挂载方式）。

- [ ] **Step 3: 构建验证 + 提交**

```bash
node build-frontend.mjs; if ($?) { git add src/frontend/entries/agent-main.ts public/assets/app/agent-main.js; git commit -m "feat: 前端usage统计+tasks任务卡片渲染——后台任务进度可视化" }
```

> 注：构建产物路径以 build-frontend.mjs 实际输出为准（public/assets/app/*.js）。

**Phase 4 验收（前端）：** 浏览器打开 agent.html，demo 模式执行"生成文档（后台）"：出现"处理中…"任务卡片 → 完成后变"✅ 完成"+ 下载链接；对话末尾展示 token 消耗徽章。

---

## 全量验证清单（每 Phase 完成后与最终交付前执行）

1. `cd server; npm test` — 全量单测（原 57 + 新增 ~30 用例）全绿
2. `cd server; npm run lint` — 无 lint 错误
3. `node server/test-agent-llm.mjs` — 真实 DeepSeek 四端链路回归（off 审批模式行为不变）
4. `node server/data/smoke-login.js` 与安全自检脚本 — 五层守卫链无回归
5. 手动验收：demo 模式"备一节课"（流水线 4 步轨迹）、"生成文档"（后台任务卡片）、`GET /agent/runs/:id`（回放含 seq/time/usage/stats）

## 风险与回滚

| 风险 | 缓解 |
|---|---|
| SQLite 自动加列失败（旧库无列） | `DB_SYNCHRONIZE` 默认 true（app.module.ts:42）；若关闭则手工 `ALTER TABLE agent_runs ADD COLUMN ...` |
| 审批模式误开 strict 破坏现有流程 | 默认 `AGENT_APPROVAL_MODE=off` 全放行；strict 仅在配置后生效；每个 Task 独立提交可单独回滚 |
| Task 3.3 后台任务进程崩溃丢任务 | 重启后 `markLost()` 标 lost（状态可见不误报）；任务结果文件本身持久于 files 表 |
| 流水线 demo 断言变更引发旧测试失败 | Task 4.1 Step 5 明确同步更新 agent-graph.test.ts 断言 |
| 前端 appendToStream 与现有挂载不一致 | Task 4.3 以现有 renderText 挂载方式为准，构建产物一并提交 |

## 后续演进（本计划外，视需要另行立项）

- 审批 UI 应答者（SSE 推审批请求 + 前端确认面板 → ApprovalService 决策），激活 strict 模式全链路
- LLM 模式接入流水线模板（模型产出流水线参数 → runPipeline）
- 子代理分节并行备课（五段教学过程各一子代理，独立上下文撰写后合并）
- Kimi 式 Goal 自治（教师长任务预算：token/turn/墙钟硬停）
